{
  "mcpServers": {
    "doctor": {
      "_comment": "v2.2 mechanical gate — lane-check, validate-map, rotate-check, runtime-scan. Doctrine source repo dogfoods own gates. Binary: ~/doctor v0.1.0.",
      "command": "/Users/nguyenhuuanh/.cargo/bin/doctor",
      "args": [
        "serve"
      ]
    }
  }
}
