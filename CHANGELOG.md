# Changelog

All notable changes to sos-kit. Format loosely follows Keep a Changelog. Versions are wave-based, not date-based.

## v2.3 forge (in progress) — Phiếu path + sentinel + agents-drift cure + portability architecture — 2026-07-22

**[P080] Dual-runtime brownfield dogfood — FAIL, 1 gap found (2026-07-23):**
- Ran the `[Thợ-local]` half of the P080 test matrix (Nhóm A fresh-dual incl.
  A2-reverse + A5-uninstall, Nhóm B brownfield, Nhóm D regression incl.
  sync/map dual smoke) on fresh git fixtures (NOT the sos-kit checkout).
  Nhóm C (cross-runtime state, real `codex exec`) and E2 (Linux) stay
  `[Sếp+Codex]` PENDING per phiếu scope.
- **Dual render co-exists cleanly** both orders (Claude `sos new`/`sos adopt`
  + Codex `sos install --runtime codex`): non-clobber holds, `core.hooksPath`
  arms exactly once, no cross-runtime file destruction, `sos sync`/`sos map`
  smoke-clean on a dual-installed repo.
- **D1 FAIL (HIGH):** the dev `[8/8]` `hooks/pre-commit` (rendered only by
  `sos new`, Claude path) is fail-OPEN on `[6/8]` no-code-on-default and
  `[7/8]` block-env-commit when their delegated scripts are missing — the
  exact "round-4 A3/A4" bug the Codex-side backstop-minimal hook (P078i) was
  supposed to have fixed. The fix only landed in the backstop hook, never
  ported back to the dev hook. Proved live: remove
  `scripts/block-env-commit.sh` + rebaseline trust-gate → a real `.env`
  secret commits cleanly (exit 0). Recommend `P080x-hook-fail-open-parity`
  (Tầng 1, Debate) to port the fail-closed pattern into the dev hook.
- **A5 N/A (not FAIL):** `uninstall()`/`RemovalPlan` is an honest empty stub
  on both adapters, with no `sos uninstall` CLI subcommand wired at all —
  same known-gap class as `ClaudeAdapter::plan()` (anchor #5).
- A4 caveat (not a FAIL): on a brand-new zero-commit repo,
  `no-code-on-default.sh`'s `refs/heads/main` fallback resolution can't see
  the not-yet-created branch ref → warns+allows on the very first commit.
  Narrow window, pre-existing, worth a note in the same gap ticket.
- **P081 distribution stays gated** until P080x closes and this round re-runs
  green.
- Full report: `docs/adapters/P080-FINDINGS-2026-07-23.md`. Discovery:
  `docs/discoveries/P080.md`.

**[P078j] FIX (SECURITY) — Codex adapter: guard path-matching upgraded from lexical strip to symlink-safe canonicalize — closes P079 round-4 B4 for real (2026-07-23):**
- **Gap closed (P079 round-4 §B4, `docs/adapters/P079-ROUND4-FINDINGS-2026-07-23.md` `:149-191`, "New/remaining gaps" #2):** P078h's `apply_patch` path-normalize was a plain literal `$REPO_ROOT/` string-prefix strip, not filesystem-equivalent. On macOS `/tmp` is a symlink to `/private/tmp`; `git rev-parse --show-toplevel` returns the canonical `/private/tmp/...` form, so a patch path spelled `/tmp/...` never shared a literal prefix with `REPO_ROOT` even though it names the identical file. This broke BOTH directions: a marked actor's forbidden self-advance (`V3/V3` → `V4/V4`) LEAKED through the alias (early-BLOCK's path compare never matched `STATE_FILE`), and a legitimate main-thread approval write via the same alias was false-BLOCKed (never won the state-file-alone exemption).
- **Fix:** `crates/sos-adapter-codex/src/templates.rs` (`guard_approval_gate_sh()`) now canonicalizes BOTH `REPO_ROOT` (`cd "$REPO_ROOT" && pwd -P`, already `cd`'d there) AND every absolute candidate patch path BEFORE the prefix-strip/compare — candidate canonicalize resolves the DIRNAME only (`cd "$(dirname PATH)" && pwd -P`, re-appending the basename), which stays correct for a file that does not yet exist on disk (bootstrap-create case, P078d2a #5/B1) and is portable to macOS BSD `pwd`/`cd` (no GNU `readlink -f`/`realpath -m` assumed). One normalize site, same two consumers as P078h (state-file-alone exemption + d2a multi-path all-path check). Does NOT alter the P078h early-BLOCK/exemption decision logic — only the path forms fed into it. Conservative fail-closed preserved: an unresolvable dirname or a path canonicalizing outside `REPO_ROOT` is never granted the exemption.
- **`|| true` fix for `set -e`:** the dirname-resolve `cd ... && pwd -P` command substitution is a plain assignment statement — under `set -euo pipefail`, a failed `cd` (dirname doesn't exist) would otherwise abort the whole script; appended `|| true` so it degrades to an empty `RESOLVED_DIR` (fail-closed fallthrough) instead.
- Tests (`crates/sos-adapter-codex/src/lib.rs`, 4 new + reuse of 2 pre-existing P078h absolute-path tests for canonical/relative no-regress): a `run_guard_symlink()` fixture harness (real dir + separate symlink alias, guard's cwd bound to the real dir, payload built via the alias) reproduces round-4 B4 directly without depending on any OS-specific mount — marked-actor advance via alias → BLOCK, main-thread approval via alias → ALLOW, bundle-via-alias (marked-actor oracle) → BLOCK (multi-path no-regress), path-trick-outside-`REPO_ROOT` → fail-closed (never exempted). **Two-directional negative-test performed and reverted:** stashing the Task-1 canonicalize change flips the marked-actor-advance test BLOCK→ALLOW (leak reproduces) AND the main-thread-approval test ALLOW→BLOCK (false-block reproduces) simultaneously — confirmed red on both, then restored (fix re-verified green, 64/64 pass, ×20 flake-clean).
- Docs: `SECURITY.md` (new "Path-matching upgraded to symlink-safe canonicalize" section), `adapters/codex/CAPABILITY.md` (P078j addendum under the P078h entry), `docs/BACKLOG.md`.
- Full report: `docs/discoveries/P078j.md`.

**[P078i] FIX (SECURITY) — `sos install` now renders a self-contained, fail-CLOSED backstop `hooks/pre-commit` (not the dev [8/8] hook) — closes P079 round-4 A3/A4 for real (2026-07-23):**
- **Gap closed (P079 round-4 `docs/adapters/P079-ROUND4-FINDINGS-2026-07-23.md`, Test A3/A4 FAIL, "structural-oracle-gap lần 3"):** P078g (below) correctly rendered + armed `hooks/pre-commit`/`hooks/pre-push`, but the CONTENT it rendered was the kit's own dev `[8/8]` hook — built for `sos new` dev checkouts, delegating every phase to `scripts/*.sh` + `docs-gate`, and **fail-OPEN** when a delegated script is absent (`echo "... missing"` then falls through, no `exit`). A fresh `sos install` into a brownfield repo never renders that `scripts/` tree — both required probes (`.env` block, no-code-on-default) silently passed on real commits (`ENV_COMMITTED=yes`, `CODE_COMMITTED=yes`).
- **Fix — route (a), minimal purpose-built backstop calling embedded verbatim guards (Debate Log Turn 1 recommendation, EXECUTE confirmed feasible):** new template `crates/sos-install/src/templates/backstop-pre-commit.sh` (embedded `EMBEDDED_BACKSTOP_PRE_COMMIT`) enforces ONLY 2 invariants — `.env*` block + no-code-on-default — no docs-gate/trust-gate/type-check phases. **fail-CLOSED, the opposite of the round-4 bug:** each phase checks its delegated guard exists first; if absent, `exit 1` with a `BLOCKED:` message — never "missing → allowed". `render_embedded_hooks()` (`crates/sos-install/src/engine.rs`) now co-renders the backstop hook AND its 2 delegated guard scripts (`scripts/block-env-commit.sh`, `scripts/no-code-on-default.sh` — embedded verbatim, single-source with the kit's own copies, `EMBEDDED_BLOCK_ENV`/`EMBEDDED_NO_CODE_DEFAULT`) in the same render pass, so a fresh install's output is a fully closed dependency set; all 4 rendered artifacts get chmod +x immediately after write.
- **`sos new` (dev projects) untouched:** still copies the real dev `[8/8]` `hooks/pre-commit` + full `scripts/` tree at runtime via `crates/sos-cli/src/commands/new.rs:312`'s `copy_tree` — a code path that never calls `engine::apply()`/`render_embedded_hooks()` (confirmed zero overlap, Worker CHALLENGE Turn 1). `hooks/pre-push` unaffected (self-contained advisory-only, no delegated scripts, no closure risk — kept as-is for both `sos new` and `sos install`).
- **Structural-oracle-gap lần 3, closed with a dependency-closure assertion:** P078f's fixture seeded hook content into the synthetic `Plan` (missed "install never renders the hook"); P078g's fixture seeded `scripts/block-env-commit.sh` directly (missed "install renders a hook referencing a script install itself never writes"). New test `pristine_install_renders_closed_dependency_set_and_blocks_real_env_and_code_commits` runs against a git repo with **ZERO seeding** (no `hooks/`, no `scripts/` beforehand), then greps the rendered hook for every `scripts/*.sh` reference and asserts each exists among that same install run's own output — closing the recursive gap mechanically.
- Tests (`crates/sos-install/tests/install.rs`, 4 new + 1 updated): dependency-closure pristine oracle (real `.env` block, real code-on-default block, closure assertion, clean-commit negative control), fail-closed-when-guard-absent (delete a just-rendered guard, confirm next `.env` commit is STILL blocked), 2 template-content assertions (references only its 2 deps, never docs-gate/trust-gate/inv-gate/python/install-hooks.sh). `cargo test -p sos-install`: 19/19 pass; `cargo test --workspace` green; `cargo build --release` clean.
- Docs: `SECURITY.md` (new "self-contained backstop" section, gap-history count 2→3), `adapters/codex/CAPABILITY.md` (gap #4 correction — round-4 found the P078g "genuinely blocked" claim was conditional, now unconditionally true), `docs/BACKLOG.md`.
- Full report: `docs/discoveries/P078i.md`.

**[P078g] FIX (SECURITY) — `sos install` now RENDERS real `hooks/pre-commit`+`pre-push` before arming; never arms an empty `hooksPath` (2026-07-23):**
- **Gap closed (P079 round-3 `docs/adapters/P079-ROUND3-FINDINGS-2026-07-23.md`, Test A2/A3/A4 FAIL):** P078f (`d35f462`) armed `core.hooksPath=hooks` but neither runtime's adapter `plan()` ever rendered `hooks/pre-commit`/`hooks/pre-push` to disk — the backstop "looked armed" (Test A1 PASS) but Git pointed at an empty dir, so a real `.env` commit (A3) and a real code-on-default commit (A4) both went through unblocked. False-security, worse than no backstop.
- **Route (Worker CHALLENGE Turn 1, Anchor #4 corrected the phiếu's `new.rs`-copy assumption):** `sos install` has NO `SOS_KIT_DIR` — it runs standalone in an arbitrary target repo, unlike `new`/`adopt`/`sync`. Fix uses **compile-time `include_str!`** of `hooks/pre-commit` + `hooks/pre-push` baked into the `sos-install` crate (`crates/sos-install/src/engine.rs`, `EMBEDDED_PRE_COMMIT`/`EMBEDDED_PRE_PUSH`), rendered by a new `render_embedded_hooks()` inside `engine::apply()`'s success path, immediately BEFORE `arm_git_hooks()`. Symmetric for both runtimes by construction (single engine call-site, no per-runtime branch). Non-clobber: a pre-existing hook file with DIFFERENT content is left untouched (warn only), extending F09's non-clobber philosophy to the render step.
- **Harden `arm_git_hooks()` — never arm-empty (Constraint 2):** new `hooks_present_and_executable()` check runs right before `git config --local core.hooksPath hooks` — if either hook script is absent/non-executable, arming is refused (`Err`, loud stderr), `core.hooksPath` is left unset. `arm_git_hooks` made `pub` so this guard is directly testable in isolation from the render step.
- **Tests (`crates/sos-install/tests/install.rs`, 5 new, fresh-no-seed fixtures — fixes P078f's own oracle gap where all 5 hook-arming tests seeded hook content into the synthetic `Plan`):** render-fresh (byte-match kit source + chmod + hooksPath armed AFTER render), **real end-to-end `.env` block** (genuine `git commit` runs the armed `hooks/pre-commit`, which runs the real `scripts/block-env-commit.sh` seeded into the fixture — blocked; clean commit passes as negative control), refuse-when-absent (`arm_git_hooks` called with no rendered hooks → `Err`, hooksPath stays unset), render non-clobber (pre-existing customized `hooks/pre-commit` preserved verbatim). `cargo test -p sos-install`: 15/15 pass (10 pre-existing + 5 new), `cargo check --workspace` clean.
- **Known asymmetry, pre-existing, out-of-scope:** `ClaudeAdapter::plan()` is a total stub (0 rendered assets) — `--runtime claude` renders nothing today regardless of this fix. Not touched here (P078g scope = engine-level hook arming only).
- Docs: `SECURITY.md`, `adapters/codex/CAPABILITY.md`, `docs/BACKLOG.md`.
- Full report: `docs/discoveries/P078g.md`.

**[P078h] FIX (SECURITY) — Codex adapter: actor-check advance-block closes P078e self-approve hole + patch-path normalize (2026-07-23):**
- **Gap #3 (P079 round-3 §B4, `docs/adapters/P079-ROUND3-FINDINGS-2026-07-23.md`):** P078e's actor-check stopped a marked actor (worker/architect subagent) from exercising the state-file-alone exemption, but still fell through to the ordinary version-match check below, which compares pre-patch state only. Starting from an already-approved `version=V3, approved_version=V3`, a worker with `.sos-state/worker-active` present could still write `version=V4, approved_version=V4` because the fall-through check saw `V3 == V3` and let it through — a self-approve **advance** hole, distinct from the create/first-approval hole P078e closed.
- **Fix:** a marked actor is now BLOCKed outright for ANY write touching `.sos-state/ticket-state.env` — create, update, or advance — with NO fall-through to version-match. Version-match is reached only when both markers are absent (main-thread). Main-thread exemption (P078e deadlock fix) unchanged and unregressed.
- **Gap #4 (P079 round-3 "Additional usability observation"):** the state-file-allow exemption compared the extracted patch path to the exact relative string `.sos-state/ticket-state.env`; a live Codex attempt that emitted a repo-root-absolute patch path false-blocked, prompt-shape dependent.
- **Fix:** normalize every extracted patch path (strip a literal `$REPO_ROOT/` prefix, reusing the guard's existing `REPO_ROOT` variable) BEFORE both the state-file-allow compare and the multi-path all-path check consume it — one normalize site, both consumers. Conservative: no symlink/`..` resolution — a path that doesn't cleanly strip to relative (still contains `..`, or was never under `REPO_ROOT`) is left as-is and can never win the exemption, so it falls through to fail-CLOSED.
- Tests (`crates/sos-adapter-codex/src/lib.rs`, 8 new): worker/architect advance-of-approved BLOCK (the gap #3 core oracle), worker create BLOCK, main-thread relative-path no-regress ALLOW, absolute-path normalize ALLOW on main-thread / BLOCK when marked, bundle-with-absolute-path no-regress BLOCK. **Negative-tests performed and reverted:** reverting the early-BLOCK flips the advance-of-approved test BLOCK→ALLOW; reverting the normalize step flips the absolute-path main-thread test ALLOW→false-BLOCK — both confirmed red, then restored.
- Oracle: `cargo check --workspace` clean, `sos-adapter-codex` 60/60 pass, ×20 = 0 flaky, dep-direction green (`sos-core` carries zero adapter import).
- **Additive, untouched:** `crates/sos-install/**`, `crates/sos-cli/**`, `sos-core`, `sos-adapter-claude`, d2a multi-path guard core, SubagentStart marker (d2b), `scripts/orchestrator-guard.sh` — only `crates/sos-adapter-codex/src/{templates.rs,lib.rs}` + docs touched. Independent of/parallel with **P078g** (install arm-hooks, gap #1).
- **Codex caveat unchanged:** strengthens guard LOGIC, does not change whether the guard fires inside a real Codex custom-role subagent spawn (`worker-active`/`architect-active` markers still absent on Codex 0.145.0, upstream `openai/codex#21753`). SOUND on Claude + manual-marker Codex repro; best-effort/inactive on a real Codex custom-role subagent spawn — Git backstop (P078f/P078g) + human-review-at-commit remain the real boundary there.
- Docs: `adapters/codex/CAPABILITY.md` §4 (P078h paragraph), `SECURITY.md` (P078h paragraph before the in-subagent-enforcement section). Trust-gate: `SECURITY.md` unicode-scanned ASCII-clean (`trust-gate.sh check` exit 0); no rebaseline (no `SURFACE_GLOBS` file touched — `crates/**` is Rust source, not a tracked runtime surface).
- Full report: `docs/discoveries/P078h.md`.

**[P078f] FIX (SECURITY) — `sos install` now arms Git hooks by default (`core.hooksPath` + F09 hijack-guard) (2026-07-23):**
- **Gap closed (P079 round-2 `docs/adapters/P079-ROUND2-FINDINGS-2026-07-23.md`, split off P078e per P085 decomposition heuristic):** `sos install` used to render `hooks/{pre-commit,pre-push}` to disk but never activate them — `git config --local core.hooksPath` stayed unset, so the tracked Git boundary never fired unless an adopter separately ran `scripts/install-hooks.sh`. This rendered the "honest-MISSING" story in `adapters/codex/CAPABILITY.md` (Git backstop as the real enforcement layer on Codex) untrue in practice: the declared backstop was off by default.
- **Fix (`crates/sos-install/src/engine.rs`, new `arm_git_hooks()` + helpers):** engine-native port (NOT a shell-out to `install-hooks.sh` — P059/P072 Windows-portability precedent) of the script's 4 steps, run once on the success path of `apply()`, after the manifest is committed: (1) `chmod +x hooks/pre-commit`/`hooks/pre-push` (`#[cfg(unix)]`, no-op on Windows); (2) F09 hijack-guard — if `core.hooksPath` is already set to something other than `hooks`, TTY prompts `[y/N]` (default N), non-TTY **ABORTs** (fail-closed, never silently clobbers an adopter's own hook chain); (3) `git config --local core.hooksPath hooks`; (4) rename (never delete) any stale `.git/hooks/{pre-commit,pre-push}` to `*.pre-hookspath.bak`. Non-git-repo target → warn-skip, install still succeeds. Idempotent: a prior run's own `hooks` value never re-trips the guard.
- **Symmetric by construction:** `crates/sos-cli/src/commands/install.rs`'s `run_adapter()` is the one shared call-site for both `--runtime claude` and `--runtime codex` → both paths through the same `engine::apply()`, no per-runtime branch needed (Task 3 = confirm + comment only).
- Tests (`crates/sos-install/tests/install.rs`, 5 new, real `git init` temp-repo fixtures): hooksPath set + chmod, stale-hook renamed to `.bak` with content preserved, non-clobber abort on foreign hooksPath (non-interactive), non-git-dir warn-skip success, idempotent second run. `cargo test --workspace` green (11/11 in `sos-install`'s `install.rs`, full workspace unaffected).
- Docs: `SECURITY.md` (install-time hook-arming boundary), `adapters/codex/CAPABILITY.md` (Git backstop now armed-by-default, not "off after install"), `docs/plans/P078d-decomposition.md` (P078f marked DONE), `docs/BACKLOG.md`.
- Full report: `docs/discoveries/P078f.md`.

**[P078e Task 1] FIX (SECURITY) — Codex adapter: approval-transition deadlock closed + self-approve actor-check (2026-07-23):**
- **Gap #1 (P079 round-2, `docs/adapters/P079-ROUND2-FINDINGS-2026-07-23.md`):** bootstrap creates state `version=V1, approved_version=empty`; the first legitimate approval write (`version=V2, approved_version=V2`) was BLOCKed by the version-match check because the state file already existed — circular deadlock (approving requires the write, the write requires already-approved).
- **Fix:** `scripts/codex/approval-gate.sh`'s state-file-alone exemption (P078d2a #5, previously create-when-missing only) now also covers **update** — any patch touching ONLY `.sos-state/ticket-state.env` is allowed through whether the file is being created or updated.
- **Actor-check (NEW, the security-critical half of this fix):** the exemption fires ONLY when neither `.sos-state/worker-active` nor `.sos-state/architect-active` is present (mirrors `orchestrator-guard.sh`'s `[ -f ".sos-state/worker-active" ] && continue` pattern) — so only the owner/orchestrator main-thread can exercise it. A worker/architect subagent turn gets no special treatment and still falls through to the ordinary version-match BLOCK, preventing the agent under review from self-approving its own ticket version. The pre-existing all-path check (P078d2a #6) is unchanged — a patch bundling `ticket-state.env` with a second path still BLOCKs.
- **Codex caveat (honest, not SOUND on Codex):** the actor-check depends on the same markers that P078d2b/gap-#6 documents as unreliable inside custom-role Codex subagents (`openai/codex#21753`). Correct+complete on Claude; defense-in-depth only on Codex — real backstop stays human-review-at-the-git-commit boundary. Gap #2 (`sos install` arming Git hooks by default) split out as **P078f** per the phiếu's own pre-declared decomposition threshold (security-arming in `install-hooks.sh` beyond a bare `git config` call) — not yet shipped.
- Tests (`crates/sos-adapter-codex/src/lib.rs`): round-2 repro ALLOW (V1→V2, no marker), self-approve BLOCK ×2 (`worker-active` set, `architect-active` set), pre-approval code-edit regression BLOCK, multi-path bundle BLOCK preserved even with no marker. **Negative-test performed and reverted:** commenting out the actor-check flips the self-approve test from BLOCK(exit 2) to ALLOW(exit 0) — confirmed the check actually bites, then restored.
- Oracle: `cargo build/test --workspace` green, `sos-adapter-codex` 53/53 pass, ×20 = 0 flaky, dep-direction green.
- **Additive, untouched:** P078d2a multi-path all-path guard logic, P078d1 3 startup render-fns, `crates/sos-install/src/engine.rs`, `crates/sos-cli/src/commands/install.rs`, `sos-core`/`sos-adapter-claude` — zero diff (`git diff --stat` = only `templates.rs` + `lib.rs` test module for this crate).
- Docs: `adapters/codex/CAPABILITY.md` §4, `SECURITY.md` "Codex adapter enforcement" section — both updated with actor-check + Codex caveat. Trust-gate: SECURITY.md is unicode-scanned (ASCII clean) but not in the auto-exec surface baseline diff (no `SURFACE_GLOBS` file touched) — no rebaseline needed.
- Full report: `docs/discoveries/P078e.md`.

**[P085] docs: codify phiếu-decomposition heuristic in ORCHESTRATION.md — 5 split signals (incompatible oracles / external-input blocker / security-surface / delivery clarity / lane budget) + keep-whole caveat. Guidance not gate (§0.1); lane budget stays the only mechanical gate. Pointer mirrored in agents/orchestrator.md.**

**[P078d2b] DOCS + DECLARATION (HONESTY) — Codex adapter: in-subagent role-envelope enforcement declared MISSING (upstream openai/codex#21753); SubagentStart-marker deprecated best-effort; P078d DONE (2026-07-22):**
- **Root cause (probe-confirmed, `docs/adapters/SUBAGENT-HOOK-PROBE-2026-07-22.md`):** Codex 0.145.0 does NOT dispatch `SubagentStart`/`SubagentStop` for CUSTOM-role spawns (`architect`/`worker`/`advisory-watch`/`boundary-check`) — only `agent_type="default"` fires. Session metadata carries `agent_role`, but the hook never runs. Upstream regression/gap: `github.com/openai/codex/issues/21753`. Dogfood-confirmed (`docs/adapters/P079-CODEX-DOGFOOD-FINDINGS-2026-07-22.md` #4): a spawned architect's forbidden `apply_patch` against Rust `src/` succeeded IN-SUBAGENT; the `architect-active` marker was never created.
- **`CodexAdapter::verify()` (`crates/sos-adapter-codex/src/lib.rs`):** added Finding #6 — "in-subagent role-envelope enforcement (architect/worker)" — `FindingStatus::Missing`, cites `#21753` and both probe/dogfood evidence files. 5 → 6 Findings; count-oracle test renamed/updated (`verify_reports_exactly_six_gaps_none_sound`) plus a new dedicated assertion (`verify_declares_in_subagent_enforcement_missing`). Findings #1/#3/#5 (PARTIAL) NOT downgraded — 1 clause added to each clarifying "enforced on the MAIN THREAD only, NOT inside spawned custom-role subagents".
- **SubagentStart/Stop marker (`templates.rs` `hooks_json()`) — GIỮ (kept), deprecated best-effort:** render output byte-identical (Rust-comment-only change, zero JSON bytes touched — `hooks_json_is_valid_json_with_expected_events` + `hooks_json_top_level_keys_are_description_and_hooks_only` still pass unmodified). A loud comment above the raw-string literal documents that the marker fires for DEFAULT subagents only and is not relied upon for any capability claim; retained (not removed) so it self-activates if upstream `#21753` is ever fixed.
- **`AGENTS.md` render (`agents_md()`):** one new orchestrator-facing bullet — in-subagent guards do not enforce on Codex; the orchestrator (main thread) owns boundary review of subagent-authored diffs before merge; Git pre-commit/pre-push is the real backstop; optional best-effort main-thread `touch`/`rm` of the marker around delegation.
- **`adapters/codex/CAPABILITY.md`:** new §6 "In-subagent role-envelope enforcement (custom-role subagents) — MISSING" — Claude-vs-Codex containment-layer table + the 3 real backstops (main-thread `PreToolUse`, guards dogfood-confirmed to fire; universal Git pre-commit/pre-push, agent-agnostic; `AGENTS.md` guidance, prose-only) + `#21753` citation.
- **`SECURITY.md`:** new threat-model paragraph in the existing "Codex adapter enforcement" section — "do not trust an architect/worker subagent to police its own envelope on Codex; trust the Git gate" — same 3-backstop declaration, cites `verify()` Finding #6 + `CAPABILITY.md` §6.
- **`adapters/codex/MAPPING.md`:** "5 `Finding`s" → "6 `Finding`s", notes #6 = Missing.
- **NOT a behavioral fix:** `openai/codex#21753` is upstream, out of scope — no hack/workaround of custom-role hook dispatch attempted.
- **Additive verified:** `git diff` on d2a guards (#5/#6 multi-path bypass fix + approval bootstrap exemption), `crates/sos-install/src/engine.rs`, `crates/sos-core`, `bin/sos.sh`, `install.sh` = empty. `cargo build/test --workspace` green (48/48 `sos-adapter-codex` tests, incl. all d1/d2a regressions), ×20 = 0 flaky, dep-direction guard green (0 hits in `sos-core`).
- **P078d = DONE (d1 startup-schema + d2a multi-path-guard + d2b MISSING-declaration).** Codex adapter build is honest-complete; remaining P078 work is behavioral re-dogfood (P079 round 2, owner-run) and P085 decompose-heuristic.
- Full report: `docs/discoveries/P078d2b.md`.

**[P078d2a] FIX (SECURITY) — Codex adapter: multi-path guard bypass hole CLOSED + approval bootstrap deadlock + spawn caveat (2026-07-22):**
- **#6 multi-path bypass (SECURITY HOLE, CLOSED):** all 4 guards with an allow-list (`architect-guard.sh`, `orchestrator-guard.sh`, `block-env-edit.sh`, `approval-gate.sh`, `crates/sos-adapter-codex/src/templates.rs`) used to extract only the FIRST `*** (Add|Update|Delete|Move) File: <path>` marker from a multi-file `apply_patch` (`| head -n1`). A patch with an allowed path FIRST and a forbidden path (`.env`, `src/**`, `.sos-state/ticket-state.env`) SECOND would exit ALLOW on the first match, never inspecting the rest. Fix: extract EVERY path (`grep -oE` already emits one line per match; removing `head -n1` is sufficient), loop over all of them, **BLOCK if ANY path violates** the guard's rule (was "first-path-exempt → allow-all", now "all-path-exempt or BLOCK"). Loop implemented as a herestring `while read` (bash-3.2-compatible population, macOS ships bash 3.2 — no `readarray`/`mapfile`) into an array, then a plain `for` over it — deliberately NOT `... | while read; do ...; exit 2; done`, which would run in a piped subshell and silently swallow the `exit 2`, keeping the hole open while still printing "BLOCKED" to stderr.
- **#5 approval bootstrap deadlock (fixed, coupled hard with #6):** `approval-gate.sh` used to BLOCK every non-ticket `apply_patch` when `.sos-state/ticket-state.env` was missing — including the patch that would create that very file, a chicken-egg deadlock at fresh install. Fix, two layers: (a) a narrow self-bootstrap exemption — allow ONLY when the state file is missing AND the patch touches `.sos-state/ticket-state.env` and NO other path; (b) a rendered skeleton state file (`STATE_SKELETON` Asset, `.sos-state/ticket-state.env`, empty `version=`/`approved_version=`) emitted at install time so most fresh installs never even reach the missing-file branch. The skeleton's non-clobber safety reuses the ALREADY-generic `sos-install::engine` checksum/manifest `Decision::Conflict` logic — zero engine code changed. **This exemption is safe ONLY because #6's all-path check lands in the same patch:** a patch bundling `ticket-state.env` with a second path (`.env`, `src/**`) is caught by #6 and falls through to fail-CLOSED BLOCK, not the bootstrap ALLOW.
- **#7 spawn caveat:** `AGENTS.md` orchestrator guidance now documents the observed P079 first-spawn failure — a full-history forked agent inherits the parent's `agent_type`; spawn `architect`/`worker`/`advisory-watch`/`boundary-check` by omitting `agent_type` or without a full-history fork.
- `all_assets()` extends 17 → 18 (`STATE_SKELETON`, `.sos-state/ticket-state.env`).
- Tests (`crates/sos-adapter-codex/src/lib.rs`): 5 new multi-path bypass BLOCK assertions (one per allow-list guard, ticket-allowed-path-first + forbidden-path-second), 1 multi-path no-regress ALLOW (all paths ticket-exempt), 2 bootstrap tests (state-file-alone → ALLOW; state-file + second path → BLOCK). **Negative-test performed and reverted**: re-adding `| head -n1` at all 4 sites flips exactly these 6 tests from BLOCK(exit 2) to ALLOW(exit 0) — confirmed the fix (and the bash-subshell-safe loop construction) actually closes the hole, not a silent no-op; reverted back to the fix immediately after.
- Oracle: `cargo build/test --workspace` green, `sos-adapter-codex` 47/47 pass, ×20 = 0 flaky, dep-direction green, `bash -n` clean on all 5 guards.
- **Additive, untouched:** `#4` SubagentStart/Stop marker lifecycle (`templates.rs:315-317` hooks wiring) — deferred to **P078d2b** pending a live SubagentStart probe. `sos-install`/`sos-core`/`sos-adapter-claude`/`bin/sos.sh`/`install.sh` — zero diff. `docs-gate`/`dep-direction` checked clean.
- Full report: `docs/discoveries/P078d2a.md`.

**[P078d1] FIX — Codex adapter startup-schema fixes: 3 STARTUP-BLOCKERS from P079 live-dogfood (2026-07-22):**
- `crates/sos-adapter-codex/src/templates.rs`, 3 content-fn format fixes, all confirmed against real Codex 0.145.0 error messages (`docs/adapters/P079-CODEX-DOGFOOD-FINDINGS-2026-07-22.md`):
  - `config_toml()` — `sandbox_mode`/`approval_policy` now emit BEFORE the first table header (`[mcp_servers.doctor]`), not merely before `[agents]` (Worker CHALLENGE caught that the original fix-wording would still nest under `[mcp_servers.doctor]`). Fixes `invalid type string "workspace-write", expected struct AgentRoleToml`.
  - `rules_exec_policy()` — `pattern` now a token LIST (`pattern = ["git", "push", "--force"]`), was a bare string. Fixes `pattern doesn't match, expected list, actual string`.
  - `hooks_json()` — dropped unsupported top-level `_provenance`/`_partial_note` fields, folded into `description` (Codex hooks schema only accepts `description`/`hooks`). Fixes `unknown field _provenance, expected description or hooks`.
- 3 new schema-shape tests added (`crates/sos-adapter-codex/src/lib.rs`) using real parsers (`toml`, `serde_json`) for config.toml/hooks.json, and a documented structural-string oracle for `.rules` (Starlark syntax — `toml`/`serde_json` cannot parse it; adding a Starlark parser crate for a test-only need was rejected as Tầng-1 overkill). All 3 negative-tested (revert fix → test FAILS, restore → PASS).
- **Honest limit (b2/b3-gap lesson):** the prior oracle (generic valid-TOML/valid-JSON) PASSED while these 3 blockers were live — schema-shape assert is still a hand-coded approximation of Codex 0.145.0's real deserializer, not a substitute for a live-Codex run.
- `cargo build/test --workspace` green, `sos-adapter-codex` lib tests ×20 = 0 flaky, dep-direction green. Live render re-smoke (`sos install --runtime codex`) confirms all 3 fixes in the actual written files.
- Additive: only `templates.rs` (3 content-fn) + test module. Enforcement content-fns (marker lifecycle, approval-gate, guard path-parsing) untouched — deferred to **P078d2**. Full report: `docs/discoveries/P078d1.md`.

**[P078c] FIX — install: render-before-toolgate reorder, unblock P079 Codex dogfood (2026-07-22):**
- `sos install --runtime <codex|claude>` used to run `resolve_tools()?` (OA-07 tool-manifest check) BEFORE `apply()`/`dry_run()`, with `?` hard-aborting on any required-tool drift/missing — so a machine with sister-tool drift (this dev machine, live: `doctor` 0.1.1 < pinned 0.1.3, `inv-gate` MISSING) could not render/write ANY adapter file, blocking P079's Codex dogfood over an unrelated concern.
- Default flow now: render (`apply()`/`dry_run()`) runs first, unconditionally; tool-check runs AFTER as a report, not a gate — required drift/missing prints a loud stderr WARNING (tool name + expected + found + `sos tools status` pointer, reusing `tools::describe_failure()`'s exact message, no new format invented) and exits **3** (distinct "installed, tools-not-ready"), never silently swallowed. Render/apply failure stays exit 1 (unchanged). All-tools-ready stays exit 0.
- New opt-in `--require-tools` flag restores the pre-P078c fail-closed order verbatim (tool-check before apply, abort exit 1, zero render) for CI/production callers that want the strongest OA-07 guarantee.
- `--dry-run` shows both the transaction plan AND the tool-drift warning (zero mutation either way); `--require-tools --dry-run` still gates before printing the plan (CI wants the same fail-closed signal even in dry-run).
- **Zero `sos-install` engine/tools change** — `engine::apply()`/`engine::dry_run()` never took tool status as input (already independently callable, per the engine's own doc comment) and `tools::check_tools()`/`tools::required_drift()`/`tools::describe_failure()` were already `pub` and non-`Result` — `install.rs` only changed WHICH functions it calls and WHEN, no new engine/tools code.
- Two near-duplicate functions (`run_claude()`/`run_codex()`) collapsed into one shared `run_adapter(&dyn Adapter, ...)` — symmetric claude+codex behavior is now structural (one code path), not maintained-by-hand.
- Stale comment fixed: `install.rs` still claimed `CodexAdapter.plan()` was "a b1 stub" — false since P078a/b shipped the real 17-artifact render (`plan_renders_seventeen_artifacts` test).
- Oracle: `cargo build/test --workspace` green, ×20 = 0 flaky, clippy 0 new warnings. Live smoke on this drift machine: `install --runtime codex` → 17 real files written (`AGENTS.md`, `.codex/**`, `scripts/codex/**`, `.agents/skills/**`) + loud drift warning + exit 3; `--require-tools` on the same machine → exit 1, zero files written; `--runtime claude` symmetric; `sos tools status` regression-verified still exit 1 on drift, unchanged. Additive: `bin/sos.sh`/`install.sh`/`crates/sos-install/` diffs stay empty.
- OA-07 preserved, not weakened: drift is always surfaced loud and non-zero; only the "block render" behavior changed to "render + report" for the default path, with `--require-tools` as the opt-in full fail-closed escape hatch.
- **P079 Codex dogfood is now unblocked** — a drifted local tool version no longer prevents adapter files from being written.

**[P078b3] ADD — Codex adapter enforcement: 7 artifacts (hooks/rules/5 guards), fail-CLOSED apply_patch parse, mock-payload oracle — P078b DONE (2026-07-22):**
- `CodexAdapter`'s `all_assets()` extends 10 → 17: `.codex/hooks.json` (wires SessionStart/UserPromptSubmit/SubagentStart-Stop/PreToolUse/Stop to 5 guard scripts), `.codex/rules/exec-policy.rules` (Starlark `prefix_rule` force-push/destructive-command deny), and 5 REWRITTEN `scripts/codex/*.sh` guards (architect / orchestrator / block-env-edit / approval-gate / idea-smell) — not copies of the Claude guards, rewritten for Codex's `apply_patch` payload shape.
- **Anchor #1 (CRITICAL) resolved via live probe, not guesswork:** Worker CHALLENGE escalated the exact `apply_patch` patch-body marker syntax (the discovery report named the envelope field but not its internal format). Sếp ran a live Codex CLI (gpt-5.6) session and captured 4 real `PreToolUse` hook payloads, confirming the V4A envelope: `*** Begin Patch\n*** Add|Update|Delete File: <path>\n...*** End Patch`, with embedded newlines escaped as literal `\n` in the JSON string. Fixture committed at `crates/sos-adapter-codex/tests/fixtures/codex-apply-patch-payloads.jsonl`.
- **fail-CLOSED (inverted from the Claude fail-open precedent):** every guard's path-extraction (`grep -oE '\*\*\* (Add|Update|Delete|Move) File: [^\\"]+'`) BLOCKs (exit 2) when it cannot extract a safe path from an `apply_patch` payload, instead of allowing through like the Claude guards do on unparseable `file_path` input — an unparsed Codex patch could otherwise silently write anywhere.
- **Approval gate (E5) is a from-scratch guard build**, not a rewrite — Codex has no native ticket-version approval concept (gap #4). It reads a `.sos-state/ticket-state.env` projection (`version`/`approved_version`), compares only, and never mutates the record; missing state fails CLOSED.
- `block-unsafe-merge`'s semantic PR-sentinel gate is explicitly DEFERRED for Codex (Decision 4) — only the mechanical force-push/destructive-command class is rendered via `.codex/rules`; the PR-merge gate stays a Git/CI backstop.
- PARTIAL-honest declared in 3 places consistently: `.codex/hooks.json`'s `_partial_note` field, every guard script's header comment, and `adapters/codex/{MAPPING,CAPABILITY}.md` + `SECURITY.md` — all state the guards are bypassable (untrusted repo / disabled hooks / obfuscated shell command) and that Git/CI backstops are retained.
- **Oracle STRUCTURAL + mock-payload** (no Codex install required): `hooks.json` valid JSON with the expected event set; every guard passes `bash -n`; a `#[cfg(unix)]`-gated `mock_payload_oracle` test module feeds the real captured fixtures (plus path-substituted variants of the same confirmed envelope) into each rendered guard and asserts block(exit 2)/allow(exit 0) correctness for architect/orchestrator/block-env/approval-gate. `cargo build/test --workspace` green, ×20 = 0 flaky. Dep-direction guard green (`grep -rn 'adapters/|\.codex|apply_patch' core/` → 0).
- Additive, verified: `bin/sos.sh`/`install.sh`/`crates/sos-install/src/engine.rs`/`crates/sos-core` diffs stay empty; `.codex/`/`scripts/codex/` never appear in this repo's `git status` (render only targets a project via `install --runtime codex`).
- **P078b is now DONE (b1 + b2 + b3).** Behavioral verification — Codex CLI actually enforcing these hooks/rules/apply_patch parsing at runtime — is deferred to P079.

**[P078b2] ADD — Codex adapter declarative render: 10 artifacts, structural oracle (2026-07-22):**
- `CodexAdapter::render()`/`plan()` (`crates/sos-adapter-codex/src/lib.rs` + new `src/templates.rs`) now LIVE — generate 10 declarative Codex-native artifacts: `AGENTS.md` (root orchestrator contract), 4× `.codex/agents/{architect,worker,advisory-watch,boundary-check}.toml`, 4× `.agents/skills/{idea,forge,apply,retro}/SKILL.md`, `.codex/config.toml`.
- Trait shape confirmed sufficient (Worker CHALLENGE Turn 1, anchors #1/#3): `render()` is per-`Asset`; `plan()` enumerates a fixed 10-`Asset` set internally, calls `render()` on each, maps `Artifact` → `ManagedOperation` — the install engine consumes this generically, zero engine change required.
- Content-source: crate-embedded template/format-string (`templates.rs`) — `render()` never reads `core/**` off the filesystem; every artifact carries a `core/ROLES.md#<role_id>`-style pointer, never a copy of role/skill semantics (`core/ASSETS.md:51`, mirrors Claude adapter P076 provenance marker).
- PARTIAL-honest: `architect.toml`'s `developer_instructions` explicitly states the envelope is enforced via a PreToolUse hook (P078b3) + prose, NOT Codex structural tool-removal — matching `verify()` Finding #1. `advisory-watch.toml`/`boundary-check.toml` render honest `sandbox_mode="read-only"` with no PARTIAL marker.
- Skill set rendered = 4 (`idea`/`forge`/`apply`/`retro`); `init` deliberately **deferred, not silently dropped** — `MAPPING.md` documents the gap (Claude's `init` caller is a Claude-CLI-bound invocation, no Codex-native trigger designed yet).
- Open escape hatch (documented, not blocking): advisory-watch needs network egress for GHSA queries but renders `sandbox_mode="read-only"` — whether Codex's read-only sandbox also gates network is unconfirmed from the discovery report; behavioral resolution deferred to P079.
- **Oracle STRUCTURAL** (Decision 5): 13 new unit tests (TOML parse validity, SKILL.md frontmatter validity, core-ID pointer presence per artifact, PARTIAL-marker presence/absence, artifact-count==10) — run without Codex CLI installed. `cargo build/test --workspace` green, ×20 = 0 flaky. Dep-direction guard green.
- **Additive, verified:** `git diff bin/sos.sh install.sh crates/sos-install/src/engine.rs crates/sos-core` empty; `.codex/`/`AGENTS.md`/`.agents/` do not appear in this repo's `git status` (render only ever targets a project via `install --runtime codex`).
- 3-phiếu decompose: this is 2/3. P078b3 (enforcement: `.codex/hooks.json`/`.codex/rules/*.rules`/rewritten guard scripts) follows.

**[P078b1] ADD — Codex adapter foundation: crate + `detect()`/`verify()` + PARTIAL-declaration mechanism + CLI wire (2026-07-22):**
- New `crates/sos-adapter-codex` (deps CHỈ `sos-core`, dep-direction preserved) — `CodexAdapter` implements the core `Adapter` trait. `detect()` is structural (static Codex 0.145.0 facts + fail-safe `codex --version` probe — absence never panics). `verify()` returns exactly 5 `Finding`s reporting Codex's known capability gaps (per-role tool allowlist / repo slash commands / skill allowed-tools / native ticket-approval / Read-Glob interception), each carrying an explicit oracle-strength status — never `Sound` for a gap (`core/ROLES.md` sep-inv #5). `plan()`/`render()`/`uninstall()` stay minimal-honest stubs — real rendering is P078b2/b3.
- **`FindingStatus` enum** (`Sound`/`Partial`/`Missing`, additive) added to `crates/sos-core/src/adapter.rs` — `Finding` previously carried no oracle-strength field at all. Vocab matches `core/POLICY.md` "Oracle-first claims". `ClaudeAdapter` needed no compile-fix (never constructs a `Finding`).
- Wired `sos-cli`: `install --runtime codex` no longer errors "not yet available (P078)" — constructs `CodexAdapter`, drives the same install engine as `--runtime claude` (zero engine change).
- New `adapters/codex/{README,MAPPING,CAPABILITY}.md`, mirroring `adapters/claude/`'s declarative-boundary pattern (P076); `CAPABILITY.md` is the frozen human-readable twin of `verify()`'s 5-gap declaration.
- **Additive, verified:** `git diff bin/sos.sh install.sh crates/sos-install/src/engine.rs` empty. `cargo build/test --workspace` green, ×20 = 0 flaky. Dep-direction guard green (`sos_core_stays_host_neutral`).
- **Oracle boundary (structural, Decision 5):** verified without Codex CLI installed. Behavioral verification (Codex actually running the rendered output) is P079.
- 3-phiếu decompose: this is 1/3 (foundation). P078b2 (declarative render: `AGENTS.md`/`.codex/agents/*.toml`/`.agents/skills/**`/`.codex/config.toml`) and P078b3 (enforcement: `.codex/hooks.json`/`.codex/rules/*.rules`/guard-script rewrite) follow.

**[P078a] ADD — `core/STATE.md` portable serialization contract (2026-07-22):**
- New `core/STATE.md`: canonical machine-readable format for ticket storage/schema, lifecycle state artifact, approval record, edit allowlist, review trigger map and blocked format — lifted from Codex Adapter Discovery Report's Core UNDER-SPECIFICATION list (`docs/adapters/CODEX_ADAPTER_DISCOVERY_2026-07-22.md:26-28`). 4 remaining items (tier-classifier owner, concurrent-ownership lock/worktree, publish-actor division, backlog serialization) deferred to follow-up P078a2.
- Runtime-neutral (no host token — verified via grep), backward-compatible with existing Claude adapter behavior (state-file is an OPTIONAL projection; the ticket debate log stays authoritative per `core/WORKFLOW.md:49`). No semantics changed in `WORKFLOW.md`/`POLICY.md`/`ROLES.md`.
- Unblocks P078b (Codex adapter render).

**[P077f] RELOCATE — Rust workspace `bootstrap/sos-rs/` → repo-root (2026-07-22):**
- Pure `git mv` + path-fix, no logic/behavior change (reversible via `git revert`). `Cargo.toml`+`Cargo.lock`+`crates/` moved to repo-root; `bootstrap/` removed (empty after move). Layout now matches `docs/PORTABILITY_ARCHITECTURE.md` target tree — **P077 fully DONE (a-f)**.
- **Path-fixes:** resolver `bin/sos.sh` `_sos_workspace_root()` `bootstrap/sos-rs` → repo-root; `crates/sos-cli/tests/parity.rs` `CARGO_MANIFEST_DIR` depth (4→2 hop to `scripts/install-hooks.sh`); `crates/sos-install/src/tools.rs` `include_str!` depth (5→3 hop to `tool-manifest.toml` — a hardcoded relative-path site OUTSIDE the `CARGO_MANIFEST_DIR` family, missed by the initial anchor-grep and caught in Worker CHALLENGE Turn 1 O1.1, added as Task 2b); `.gitignore` `bootstrap/*/target/` → `/target/`.
- **Security-surface fix (Worker CHALLENGE Turn 1 O1.2):** `scripts/orchestrator-guard.sh`'s kit-maintenance allow-list glob (`bootstrap/*`) would have silently stopped matching the moved Rust source, making the guard MORE restrictive (blocking Quản đốc from editing `crates/*/src/*.rs` without the `worker-active` marker) — a real behavior change on a security surface that the original phiếu draft undersold as a cosmetic path-ref. Fixed by adding `crates/*`/`Cargo.toml`/`Cargo.lock` to the allow-list (before the product-source case), keeping `bootstrap/*` as a harmless dead glob. Guard smoke verified: `crates/sos-cli/src/main.rs` still exit 0 without marker; other `src/*.rs` still exit 2.
- Trust-baseline rebaselined (`bin/sos.sh` + `scripts/orchestrator-guard.sh` hash changed).
- **Verified:** `cargo build --workspace` + `cargo test --workspace` from repo-root green (18 tests, incl. `dep_direction.rs` — needed `cargo clean -p sos-core` to invalidate a stale cached binary embedding the OLD `CARGO_MANIFEST_DIR` via `env!()`, since cargo doesn't track manifest-directory moves when source content is unchanged); ×20 parallel = 0 flaky; cutover smoke (`bin/sos.sh map` byte-identical to direct Rust binary invocation, both shared-cache `CARGO_TARGET_DIR` and non-redirect repo-root `target/` resolution paths); guard smoke (above); `bash -n bin/sos.sh` + `bash -n scripts/orchestrator-guard.sh` clean; `docs-gate --all` PASS.
- **Open doctrine question (not resolved here, captured in `docs/BACKLOG.md`):** post-cutover Rust source is now product, not just kit-tooling — should the guard eventually gate it as Worker-only instead of the current kit-maintenance allow-list? Out of scope for this pure-relocate phiếu.

**[P077e] CUTOVER — Rust `sos` canonical for 6 heavy subcommands; `bin/sos.sh` becomes a thin launcher (2026-07-22):**
- Approach (A), founder-confirmed: 6 heavy subcommands (`new`/`adopt`/`sync`/`map`/`install`/`tools`) now dispatch (`exec`) to the canonical Rust `sos` binary; 7 guidance subcommands (`init`/`blueprint`/`contract`/`apply`/`recipe`/`launch`/`status`) stay Bash in `bin/sos.sh` (Claude-flavored, per-runtime render deferred to P078).
- **Dispatch flip:** new resolver `_sos_rust_bin()` — precedence `SOS_RUST_BIN` override → known build dirs (`CARGO_TARGET_DIR`/workspace `target/`) → `cargo build --bin sos` on-demand → fail-LOUD (exit 127, clear message, **no silent fallback to Bash** — the old Bash `map` still carries bug OA-02). Resolver deliberately never calls `command -v sos` (the launcher itself installs as `sos` on PATH — would recurse). 4 arms flipped (`new`/`adopt`/`sync`/`map`), 2 arms **added new** (`install`/`tools` — these never had a Bash counterpart). Old `sos_new`/`sos_adopt`/`sos_sync`/`sos_map` Bash functions kept DORMANT (not deleted — rollback safety + parity-test oracle; delete at P077f/P081).
- **Repo contract flip:** root `CLAUDE.md` "What this repo is NOT" #1 + Rules #1 + repo-structure now describe sos-kit as the runtime monorepo for the `sos` binary (sister CLIs `ship`/`docs-gate`/`guard`/`vps` still external, unchanged). `bootstrap/sos-rs/README.md` ownership flipped (canonical, not extracting; relocation = P077f, deferred/orthogonal/reversible). `docs/PORTABILITY_ARCHITECTURE.md` + `docs/SETUP.md` status notes added.
- **`install.sh` zero-touch** — prebuilt-binary bootstrap for the `sos` binary is P081, not this phiếu.
- **Verified:** cutover smoke — heavy cmd via `bin/sos.sh` byte-identical to invoking the Rust binary directly (new/adopt/map/sync/install/tools `--help`, `tools status`); fail-LOUD confirmed (no cargo + invalid `SOS_RUST_BIN` + fake build dir → exit 127, no Bash fallback ran); guidance commands (`status`/`init`) unchanged; recursion-safe (no `command -v sos` in resolver); `bash -n bin/sos.sh` clean; `cargo test --workspace` ×20 = 0 flaky.

**[P077c6] `cargo test --workspace` flaky (~10-25%) → root-cause pinned + fixed: `TempFixture` dir-collision under parallel execution (2026-07-22):**
- Follow-up to P077c (CLOSED at c5). **V1→V2 pivot:** the flake was initially suspected to be a line-*order* nondeterminism (report-list unsorted / find-order dependent) — that premise was **RUT LAI hoàn toàn** after Worker CHALLENGE (28 sequential single-process runs never reproduced order-flakiness) + a full assertion diff showing `same set of lines? False` (genuine content mismatch, not reordering). This does NOT retract P077c2's separate finding about Bash's unsorted `find`-enumeration order — that stands; this was an unrelated bug entirely.
- **Root cause (DIAGNOSE mode, then EXECUTE fix):** `parity.rs`'s `TempFixture::new` keyed its throwaway-dir name on `process::id() + SystemTime::now().as_nanos()` only. Every test function in one `cargo test` binary runs as a THREAD inside a single process (identical `process::id()` across parallel tests), and this measured machine's clock resolution is far coarser than 1ns (195,032/200,000 sequential calls returned an identical nanos value in a tight loop). Under real parallel execution, two independent `build_new_fixture()` calls (`parity_new_enforced` + the one nested inside `parity_adopt_enforced`'s `build_adopt_fixture()`) could sample the same nanos and collide on the exact same directory path; whichever `TempFixture`'s `Drop` (`fs::remove_dir_all`) fired first wiped the OTHER test's still-in-use fixture out from under it mid-flight — confirmed live via instrumentation (spine files present immediately before `sos adopt` ran, gone immediately after, with the colliding `Drop` firing in between). H1 (fixture-copy race against the real repo's `install-hooks.sh`), H2 (cwd/symlink race), H3 (fixture-not-finished race), and H5 (production `adopt`/`new`/`sync` global-state race) were all explicitly measured and ruled out — every `sos` invocation from the test harness runs as an isolated child process, so **no production code (`src/commands/*.rs`, `bin/sos.sh`) needed to change.** Pure test-harness fixture race.
- **Fix:** `TempFixture::new` now mixes a process-wide monotonic `AtomicU64` counter into its key (`sos-parity-{name}-{pid}-{nanos}-{counter}`) — collision-proof independent of clock resolution, no new crate dependency.
- **Oracle (hard-fail, run for real):** before fix, `cargo test --workspace` parallel was ~10-16% flaky (4/25 observed in targeted repro). After fix: **20/20 consecutive parallel `cargo test --workspace` runs, 0 flaky.** `cargo build --workspace` clean; all suites pass (8/8 `parity.rs` + adapter-trait-shape + dep_direction + sos_core). `git diff bin/sos.sh install.sh` empty; all goldens byte-identical (untouched — order/sort was never the bug, none changed).
- Docs updated: `crates/sos-cli/tests/README.md` (new "`TempFixture` content-nondeterminism under parallel `cargo test`" section — full root-cause writeup + fix rationale), `docs/discoveries/P077c6.md` (Discovery Report), `docs/ticket/P077c6-report-order-determinism.md` (Debate Log Turn 2, DIAGNOSE→EXECUTE consensus).

**[P077d3] tool-manifest.toml — pin sister-tool version+asset+checksum + `sos tools status` + doctor fail-clear (OA-07) — P077d CLOSED (2026-07-22):**
- Third and final sub-phiếu of the P077d decomposition, depends on P077d2's install engine (fills its step-5 seam). Additive-only, `bin/sos.sh` + `install.sh` untouched (`git diff` empty for both) — `sos tools status` is a brand-new command, no Bash counterpart.
- Worker CHALLENGE Turn 1 (pre-execute) resolved all 4 `[needs Worker verify]` anchors with live code citations, no objections, V1 accepted straight through — including a live-tested E1 check (`<tool> --version` format uniform `"<name> <x.y.z>"` across all 9 sos-kit tools on PATH, no `v` prefix) and confirming no `sos doctor` subcommand exists anywhere in `sos-cli` (every `doctor` reference shells out to the external binary).
- Shipped: (1) `tool-manifest.toml` (kit ROOT, committed config — distinct from d2's generated project-root `.sos-manifest.toml`) — `[[tool]]` array-of-tables pinning 10 sister tools' `name`/`version`/`required` + per-platform `[tool.asset]`/`[tool.checksum]` (3 target-triple, matches `install.sh:52-60`); 6 required (`doctor`/`claude-hooks`/`docs-gate`/`ship`/`advisory-inbox`/`inv-gate`, matches `install.sh:40`) + 4 optional (`guard`/`vps`/`doc-rotate`/`advisory-cron`, matches `install.sh:48`); versions filled from each sister tool's own source `Cargo.toml` (NOT `releases/latest` — exactly the OA-07 bug), checksums are honest `TODO-sha256-<tool>-P081` placeholders (E2 — no prebuilt asset hashed against this pin shape yet, real fill is P081); (2) `sos_install::tools` (NEW module, `crates/sos-install/src/tools.rs`) — `ToolManifest`/`ToolPin` (serde), `check_tools()` (resolves `<tool> --version`, hand-rolled dotted-tuple version compare since no `semver` crate exists anywhere in the workspace, anchor #8), `Verdict` (`Ok`/`Newer`/`Drift`/`Missing`/`Unparseable`), `validate_manifest()` (schema-integrity: every checksum cell must be 64-hex or a TODO placeholder — catches a corrupted manifest even without a real binary to hash against), `gate_required()` (fail-closed core shared by both surfaces below), embedded at compile time via `include_str!` (chosen over an env-var/`--manifest` flag lookup — the compiled `sos` binary runs inside arbitrary target projects that don't necessarily have a sos-kit checkout, so the pin must travel WITH the binary); (3) `sos tools status` (NEW, `crates/sos-cli/src/commands/tools.rs`, new clap `Tools{Status}` variant) — table report, exit 1 if any REQUIRED tool is Drift/Missing/Unparseable, exit 0 otherwise (optional drift = warn-only, never flips exit); (4) step-5 `resolve_tools()` filled (`sos_install::engine`, was a `Vec::new()` no-op stub) — now reads the embedded manifest and calls `gate_required()` at the SAME call position d2 wired, `?`-propagated in `commands/install.rs` so a required-tool failure aborts BEFORE any filesystem mutation (trivially satisfies "fail rõ + rollback" — nothing was written yet); (5) doctor fail-clear surfaces via (a)+(b) above only — no `sos doctor` subcommand was created (would collide with the external binary's identity).
- **Oracle** (`crates/sos-install/tests/tools.rs`, NEW) — 3 fixture groups (manifest-pin-verify incl. sabotage-checksum-fails-loud / status-drift incl. required-vs-optional-missing split / doctor-step-5 fail-clear incl. exact-pin-passes), entirely synthetic (fake-PATH stub `#!/bin/sh` scripts, `TempFixture` collision-safe pattern replicated from `tests/install.rs`) — zero dependency on any real sister tool being installed. `Command`'s `PATH` is overridden PER-CHILD-PROCESS (not global `env::set_var`), so parallel `#[test]` threads never race.
- `cargo build --workspace` clean; `cargo test --workspace` green (8 `parity.rs` + 2 manifest + 1 adapter-trait-shape + 1 dep_direction guard + 6 `install.rs` + 6 NEW `tools.rs`, all pass). `cargo clippy --workspace` shows only the 1 pre-existing unrelated warning (`sync.rs:102`). **`cargo test --workspace` × 20 parallel — 0 flaky.** `git diff bin/sos.sh install.sh` empty. Live smoke on this dev machine (unmodified, real installed tools) — `sos tools status` and `sos install --dry-run` both reproduce OA-07's exact evidence: `doctor 0.1.1` vs pinned `0.1.3` (DRIFT), `claude-hooks`/`docs-gate`/`ship`/`advisory-inbox` also DRIFT, `inv-gate` MISSING entirely, exit 1 — with zero filesystem mutation confirmed for the `--dry-run` path. Trust-gate exit 0 (no rebaseline needed, same finding as prior P077 sub-phiếu — `bootstrap/sos-rs/**` not tracked in `.sos-trust-baseline`).
- Docs updated: `docs/PORTABILITY_ARCHITECTURE.md` (new "P077d3 status" line), `bootstrap/sos-rs/README.md` (module-layout row + new "tool-manifest (P077d3, OA-07)" section), `docs/plans/P077d-decomposition.md` (P077d3 status line + table row, **P077d marked CLOSED**), `docs/retro/OUTSIDER_AUDIT_SYSTEM_GAPS_2026-07-22.md` (OA-07 → RESOLVED-in-Rust-path note appended, audit body not rewritten).
- **P077d CLOSED (d1+d2+d3) → next up: P077e cutover** (flip `sos install`/Rust binary to canonical, update root `CLAUDE.md`'s "not a runtime binary source" contract).

**[P077d2] Install engine — transaction plan / dry-run / non-clobber / rollback / apply (2026-07-22):**
- Second sub-phiếu of the P077d decomposition, depends on P077d1's `Adapter` trait + `ManagedManifest` schema. Additive-only, `bin/sos.sh` + `install.sh` untouched (`git diff` empty for both) — `sos install` is a brand-new command, no Bash counterpart.
- **Worker CHALLENGE Turn 1 finding (O1.1, Architect ACCEPT Alt A):** `ManagedOperation` (d1's placeholder type) carried only `description`+`target_path` — no content field — leaving the engine no trait-pure way to obtain the bytes it must write/hash/record, a genuine seam-risk in the engine-vs-adapter separation. Fixed via a **narrow amendment**: added `content: String` to `ManagedOperation` only; `Adapter`'s 5 methods and `ManagedManifest`'s 6 fields (the hard SHAPE) are untouched.
- Shipped: (1) `sos-install::engine` (`crates/sos-install/src/engine.rs`) — pure-Rust transaction executor, driven THUẦN qua the `Adapter` trait (zero host token, same dep-direction discipline as `sos-adapter-claude`), implementing transaction steps 1-4+6-7 of `PORTABILITY_ARCHITECTURE.md:109-117` (step 5 tool-resolve = `resolve_tools()` stub seam, `// SEAM P077d3`, P078/OA-07 fills it); (2) **4-way non-clobber discrimination** (`Decision::Create`/`NoOp`/`Update`/`Conflict` — the `NoOp` variant was added mid-EXECUTE after the idempotence fixture caught a naive 3-way split rewriting identical bytes and bumping `rollback_ref` on every re-run, breaking true idempotence): absent→CREATE; on-disk already == desired bytes→NoOp (zero mutation); differs but hash matches manifest's recorded `content_hash`→UPDATE allowed; differs + no match/no record (user-customized)→CONFLICT, staged to `.sos-install-incoming/<path>` (mirrors `.sos-adopt-incoming`), original left byte-untouched; (3) **rollback** — snapshot-before-mutate (CREATE ops delete-on-fail, UPDATE ops restore exact prior bytes), `.sos-manifest.toml` only committed after the WHOLE transaction succeeds; (4) `.sos-manifest.toml` at project root, `[[managed]]` array-of-tables of `ManagedManifest` entries; (5) `sos install --runtime <auto|claude|codex> [--dry-run]` wired in `sos-cli` (`crates/sos-cli/src/commands/install.rs`, new clap `Install` variant) — `claude` constructs `ClaudeAdapter` (still d1-stub, plan/render stay minimal/empty; real Claude asset rendering deferred past d2), `codex` errors clearly ("not yet available, P078"); (6) **install correctness oracle** (`crates/sos-install/tests/install.rs`) — a deterministic `MockAdapter` + 5 hard-fail fixtures (additive / non-clobber×2 / rollback / idempotence / dry-run), proving the engine correct independent of which adapter drives it (the entire point of the d1 trait abstraction) — NOT a Bash-parity fixture.
- `cargo build --workspace` clean; `cargo test --workspace` green (8/8 pre-existing `parity.rs` + 2 manifest + 1 adapter-trait-shape + 1 dep_direction guard + 6 new install fixtures, all pass); `cargo clippy --workspace` shows only the 1 pre-existing unrelated warning (`sync.rs:102`). **`cargo test --workspace` run 20× in parallel (`xargs -P 20`) — 0 flaky** (chuẩn post-c6). `git diff bin/sos.sh install.sh` empty. `sos install --runtime claude --dry-run` smoke: exit 0, prints empty plan (ClaudeAdapter stub), zero mutation. Trust-gate `bash scripts/trust-gate.sh` exit 0 (`bootstrap/sos-rs/**` not in `.sos-trust-baseline` — same as prior P077 sub-phiếu, no rebaseline needed).
- Docs updated: `docs/PORTABILITY_ARCHITECTURE.md` (new "P077d2 status" line — `ManagedOperation` amendment, non-clobber/rollback semantics, manifest path), `bootstrap/sos-rs/README.md` (new "Install engine (P077d2)" section, module-layout row updated skeleton→LIVE), `crates/sos-cli/tests/README.md` (new "`sos install` — correctness oracle" section), `docs/plans/P077d-decomposition.md` (P077d2 status line).

**[P077d1] Adapter contract trait + managed-manifest schema (foundation, zero engine, zero fs mutation) (2026-07-22):**
- First sub-phiếu of the P077d decomposition (`docs/plans/P077d-decomposition.md`). Additive-only, `bin/sos.sh` + `install.sh` untouched (`git diff` empty for both). Carves the **foundation abstraction** d2 (install engine) and P078 (Codex adapter) build on, before any engine logic exists — same "carve boundary before impl" strategy as P077b.
- Shipped in `sos-core`: `crates/sos-core/src/adapter.rs` — `Adapter` trait, 5 methods (`detect`/`plan`/`render`/`verify`/`uninstall`) per `docs/PORTABILITY_ARCHITECTURE.md:63-69`, plus runtime-neutral placeholder types (`Capabilities`, `Plan`/`ManagedOperation`, `Asset`/`Artifact`, `Findings`/`Finding`, `RemovalPlan`/`RemovalStep`) — zero host token, verified by the dep-direction guard's forbidden-token scan; `crates/sos-core/src/manifest.rs` — `ManagedManifest` struct, 6 fields per `core/ASSETS.md:57-64` (`owner`/`source_version`/`source_identity`/`target_path`/`content_hash`/`rollback_ref: Option<String>`), serde-derived, TOML round-trip (2 tests: `rollback_ref` set + `None`).
- Shipped in `sos-adapter-claude`: `ClaudeAdapter` implements `Adapter` with **stub bodies only** (no fs mutation, no real rendering) — proves the trait is implementable end-to-end while exercising the dependency-direction gate (adapter → core, one-way; confirmed `sos-adapter-claude/Cargo.toml` deps = `sos-core` only, `sos-core/Cargo.toml` deps = zero sos-* crate).
- Worker CHALLENGE (pre-execute) verified all 5 `[needs Worker verify]` anchors clean (skeletons empty, dep-direction confirmed adapter→core-only, trait placement in `sos-core` follows from that, dep_direction guard test exists, serde already available via workspace `serde = { features = ["derive"] }`) — accepted V1, no objections.
- One live gotcha found+fixed mid-EXECUTE: the module doc comment in `adapter.rs` initially cited the dep-direction guard's own forbidden tokens (`sos_adapter`/`sos_install`) verbatim in prose, which the guard's substring scan flagged as a false-positive violation — reworded to describe the tokens without literally spelling them, guard back to green (no scope/design change, pure prose fix).
- New tests: `crates/sos-core/tests/adapter_trait_shape.rs` (compile-level trait-bound proof via a local `NoopAdapter`), `sos-adapter-claude`'s own inline trait-bound test, `manifest.rs`'s 2 round-trip tests. `cargo build --workspace` clean; `cargo test --workspace` green (8 pre-existing `parity.rs` + 2 manifest + 1 adapter-trait-shape + 1 dep_direction guard, all pass); `cargo clippy --workspace` shows only 1 pre-existing unrelated warning (`sync.rs:102`, not touched by this phiếu). Docs updated: `bootstrap/sos-rs/README.md` (new "Adapter contract + managed-manifest schema (P077d1)" section), `docs/plans/P077d-decomposition.md` (P077d1 status line).
- **d2 (install engine) and d3 (tool-manifest.toml OA-07) not started** — this phiếu is foundation only, zero fs mutation, zero `sos install` command.

**[P077c5] Rust `sos map`/`adopt` → OA-02 scanner fix, CORRECTNESS oracle (Rust beat Bash) — P077c CLOSED (2026-07-22):**
- Fifth and FINAL sub-phiếu of the P077c decomposition. Fixes OA-02 (`docs/retro/OUTSIDER_AUDIT_SYSTEM_GAPS_2026-07-22.md:93-131`, 🔴 false-green): `sos map` could map the kit's own just-adopted assets while missing real source, and `doctor validate-map` still PASSed (soundness-only check, not completeness). c1–c4 deliberately ported this bug bug-for-bug (parity-first strategy); c5 is where it gets fixed — INTENTIONALLY diverging Rust from Bash, on purpose, against a different oracle (correctness fixtures, not `==Bash golden`). `bin/sos.sh` stays UNCHANGED (`git diff bin/sos.sh` empty — P077c invariant; Bash fix, if ever done, deferred to the P077e cutover decision, not this phiếu — Constraint 8 escalate-gate not fired, Worker CHALLENGE + Chủ nhà agreed Rust-only proceeds).
- **3-part fix, all in `map.rs`:** (1) stack-aware generic source surface — `STACK_MARKERS`/`detect_present_stacks` detects a manifest (`Cargo.toml`, `pyproject.toml`/`setup.py`/`requirements.txt`, `package.json`, `go.mod`, `Package.swift`) anywhere in the tree (unbounded depth, monorepo-aware) and emits a matching `rust_src`/`python_src`/`node_src`/`go_src`/`swift_src` surface additive to the 6 pre-existing dir-pattern surfaces — a standard Rust crate now maps `src/main.rs`; (2) `KIT_MANAGED_ROOTS` static exclude-list (`templates`/`phieu`/`scripts`/`hooks`/`.claude` — verified exactly matches `adopt.rs:506-524`'s actual copy targets at Worker CHALLENGE) checked against every scanned entry's first relative-path component inside `scan_surface` — one mechanism fixes both standalone `sos map` and map-within-adopt; (3) 3-verdict `status` field (`PATH_VALID`/`COVERAGE_UNKNOWN`/`COVERAGE_REVIEWED` replacing the single `draft_needs_review` string) — fresh scans always emit `coverage_unknown`, never routing authority; the zero-surface stub case (previously a distinct `draft_unmapped`) folded into `COVERAGE_UNKNOWN` too (Worker CHALLENGE O1.1, self-decided Tầng 2 — still a fresh unreviewed scan, not a 4th state).
- **`adopt.rs` needed ZERO code change** (Task 4, verified) — map-within-adopt re-invokes the same compiled `sos` binary's `map` subcommand as a subprocess, so it inherits the exclude-list + 3-verdict fix automatically.
- **Anchor #7 (Worker CHALLENGE, the one genuine escalation risk) resolved CLEAN, no doctor change needed:** read `~/doctor/src/cli/validate_map.rs` (read-only, off-limits repo) directly — it deserializes only `version`/`surfaces{...}`/`never_default_read`, has zero reference to a top-level `status` field anywhere. The 3-verdict rename is safe; no Constraint 9 escalation fired.
- **Harness flip (Task 5-6):** `parity_map_enforced` (fully) + `parity_adopt_enforced`'s `docs/AGENT_MAP.yaml` gen-content line (only) flipped from Bash-parity to **CORRECTNESS oracle** — asserted against hand-authored/frozen-from-corrected-Rust goldens, not Bash (`map.golden`, `map.agent_map.golden`, `adopt.gen.golden`'s AGENT_MAP hash all re-froze). `sync`/`new` untouched, still pure Bash-parity. `PARITY_ENFORCED` keeps its 4-name shape but the const now spans two DIFFERENT oracle kinds — documented explicitly in `parity.rs`'s doc comments and `tests/README.md`'s new "correctness oracle vs parity oracle" section to avoid misleading future readers.
- **`capture.sh` mechanically guarded** against silently re-introducing OA-02 on a naive re-run: the `map`-capturing block now writes `map.golden.bash-reference`/`map.agent_map.golden.bash-reference` (informational, no test reads them) instead of clobbering the real goldens; `freeze_adopt_gen()` explicitly SKIPS `docs/AGENT_MAP.yaml` (prints `WARN:` to stderr) rather than hashing the Bash-buggy file. The 3 correctness-oracle golden artifacts are updated by hand only, verified against the corrected Rust binary's actual output.
- **3 new acceptance fixtures (Task 6, `parity.rs`):** `oa02_rust_crate_maps_src_main` (Cargo.toml+src/main.rs → `rust_src` surface present, status `coverage_unknown`), `oa02_templates_excluded_from_frontend` (kit `templates/` never becomes a `frontend` surface; real product surfaces still map), `oa02_monorepo_nested_packages_mapped` (nested `migrations/` dir + a nested Rust sub-package's own `Cargo.toml` both produce expected surfaces). Negative-test verified: a 1-token sabotage (renaming the `rust_src` marker's surface name) failed 2 of the 3 `oa02_*` tests loud with a clear panic message; reverted clean (`diff` confirmed byte-identical), `cargo test --workspace` returned to 8/8 green.
- `cargo build --workspace` clean, `cargo test --workspace` green (8/8 `parity.rs` tests: 5 pre-existing + 3 new `oa02_*`). `git diff bin/sos.sh` empty; `sync.*`/`new.*` goldens byte-identical (untouched). `bash scripts/trust-gate.sh` exit 0 (`bootstrap/sos-rs/**` not in `.sos-trust-baseline` — no rebaseline needed). Docs updated: `bootstrap/sos-rs/README.md` (map/adopt status rows rewritten, correctness-vs-parity framing), `crates/sos-cli/tests/README.md` (new "correctness oracle vs parity oracle" section, full OA-02 fix writeup), `docs/plans/P077c-decomposition.md` (P077c5 status line — **P077c CLOSED, all 5 sub-phiếu shipped**), `docs/retro/OUTSIDER_AUDIT_SYSTEM_GAPS_2026-07-22.md` (OA-02 marked ✅ Rust-only, Bash deferred P077e).

**[P077c4] Rust `sos adopt` → parity (bug-for-bug) + 4-layer fixture (stdout+tree+gen+preservation) + doctor-absent re-froze (2026-07-22):**
- Fourth sub-phiếu of the P077c decomposition, heaviest command (brownfield retrofit, non-clobber discipline OPPOSITE of `new`). Additive-only, `bin/sos.sh` untouched (`git diff bin/sos.sh` empty). Shipped: (1) Rust `adopt` subcommand (`crates/sos-cli/src/commands/adopt.rs`, new clap `Adopt` variant) — port of `bin/sos.sh:606-971` `sos_adopt` bug-for-bug (dirty-warn, `adopt_item` non-clobber core with symlink-escape guard, skills remap, `.mcp.json`/`.claude/settings.local.json` create-if-absent + jq-merge-if-exists, Category C generate-if-missing, `.gitignore`/`.phieu-counter` seed, born-wire install-hooks + `sos_init_security` with its 0/1/2 return-code semantics, validator honoring `DOCTOR_BIN`); (2) map-within-adopt calls into `map`'s scan logic by re-invoking the SAME compiled `sos` binary as a subprocess with output discarded (`run_map_subcommand`, matches Bash's `sos_map "$target" >/dev/null`) — reuses `map.rs`'s logic WITHOUT modifying it; (3) **4-layer parity fixture** — `adopt.golden` (stdout, **re-froze**: the pre-existing golden was captured on the CONNECTED doctor path, same host-artifact class P077c3 found for `new.golden`), `adopt.tree.golden` (NEW — sorted path-shape manifest, excludes `.git*`, INCLUDES `.sos-adopt-incoming/**`), `adopt.gen.golden` (NEW — content-hash manifest, GENERATED-authored files ONLY), and a NEW in-test **preservation-assert** (not a golden — universal non-clobber property: every seeded pre-existing file's sha256 identical before/after, every `.sos-adopt-incoming/<path>` byte-matches its kit source); (4) synthetic fake-kit reuses `new`'s shape PLUS `templates/claude-settings.local.json` (2nd fixture gotcha found live during Worker CHALLENGE Turn 1's probe — `[1/4]`/`[3/4]` error without it) + a **4-collision brownfield target** (spine-absent / spine-collision / doc-existing / source-non-spine), committed clean so dirty-warn doesn't fire. **OA-02 confirmed reproducing bug-for-bug inside adopt** (map-within-adopt scans kit assets `[1/4]` already copied in — verified deterministic across 2 independent live runs during CHALLENGE, not flaky nondeterminism); non-clobber `added`/`conflicts` list order = raw traversal order (not sorted), same divergence class as `sync`. `PARITY_ENFORCED` += `"adopt"` (now `["map", "sync", "new", "adopt"]` — **4/4 commands enforced, zero informational left**). `.mcp.json`/`.claude/settings.local.json` exists-branch shells out to real `jq` (bug-for-bug, no new JSON-merge crate dependency) — not exercised by the fixture (Constraint 7), implemented for real-world correctness only. 3 negative tests, each firing on exactly one layer: (a) sabotaged a directory-copy to skip `phieu/README.md` while still reporting it ADDED → failed on **tree** only; (b) sabotaged `.docs-gate.toml`'s `ticket_dir` value → failed on **gen** only; (c) sabotaged the collision branch to clobber the pre-existing target file → failed on **preservation** only (stdout/tree/gen all unaffected) — proving the 4-way split isn't redundant, each layer catches a distinct regression class. `cargo build --workspace` + `cargo test --workspace` green; `map`/`sync`/`new` goldens byte-identical after re-running `capture.sh` twice (additive + determinism proof, `adopt.golden`/`adopt.tree.golden`/`adopt.gen.golden` byte-identical between the 2 runs too). Docs updated: `bootstrap/sos-rs/README.md` (adopt parity status), `crates/sos-cli/tests/README.md` (4-layer fixture section + preservation-assert rationale + brownfield collision table + negative-test summary), `docs/plans/P077c-decomposition.md` (mark c4 done, feed-forward for c5 re-froze note).

**[P077c3] Rust `sos new` → parity (bug-for-bug) + 3-layer fixture + doctor-absent re-froze (2026-07-22):**
- Third sub-phiếu of the P077c decomposition, heaviest command (dresses a whole greenfield tree). Additive-only, `bin/sos.sh` untouched (`git diff bin/sos.sh` empty). Shipped: (1) Rust `new` subcommand (`crates/sos-cli/src/commands/new.rs`, new clap `New` variant) — port of `bin/sos.sh:355-604` `sos_new` bug-for-bug (Category A freeze / Category C skeleton / Category B defaults / VALIDATOR / git-init+arm-hooks, no auto-commit); (2) **3-layer parity fixture** — `new.golden` (stdout), `new.tree.golden` (NEW — sorted path-shape manifest, no content, excludes `.git/`), `new.gen.golden` (NEW — content-hash manifest for GENERATED-authored files ONLY; copied kit assets stay tree-only, never hashed, to avoid coupling the fixture to every future kit-asset content edit — the same kit-content-coupling guard c2 established, applied here to the heaviest copier); (3) synthetic fake-kit **simpler than c2's** — plain directory tree, no git history needed (`new` only copies verbatim, never reads kit git history), except `scripts/install-hooks.sh` which is copied from the real checkout since it actually executes during `new`'s git-init step. **Critical finding (Worker CHALLENGE Turn 1, confirmed via live probe):** the pre-existing `new.golden` (P077a) had been captured on a machine with `doctor` on `PATH`, showing the full CONNECTED `[WIRED] J1..J6` block — a **host artifact, not a deliberate design choice**. Re-froze with `DOCTOR_BIN=/nonexistent/doctor` forcing the deterministic skip-branch (`⏭ doctor not found`), matching the CLI's own documented fallback and making the fixture host-independent. Two normalization bugs found+fixed mid-EXECUTE: (a) `.sos-stack.toml`'s full ISO-8601 `detected_at` timestamp was only half-stripped by the pre-existing bare-date rule (`<DATE>T14:23:01Z` leaking through) — fixed by adding `strip_timestamp` and running it BEFORE the bare-date substitution, in both `capture.sh` and `parity.rs`'s Rust-side equivalent; (b) a locale-dependent `sort` ordered `templates/claude-settings.local.json` before `templates/INVARIANTS-template.md` case-insensitively, mismatching Rust's byte-order `Vec::sort()` — fixed by pinning `LC_ALL=C sort` on both `capture.sh` sort call sites. `new.rs` also deliberately sorts its own TODO-placeholder list (documented divergence from Bash's raw, enumeration-order-dependent `grep -rl` — the FROZEN golden is already sorted via `capture.sh`'s pre-existing `sort_todo_block`, so Rust's direct sort matches the canonical oracle with no extra harness-side reordering, same precedent as `map.rs`'s `hits.sort()`). `PARITY_ENFORCED` += `"new"` (now `["map", "sync", "new"]`), dedicated `parity_new_enforced` test hard-fails on any of the 3 asserts. Negative tests: (1) sabotaged a `phieu/` copy-skip → failed specifically on the tree-manifest assert (stdout unaffected); (2) sabotaged one authored heredoc (`CLAUDE.md`'s Rules line) → failed specifically on the gen-content assert (tree unaffected); both reverted, re-green — proves the 3-layer split isn't redundant. `cargo build --workspace` + `cargo test --workspace` green; `map`/`sync`/`adopt` goldens byte-identical after re-running `capture.sh` twice (additive + determinism proof, both `new.golden`/`new.tree.golden`/`new.gen.golden` byte-identical between runs too). Docs updated: `bootstrap/sos-rs/README.md` (new parity status), `crates/sos-cli/tests/README.md` (3-layer fixture section + doctor-absent lever + normalization gotchas), `docs/plans/P077c-decomposition.md` (mark c3 done, feed-forward for c4).

**[P077c2] Rust `sos sync` → parity (bug-for-bug) + synthetic fake-kit determinism (2026-07-22):**
- Second sub-phiếu of the P077c decomposition, depends on P077c1's per-command harness mechanism. Additive-only, `bin/sos.sh` untouched (`git diff bin/sos.sh` empty). Shipped: (1) Rust `sync` subcommand (`crates/sos-cli/src/commands/sync.rs`, new clap `Sync` variant) — port of `bin/sos.sh:973-1053` `sos_sync` bug-for-bug, provenance oracle (`_blob_in_history`) replicated by shelling out to `git rev-list --all`/`hash-object` (no `git2` dep, confirmed absent from workspace `Cargo.toml`s); (2) **re-froze `sync.golden`** from a real-sos-kit-HEAD 0-change scenario (blind to all file-side-effect logic — same false-green class OA-02 P077c1 already fixed for `map`) to a **synthetic self-contained fake-kit fixture**: `capture.sh`'s `build_fake_kit()` does a real `git init` + 2 commits (blob v1→v2) inside a throwaway dir, `SOS_KIT_DIR` points there instead of real sos-kit — eliminates the real-HEAD dependency entirely (2 independent capture runs produced byte-identical output) and exercises all 4 outcomes (ADDED/UPDATED/FLAGGED/ALREADY-CURRENT) in one deterministic scenario; (3) **new `sync.tree.golden`** — sorted post-sync file-tree manifest (`<verb> <relpath> <sha256>`) verifying actual file placement/content (e.g. `.sos-sync-incoming/<rel>` really contains kit source bytes), which stdout counts alone can't prove; (4) `PARITY_ENFORCED` += `"sync"` (now `["map", "sync"]`), dedicated `parity_sync_enforced` test hard-fails on either stdout or tree-manifest mismatch. **Traversal-order finding** (Worker CHALLENGE pre-execute, Debate Log Turn 1): Bash's spine `find` is UNSORTED (raw filesystem creation-order, unlike `map`'s Bash `scan_files()` which pipes `sort`) — `sync.rs` deliberately does NOT sort its walk (unlike `map.rs`'s `hits.sort()`) to preserve bit-exact parity; verified matching on the same machine (`parity_sync_enforced` passed with no order massaging). Negative test (sabotaged the FLAGGED file-copy while still reporting it in stdout) confirmed the failure fires on the tree-manifest assert specifically, not stdout — proving the two-fixture oracle catches the exact false-green class it exists to prevent; reverted, re-green. `cargo build --workspace` + `cargo test --workspace` green; `map`/`new`/`adopt` goldens byte-identical after re-running `capture.sh` (additive proof). Docs updated: `bootstrap/sos-rs/README.md` (sync parity status), `crates/sos-cli/tests/README.md` (synthetic fake-kit section replaces old HEAD-pin caveat), `docs/plans/P077c-decomposition.md` (mark c2 done).

**[P077c1] Rust `sos map` → parity + per-command harness hard-fail flip (2026-07-22):**
- First sub-phiếu of the P077c decomposition (`docs/plans/P077c-decomposition.md`). Additive-only, `bin/sos.sh` untouched (`git diff bin/sos.sh` empty). Shipped: (1) Rust `map` subcommand (`crates/sos-cli/src/commands/map.rs`, new clap `Map` variant) — port of `bin/sos.sh:279-352` bug-for-bug (no generic `src/*.rs` surface, no kit-asset exclusion — that's OA-02, deferred to P077c5); (2) parity harness refactored `HARD_FAIL: bool` → per-command `PARITY_ENFORCED: &[&str] = &["map"]`, `sync`/`new`/`adopt` stay informational; (3) **two-fixture oracle for `map`** — Worker CHALLENGE (pre-execute, Debate Log Turn 1) found `map.golden` (123b) is a 1-line stdout confirmation only, NOT the surface data; the real work-product is the FILE `sos_map` writes (`<target>/docs/AGENT_MAP.yaml`), which the original stdout-only oracle never froze/verified — a false-green class OA-02-adjacent gap. Architect ACCEPTED (V2): `capture.sh` additively freezes that file's content to a new `map.agent_map.golden`; harness now hard-fails on either stdout OR file-content mismatch. Verified bit-exact against live Bash run on the same fixture (`routes/`+`models/` files); negative test (corrupted 1 surface name) proved the file-content assert catches it, reverted clean. `cargo build --workspace` + `cargo test --workspace` green; existing 4 goldens byte-identical after re-running `capture.sh` (additive proof). Docs updated: `bootstrap/sos-rs/README.md` (command-status table), `crates/sos-cli/tests/README.md` (per-command mechanism + two-fixture pattern), `docs/plans/P077c-decomposition.md` (feed-forward note for c3/c4).

**[P077b] Crate boundary carve + dependency-direction gate (2026-07-22):**
- Second sub-phiếu of the P077 decomposition. Additive/reversible, `bin/sos.sh` untouched (`git diff bin/sos.sh` empty) and remains canonical. Carved the flat `bootstrap/sos-rs` crate into a virtual workspace (`members = ["crates/*"]`) with 5 crates: `sos-core` (lifted `state.rs`, verified host-neutral — no `CLAUDE_*`/`.claude`/clap/permission token), `sos-cli` (composition root, binary `sos`, lifted `main.rs`+`commands/`, `use crate::state`→`use sos_core::state`), and 3 empty skeletons (`sos-install`, `sos-adapter-claude`, `sos-hooks` — logic in P077d). Dependency-direction gate shipped two-layer: (a) compiler graph — `sos-core/Cargo.toml` declares zero adapter/install/hooks/cli dep; (b) guard test `crates/sos-core/tests/dep_direction.rs` scans core src for forbidden tokens. Negative-test proof: temporarily added `use sos_adapter_claude;` to `sos-core/src/state.rs` → `cargo build -p sos-core` failed with `E0432 unresolved import` as expected → reverted (`git checkout --`). `cargo tree -p sos-core` shows zero sos-* deps; `cargo tree -p sos-adapter-claude` shows sos-core only, no sos-cli — direction proof. Parity harness (`tests/parity.rs`) + golden fixtures (`tests/golden/*.golden`) moved together into `crates/sos-cli/tests/` in the same git-mv (byte-identical content, confirmed via `git status --porcelain` pure-rename `R` entries) — `CARGO_MANIFEST_DIR`/`CARGO_BIN_EXE_sos` resolve correctly with no path-string edit needed. `cargo build --workspace` and `cargo test --workspace` both green; parity harness still prints "not yet parity" informational (`HARD_FAIL=false`, unchanged). Worker CHALLENGE (pre-execute) verified all 5 remaining `[needs Worker verify]` anchors clean — no coupling/host-leak issues found, accepted V1 with no objections. `docs/PORTABILITY_ARCHITECTURE.md` and `bootstrap/sos-rs/README.md` updated with module layout + the 3 known target deviations (workspace root stays at `bootstrap/sos-rs/`, `sos-adapter-codex` deferred to P078, install/adapter/hooks skeletons deferred to P077d). `CLAUDE.md` "Not a runtime binary source" contract left untouched (P077e owns the flip).

**[P077a] Rust workspace scaffold + freeze Bash golden oracle + parity-harness skeleton (2026-07-22):**
- First sub-phiếu of the P077 decomposition (`docs/plans/P077-decomposition.md`). Additive-only, `bin/sos.sh` untouched (`git diff bin/sos.sh` empty) and remains canonical through P077a–P077d. Shipped: (1) `bootstrap/sos-rs/Cargo.toml` gained a transitional `[workspace]` shell rooted at `bootstrap/sos-rs/` (NOT repo-root — that's P077e), crate `sos` still builds identically (`cargo build` green); (2) frozen Bash golden oracle for `new`/`adopt`/`map`/`sync` at `bootstrap/sos-rs/tests/golden/*.golden` + reproduction script `tests/golden/capture.sh`, with normalization for absolute paths, dates, and `new`'s filesystem-enumeration-order-dependent TODO-list (`bin/sos.sh:583` `grep -rl`, unsorted) — 2 independent capture runs verified byte-identical after normalization; (3) parity-harness skeleton `tests/parity.rs` — diffs Rust output (currently clap "unrecognized subcommand", since `new/adopt/map/sync` aren't implemented in Rust yet) vs golden, prints "not yet parity" per command, stays green (`HARD_FAIL` const, P077c flips it); (4) fixed `bootstrap/sos-rs/README.md` stale ownership claim ("move to its own repo") to match `docs/PORTABILITY_ARCHITECTURE.md` — workspace lifts INTO sos-kit, cutover is P077e. Worker CHALLENGE (pre-execute) surfaced a Tầng-2 refinement beyond the phiếu's anchor: `sos sync`'s take-newer/flag classification depends on sos-kit's OWN git history (`_blob_in_history`, `bin/sos.sh:992-999`), not just fixture-repo state — documented in `tests/README.md` with the pinned sos-kit HEAD sha the fixture was frozen against. `CLAUDE.md` "Not a runtime binary source" contract left untouched (P077e owns the flip).

**[P076] Claude adapter parity — declarative boundary (2026-07-22):**
- Sếp confirmed **(B) declarative boundary** at the phiếu's ESCALATE GATE (physical file move + renderer explicitly deferred to P077 — physical move proven infeasible in P076 scope without either flipping `.claude/settings.json` git mode `100644`→`120000` or writing a temp renderer, both plan-forbidden). Shipped: `adapters/claude/README.md` (boundary doc — adapter owns serialized representation only, one-way adapter→core dependency) + `adapters/claude/MAPPING.md` (artifact → class → `core/ROLES.md#<role_id>` mapping table, covers golden baseline sections 1,2,3,4,5,6,8 — `bin/sos.sh` CLI surface (section 7) and `doctor` connectivity (section 9) stay out of scope per `core/ASSETS.md` line 35). Annotated inert HTML-comment provenance markers (body-only, zero frontmatter touch) on 11 files: 5 `agents/*.md` + `agents/README.md` + 5 `skills/*/SKILL.md`. Worker CHALLENGE verified all 6 `[needs Worker verify]` anchors (#7-9, #11-13) against real repo state pre-execute; golden parity re-run post-execute confirmed sections 1,2,3,4,6,8 diff = 0 (frontmatter `name`/`model`/`tools`/`caller` byte-identical; `.claude` tree modes/symlinks/settings.json/`.mcp.json` untouched). Docs updated: `docs/LAYERS.md` access-matrix note, `docs/HANDOFF.md` Handoff 2 boundary note, `core/ASSETS.md` transitional-mixed rows (P076 DECLARED), `CLAUDE.md` repo-tree `adapters/` entry. "Not a runtime binary source" contract left untouched (P077 owns).

**[P083] Doc-drift cleanup — README install fresh-clone break + LAYERS v1-residue re-frame + tagline overclaim (2026-07-22, out-of-sprint, Sếp-ratified):**
- External kit review found 3 real drift, orchestrator verified with file:line, Worker CHALLENGE accepted V1 (anchors #2/#4/#8 confirmed). Fixed: **F3** `README.md:238-255` + `docs/SETUP.md` skill-install blocks copied 12 skills incl. 8 attic'd ones (`insight route decide plan verify review qa ship`) → FAIL on fresh clone; trimmed to the 5 living (`init forge apply retro` global + `idea` project-local), matching skill tables at `README.md:134-142` and `docs/LAYERS.md:216-222`. **F2** `docs/LAYERS.md` re-framed v1 "Kiến trúc sư lives in Claude Web Project" residue (`:48,76,84-85,91-92,104,115,166,195-198`) → v2 subagent-in-session model (Architect's docs-only envelope is a tool grant per `agents/architect.md`, not a location); reconciled ASCII layer-box skill lists to 5-living + attic notes; kept Tầng-def single-source (`:145-154`) and Chủ nhà's Claude-Code-or-Web row (`:77`, human, not a drift target) untouched. `phieu/RELAY_PROTOCOL.md` gained a v2-mode banner (protocol now scoped to v1/cross-session mode only). `docs/HANDOFF.md` Handoff 3 (`:146`) updated to point at the in-session Debate Log path (Handoff 2.5) as v2 default; Handoffs 0/1/2/4 v1-baseline framing (`:31,35,47,70,163,208,258`) left as-is (pre-existing v1/v2.1 banner at top already scopes them) — logged in Discovery. **F1** `README.md:3` tagline changed to Sếp-approved Option A: "One person. No team. Full operating system from code-ready to production health" (was "from inbound request", contradicting `docs/PHILOSOPHY.md:26` "tail of the pipeline"). Docs-only phiếu, no runtime/code touched.

**[P082] Lane-field template drift fix — OA-01 (2026-07-22, out-of-sprint, Chủ nhà-ratified):**
- Audit finding OA-01 🔴: `agents/orchestrator.md` lane budget pre-CHALLENGE gate runs `doctor lane-check --ticket <path>`, which requires a `**Lane:**` header field, but `phieu/TICKET_TEMPLATE.md` never had one — every phiếu born from the template exited 2 (missing/unparseable field), blind to the gate. Fixed entirely inside sos-kit (no `~/doctor` source touched): added `**Lane:** Normal` (bare token, 2-axis note: Tầng=consequence/debate-flow vs Lane=budget/size-cap) to the template header; added `scripts/lane-check-contract.sh` (degraded warn-skip when `doctor` absent, exit-2→FAIL LOUD / exit-1→WARN / exit-0→OK); wired as sub-check `3f` inside pre-commit `[3/8]` (no new phase, `[8/8]` count unchanged) firing only when `phieu/TICKET_TEMPLATE.md` is staged. Verified via real oracle runs (not fixture): template pre-fix exit 2 → post-fix exit 0 (`Normal — 181 lines, 3 anchors, 2 constraints`, well under 250/5/5 budget — no F1/F2 fallback needed); regression test on a temp-swapped copy confirmed FAIL LOUD (exit 1) when Lane field is stripped. Docs updated: `phieu/README.md` (Tầng vs Lane explainer), `docs/HANDOFF.md` Handoff 2 (header field list), `CLAUDE.md` (scripts list + hook-chain note), `docs/SETUP.md` (scripts inventory).

**[P075] Portable SOS Core — neutral roles, workflow, policy and asset ownership (2026-07-20):**
- Added `SOS.md` plus `core/README.md`, `core/ROLES.md`, `core/WORKFLOW.md`, `core/POLICY.md` and `core/ASSETS.md` as the runtime-neutral semantic contract. Roles now use intent-level capability names instead of host tool/model/event vocabulary; lifecycle defines one ticket as one delivery unit (`gates → commit → push/merge → verify remote → next ticket`); policy centralizes authority, bounded delegation, information envelopes, oracle/scope/safety rules; asset ownership separates portable, transitional, adapter-owned and generated content. Negative runtime-token oracle passes with zero hits. Existing adapter behavior is unchanged; parity migration remains P076.

**[P074] Portability architecture — one monorepo, one `sos`, modular adapters (2026-07-20):**
- Added tracked-tree runtime inventory and target architecture. Chủ nhà selected ownership A: Rust CLI remains in `sos-kit`; P077 will promote the skeleton into the canonical workspace and update the old “not runtime source” contract. Target separates portable core, Claude adapter, Codex adapter and generated artifacts while distributing one product/version/binary. One-command install uses a managed tool manifest for hooks, doctor, inv/docs gates, doc-rotate and ship; sister tools need not be source-merged before the UX is unified. No runtime behavior changed in P074.

**[P073] Trust gate — auto-exec baseline-diff + hidden-unicode (2026-06-15):**
- Open-source hardening Leg 3 of 3 (port thanhtra v1.2). Ships: `scripts/trust-gate.sh` (two checks: sha256 baseline-diff of 20 auto-exec surfaces + hidden-unicode scan of instruction/doc files); `.sos-trust-baseline` (committed hash snapshot, 20 surfaces: 5 non-script + 13 scripts + install.sh + phieu/phieu.sh + templates/setup-dev.sh); `SECURITY.md` (threat model, 6 invariants, trust anchor, rebaseline workflow, future-work notes); `hooks/pre-commit` bumped to `[1/8]`…`[8/8]`. Unicode pre-fix: escaped 2 raw U+FEFF chars (`phieu/DISCOVERY_PROTOCOL.md:196` + `docs/ticket/P073-trust-gate.md:45`); re-scan confirmed 0 remaining. Challenge resolution: BROAD scan scope (incl. `docs/ticket/`), `settings.local.json` excluded (globally gitignored), 3 extra surfaces added (`install.sh` + `phieu/phieu.sh` + `templates/setup-dev.sh`). Discrimination tests (3 cases) PASS. `INVARIANTS-template.md`: N/A (gate is sos-kit-specific, not a generic project-level INV).

**[P071] Stage 2 — install.sh sha256 fetch-verify shipped (2026-06-15):**
- `install.sh` now verifies every binary download against a published `.sha256` checksum (P071-Stage2). Two additions: (1) Hash-tool probe after platform-detect: prefers `sha256sum` (Linux-native; macOS also has `/sbin/sha256sum` Darwin 1.0 — [O1.1] self-adapt), falls back to `shasum -a 256`. Abort if neither present. (2) Verify block inside `fetch_bin()`: fetches `${url}.sha256`, recomputes hash of `${dest}.tmp` via `$SHA_CMD`, first-field string-compare (NOT `sha256sum -c` — filename differs across `.tmp` rename). Mismatch → ABORT. Required-bin missing `.sha256` → ABORT. Optional-bin missing `.sha256` → warn + install unverified. Mac discrimination test (3 cases) PASS: good binary verifies green; corrupted binary (byte-flipped) rejected; missing `.sha256` on required aborts, on optional warns. Linux + Windows Git Bash dogfood pending (Sếp). `docs/security/INVARIANTS.md` INV-LOCAL-1 flipped from candidate to active. `docs/SETUP.md` N/A (no user-facing install step change — dev path is cargo install). Version pinning → follow-up `[P-pin]`.

**[P072] Fleet Node20-action bump — all 10 repos rc-oracle PASS (2026-06-15):**
- Fleet bump of `release.yml` across all 10 tool repos (`doctor`, `claude-hooks`, `docs-gate`, `ship`, `advisory-inbox`, `inv-gate`, `guard`, `vps`, `doc-rotate`, `advisory-cron`) — `actions/checkout@v4→v5`, `softprops/action-gh-release@v2→v3`, `actions/github-script@v7→v8` (where present). Added `prerelease: ${{ contains(github.ref_name, '-rc') }}` so rc tags publish as prereleases and don't claim the `latest` pointer. Sequential per-repo (IG-09): each repo committed → rc tag pushed → CI verified (3/3 green, 0 Node20 deprecation annotations, prerelease flag correct, latest unchanged) → rc tag+release deleted → next repo. 4 optional repos (guard/vps/doc-rotate/advisory-cron) had old-style `release.yml` (scalar `files:`, no sha256, no draft/publish) and were upgraded to the full doctor template in the same touch (P071+P072 combined). GitHub Node20 runtime deprecated 16/06/2026.

**[P071] phiếu drafted + approved — open-source hardening leg 2 of 3 (2026-06-15):**
- Release-asset integrity phiếu (`docs/ticket/P071-install-checksum.md`) — closes the supply-chain HIGH on the `curl|sh` installer (`install.sh` pulls 10 release binaries with NO checksum + unpinned `releases/latest`). Workflow: Architect DRAFT → Worker CHALLENGE (0 Tầng-1 objections; verified all 10 sibling repos' `release.yml` shape, the critical `$2`-scope-in-`fetch_bin` POSIX semantics, `files:` scalar→`|`-block, mac-has-/sbin/sha256sum) → APPROVAL_GATE (Sếp 2026-06-15): **rollout Path A (clean — re-release all 6 required repos + verify `.sha256` present BEFORE flipping install.sh enforce); Task 5 version-pinning SKIPPED → follow-up `[P-pin]` (checksum closes the HIGH, pinning is reproducibility)**. 3 Tầng-2 items self-adapt at EXECUTE. Task 6 (2-OS discrimination test) acceptance-gated on the Linux+Windows dogfood. Sprint setup: `docs/BACKLOG.md` Active sprint `open-source-hardening` (3 legs gate public) + `.gitignore` for thanhtra (3rd-party scan skill) artifacts.

**`.mcp.json` PATH-portable — open-source hardening leg 1 of 3 (2026-06-15):**
- Pre-open-source security pass flagged `.mcp.json` shipping a hardcoded `"command": "/Users/nguyenhuuanh/.cargo/bin/doctor"` — both a per-machine path that breaks on every clone AND a maintainer-username leak. Changed to PATH-resolved `"command": "doctor"` (install.sh installs binaries to `~/.local/bin`), matching the doctrine already written in BACKLOG ("MCP wiring PATH-rel, never hardcode `~/.cargo`") — closing the doctrine-vs-reality drift (BACKLOG line 246 claimed `.mcp.json` was "đã sạch"; it wasn't). Opens the **open-source-hardening sprint** (3 legs gating public release): leg 1 = this path/username leak (done); leg 2 = `[P071]` install.sh release-asset checksum + version pinning; leg 3 = `[P-trust-gate]` auto-exec baseline-diff (port from thanhtra v1.2 — hooks/`.mcp.json`/settings change = FAIL until reviewed + rebaselined).

**`sos sync` dirty-warn (2026-06-11):**
- Same JA-08 doctrine as adopt, triggered by a live incident: Quản đốc synced into inv-gate while its session was MID-PHIẾU and a careless `git add -A` staged that repo's in-flight work (own gate blocked the commit; staging reset). sync now warns when the target tree is dirty: spine files still land, but explicitly instructs NOT to `git add -A` — the target repo's own session commits. Warn-not-block.

**IG-12 harvest — shared-working-tree race (inv-gate Sprint 3, 2026-06-11):**
- First REAL instance of the parallel-phiếu race Rule 6 only warned about theoretically: running P009 + P010 concurrently in ONE working tree, Quản đốc committed P009 evidence while a P010 Worker had its own branch checked out → orchestrator was on the Worker's branch, evidence staged into its WIP; the release binary vanished mid-build → **fail-closed build-guard [4/7] caught the bad commit** (a gate built for another purpose saving an out-of-design accident — proof the fail-closed layer earns its keep). Doctrine landed: (a) orchestrator must NOT git commit/checkout/add/stash the shared tree while `worker-active` is set (the Worker commits, worker.md step 8) — orchestrator.md Marker-safety + ORCHESTRATION Rule 6; (b) deliberate parallel-phiếu fan-out → spawn each Worker with the Agent tool's `isolation: "worktree"` (own git tree); sequential default keeps the shared tree (no worktree overhead) — Rule 13(g). Source: inv-gate docs/SOS_KIT_FEEDBACK.md IG-12.

**inv-gate dogfood harvest #2 — 6 actions + IG-08/09 worker rules (2026-06-11):**
- Harvested the full inv-gate end-to-end dogfood (8 phiếu/2 sprints in 1 day; 11 findings; report `docs/retro/FRICTION_inv-gate_e2e-dogfood_2026-06-11.md`). Actions executed: **(1)** `inv-gate` joined `install.sh` BINARIES (v0.1.0 public, 3 assets). **(2)** `scripts/security-gate.sh` now **binary-first** — `inv-gate check secrets|runtime` when the binary is present (kills the last python3 dependency in the gate chain), python3 `check-*.py` kept as fallback so pre-binary repos don't regress; per-check swap (not `gate --all`) per IG-06; both branches fire-tested green. **(3)** install.sh platform (IG-10): Intel-Mac → arm64 via Rosetta + 3-platform-arch scope documented in the error branch. **(4)** Darwin downloads strip `com.apple.quarantine` xattr (IG-11 — Gatekeeper "can't open" on curl'd binaries). **(6)** doctrine items (GATE_DELEGATION / symmetric marker / Debate-Log-state / model-selection) confirmed already synced. **Worker contract gained two rules:** IG-08 — synthetic secrets in `src/**`/tests must be non-contiguous (`format!("sk_live_{}", x)`) or GitHub Push Protection blocks the push (invisible to local gates + cargo test; the 3rd "ship≠chạy" layer = remote boundary); IG-09 (direct Chủ-nhà feedback) — ship ONE phiếu per merge cycle: commit → review → merge → push → delete branch → next phiếu (no stacked branches; a stack let one P003 token poison-block 5 branches → 7-commit rewrite). **Flagged not executed:** action 5 (Node20 deprecation, deadline 16/06) is a 10-repo fleet op whose canonical bump is unshipped (inv-gate P009 in flight) → BACKLOG [FLEET-NODE], mirror when it lands. Open decisions → BACKLOG: IG-07 local-merge sentinel gap, IG-01-deeper guard role-distinction, 5-target platform expansion, W16 gate-not-Rust-aware.

**MODEL_SELECTION rule — Quản đốc owns per-spawn model choice (2026-06-11):**
- Worker model analysis (Sếp + Quản đốc, grounded in claude-api reference + Sprint 1 evidence): sonnet 4.6 stays the worker DEFAULT (fast/cheap, fits mechanical CHALLENGE + oracle'd EXECUTE; fable = ~4-5× cost incl. new tokenizer, minutes-long turns, second-architect risk, refusal-classifier risk on security-tooling content, and homogeneous-debate loss vs a fable architect). Escalation is per-spawn via the Agent tool's native `model` param (overrides frontmatter — no hot-swap needed): E1 Tầng-1 cross-system CHALLENGE needing environment reasoning (IG-06 class) · E2 EXECUTE without oracle (creative/algorithmic) · E3 failed-once-on-sonnet retry · E4 Chủ nhà chỉ định. Every override logged in the phiếu Debate Log. Rule landed in orchestrator.md + ORCHESTRATION.md 12b.

**inv-gate Sprint 1 harvest — marker symmetry + GATE_DELEGATION + Debate-Log-as-state + cite-ranges (2026-06-11):**
- inv-gate shipped 5/5 phiếu in ONE day (P001 pin-oracle → P002-005 ports, byte-exact parity, per-check dogfood swap) = first FULL workflow run on a fresh adopt-born repo. Watchlist 19/23 ticked, 6 findings (`~/inv-gate/docs/SOS_KIT_FEEDBACK.md`). Headline proof: CHALLENGE lane caught a sprint-goal assumption break BEFORE ship (IG-06: swapping the parity binary wholesale into pre-commit would have blocked every commit — hook runs the 99-LOC adapted gate, not the 210-LOC golden; "ship được vs chạy được" gap is real and the lane paid for itself). Doctrine TRANSFER verified (W11: agents followed pin-before-port from CLAUDE.md without being told — first evidence written doctrine self-executes in a fresh repo).
- Fixes landed from findings: **(IG-01 ×2)** marker protocol now SYMMETRIC — "after Architect returns: rm architect-active" added to orchestrator.md + ORCHESTRATION Rule 6 + banner (stale marker turned architect-guard against the main session itself). **(IG-03)** GATE_DELEGATION first-class: Chủ nhà may delegate APPROVAL_GATE for a bounded scope; self-approve only on oracle-closed CHALLENGE; never for scope/vision/security/irreversible; grant logged in Debate Log. **(IG-05 ×2)** notification-routing caveat: challenge reports can return via the architect task-id (loop self-closes) — phiếu Debate Log = single source of truth for state/turn-count, read before every spawn. **(IG-04)** architect.md: cite RANGES never counts (7→8→9 drift observed across one phiếu's hand-offs). Backlog'd: security gate not stack-aware (.rs unprotected — W16 reverse), phieu.sh CLI 0-use caller-law case (W22), sprint-item assumption-note (IG-06), P7 calibration n=1 ≈60-70%% fit.

**Skills: 13 → 5 living + caller law + idea-smell hook [skills dogfood] (2026-06-11):**
- Full-13 dogfood (report: `docs/retro/SKILLS_DOGFOOD_2026-06-11.md`; evidence: tarot registered all 13 for months, 0 invocations). **Caller law ratified (Sếp):** a skill ships only with a declared mechanical `caller:` frontmatter field (hook/cron/CLI/gate/handbook) — "skill đẹp mà không dùng = không tác dụng". **8 parked** to `skills/attic/` (plan = STALE v1 Web-Project doctrine + absorbed in architect.md; verify = worker.md inlines Task 0 ×14 + name-collides built-in; decide/route/insight/qa/review/ship = no caller, roles absorbed — reasons + revive conditions in attic/README.md). **5 living with callers:** idea (NEW `scripts/idea-smell.sh` UserPromptSubmit hook — regex idea-smell in Sếp's message → inject /idea reminder; fire-tested both ways; wired in settings.json), retro (weekly-cron caller declared; register BLOCKED on advisory-cron fire_task no-timeout debt — honest pending), init/apply/forge (sos CLI, 0→1 phase, n=0). Living skills now **symlink-registered into sos-kit's own `.claude/skills/`** (fix: kit couldn't invoke 12/13 of its own skills — `Unknown skill`). `sos new/adopt/sync` exclude attic (fire-tested: adopt ships exactly 5). Known collision logged: kit `/init` vs Claude Code built-in `/init`. README/LAYERS/CLAUDE.md skill maps rewritten around callers.

**Full-toolset manifest + verify-setup shim-aware + jarvis spine restore (2026-06-11, follow-on):**
- **Installer manifest 2 → 9 binaries** (Sếp: "chạy 1 phát là ok hết"): release-CI added to `docs-gate`/`ship`/`guard`/`vps` (v0.1.0) + `doc-rotate` (v0.21.0) + `advisory-inbox`/`advisory-cron` (v0.1.0) — same 3-target workflow, asset contract unchanged. `install.sh` BINARIES now pulls the full set (advisory-cron register stays per-repo opt-in; binary just lands). `sos adopt` .mcp.json jq-merge now wires the same 5 MCP servers `sos new` ships (doctor/docs-gate/ship/guard/vps, `//=` non-clobber, fire-tested merge + idempotent) — was doctor-only.
- **doctor v0.1.1 — verify-setup B+3-shim-aware (Thợ executed, 40/40 tests):** J1/J6 now recognize the merge hook delegating to the `claude-hooks` binary (`exec claude-hooks block-unsafe-merge`) — agent-side contract still verified, hook-side delegated to the binary's parity suite. Found via Chủ nhà's live install test (fixture DORMANT that should be CONNECTED). Also surfaced: sos-kit itself lacked `docs/security/INVARIANTS.md` (J4) — now seeded from its own template.
- **Jarvis spine restore via `sos sync` (n=3):** the parallel jarvis session's housekeeping had deleted `scripts/` + `phieu/` + the security agent pair and regressed 3 agents to a pre-`background: true` canonical version. `sos sync ~/jarvis` re-added 55 files + take-newer'd the 3 stale-canonical agents (blob-provenance worked exactly as designed); 2 genuine customizations (jarvis's own `hooks/pre-commit` with pytest phase + `idea` skill) correctly flagged to `.sos-sync-incoming` for hand-merge. Lesson logged: a sibling session can clobber the spine post-adopt — `sos sync` is the cheap recurring cure.

**"1 lệnh" SHIPPED — install.sh (curl|sh) + prebuilt releases + B+3 shim + setup-dev golden [P064+P065] (2026-06-11):**
- **Tier-3 distribution built and acceptance-tested.** (1) **Release CI** added to `doctor` (v0.1.0) + `claude-hooks` (v0.9.1) repos — tag `v*` builds the 3 ratified targets (mac-arm64/linux-x64/win-x64) and attaches to GitHub Releases; asset contract `<bin>-<triple>[.exe]`; both pipelines GREEN first run (~3min). (2) **`install.sh`** (repo root, `curl -fsSL …/install.sh | sh`): platform detect → download prebuilt binaries to `~/.local/bin` (fail-closed: failed download ABORTS) → clone kit → `sos` launcher (sh wrapper, not symlink — Windows Git Bash safe). NO Rust needed (closes JA-06; cargo = dev path). (3) **B+3 fail-closed shim [P064 ratified]:** `scripts/block-unsafe-merge.sh` bash logic replaced by shim → `exec claude-hooks block-unsafe-merge`; binary absent → exit 2 BLOCK LOUD with the install one-liner (never silent fail-open — P059 class killed). Live-fire proof: the gate blocked the orchestrator's own test command mid-session (shim→binary→`gh pr diff` verify→fail-safe block). Parity note: force-push was never gated by the old bash either (doc drift fixed in CLAUDE.md/INSTALL.md descriptions). 3 fail-open hooks stay bash (absent=allow=correct default). (4) **`templates/setup-dev.sh` golden [P065]** generalized from tarot P344: Rust check → cargo install TOOLS list → F09-guarded hook arming; advisory-cron register stays opt-in. **Acceptance test:** fresh-machine simulation (PATH = only the curl-downloaded binaries) → `sos adopt` full flow → born-wire armed + `doctor verify-setup` CONNECTED. **Giám sát review: NEEDS_REVIEW → hardened same-branch:** curl `--connect-timeout 30 --max-time 300` + `GIT_TERMINAL_PROMPT=0` (INV-2 timeouts); shim now execs trusted install paths FIRST (`~/.local/bin` → `~/.cargo/bin` → PATH last) killing the PATH-spoof vector on the gate binary. Supply-chain residue captured as explicit deferral [P071]: no checksum/signature on release assets + unpinned `releases/latest` — acceptable at n-user=1/2FA, cure (.sha256 publish+verify) planned before public.

**`sos adopt` born-wire + INSTALL.md 1-command rewrite — JA-03/04/05/07/08/09 closed (2026-06-11):**
- Installer step-4 build, cụm 1 (requirements from the jarvis friction log): `sos adopt` now **born-wires** like `sos new` — new `[3/4] Born-wire` step arms hooks via F09-guarded `install-hooks.sh` (declined → clear report, repo's own hooksPath untouched) + runs `sos init security` (stack detect → `.sos-stack.toml`); **jq-merges** the `doctor` entry into an existing `.mcp.json` (`//=` — never touches existing keys) and the 5 marker perms into `settings.local.json` (unique/idempotent; no jq → precise hand instructions); **warns on a dirty target tree** (warn-not-block); report footer now guides the FIRST gated commit (fresh CHANGELOG entry + fill BACKLOG + restart Claude Code). Fire-tested 3 fixtures: full born-wire on a dirty repo (verify-setup CONNECTED), idempotent re-run (merges hit "kept" branches), F09-declined custom-hooksPath repo (no clobber). **INSTALL.md rewritten** to lead with the 3-command table (`sos new`/`adopt`/`sync`) + one-time `sos`-on-PATH step (JA-06 partial — full cure = curl|sh installer); manual steps demoted to a reference appendix. Remaining for "1 lệnh": prebuilt binaries + curl|sh + B+3 shim [P064] + setup-dev.sh [P065].

**JARVIS foreign-adopt dogfood — 2 adopt bugs fixed + 12-finding friction log (2026-06-11):**
- Step 2 of the dogfood-BEFORE-infra plan: ran `sos adopt` against `~/jarvis` (fresh-foreign Python bot — the real new-user portrait). **JA-01 🔴 FIXED:** adopt dropped ALL 4 spawnable agents — kit's `.claude/agents/*.md` are symlinks (drift-fix 2026-06-01) and `adopt_item` walked with `find -type f`, which skips symlinks; `sos new` dodged this via `cp -RL` but adopt didn't. Fix: `find -L` in `adopt_item` (bin/sos.sh). Verified: re-adopt lands all agents, `doctor verify-setup --repo ~/jarvis` J6 BROKEN → **CONNECTED rc=0**, third run idempotent. **JA-02 fixed:** adopt generated a competing root `CHANGELOG.md` skeleton when the repo keeps its changelog at `docs/CHANGELOG.md` (per its own `.docs-gate.toml`) — now checks `docs/CHANGELOG.md` before generating. Full harvest (12 findings = installer "1 lệnh" requirements: adopt should born-wire like `sos new` — arm hooks + init security; jq-merge JSON; `sos` on PATH; INSTALL.md vs `sos adopt` unify; dirty-tree warn; guided first-commit): `docs/retro/FRICTION_jarvis_foreign-adopt_2026-06-11.md`. Bonus: INV-010 caught a REAL committed GitHub PAT in jarvis day-1 (JA-12 — routed to jarvis: revoke + env var); F09 install-hooks guard fired correctly on jarvis's pre-existing `.git/hooks/pre-commit` (n=1 foreign, worked-as-intended). Giám sát reviewed the branch (APPROVE 5/5 INV) and flagged a latent `find -L` supply-chain risk — hardened same-branch: adopt_item now `realpath`-guards every deref'd file to stay inside the kit tree (out-of-tree symlink → skip + warn; fire-tested with an `/etc/hosts` symlink: skipped, not copied, legit flow idempotent).

**Agents — `background: true` rollout (2026-06-09):**
- All 4 spawnable subagents (architect, worker, advisory-watch, boundary-check) now carry `background: true` in frontmatter → run in the background by default when spawned, so the main session isn't blocked (incident 2026-06-09: a foreground subagent queued Chủ nhà's messages until it finished). `orchestrator.md` excluded (main session, never spawned). `agents/README.md` documents the convention + 3 enable mechanisms + hard rule "new spawnable subagent → add `background: true`". Consistent with the corrected background-spawn doctrine (Rule 13). Effective only in NEW sessions.

**F09/F13/F10 fixed — doc-rotate friction harvest [P070] (2026-06-09):**
- doc-rotate Rust-port friction log (13 findings) reconciled: F01-F08 already actioned this session. F09-F13 are new. **F09 🔴 FIXED:** `install-hooks.sh` silently hijacked an adopter's `core.hooksPath` + renamed their `.git/hooks/*` → could disable an existing security gate (1-installer-2-audiences bug; tarot P275 hit it). Added a guard: detect prior `core.hooksPath` (≠ hooks) or a real `.git/hooks/pre-commit` → confirm (TTY) / ABORT exit 1 (non-TTY) before overriding. Fire-tested 3/3 (clean proceeds; adopter-hooksPath non-TTY aborts without clobbering; idempotent). **F13 fixed:** scaffold `version` 0.0.0→0.1.0 (`bin/sos.sh`) + DOCS-GATE row "CHANGELOG bump → manifest version sync". **F10 fixed:** DOCS-GATE row "language port/module rename → AGENT_MAP paths + re-run validate-map". F11/F12 (heuristic-tool content-bending) captured as a recipe-level escape-hatch lesson. Source: docs/retro/FRICTION_doc-rotate_rust-port_2026-06-09.md.

**P069 SHIPPED — Architect Write-envelope now hook-enforced (2026-06-09):**
- `architect-guard.sh` now dispatches by `tool_name`: Read/Glob keep the read-block; **Write/Edit/MultiEdit → allowlist ONLY phiếu files** (`basename` matches `P[0-9]*-*.md`) — code/scripts/other-docs are blocked (exit 2). Closes the gap where architect.md said "only Write phiếu" but no hook enforced it (any .md / kit-maintenance write slipped through). `.claude/settings.json` architect-guard matcher `Read|Glob` → `Read|Glob|Write|Edit`. Symmetric to orchestrator-guard (allowlist vs that denylist). Fire-tested 9/9 (Write phiếu→allow; Write scripts/docs-random/Edit src→block; Read .md→allow; Read src→block; no-marker→allow-all). Downstream gets it via sos sync (doc-rotate later — hot; claude-hooks keeps its own combined port-reference version).

**Ratified: fail-closed binary-hook deploy = B+3 (shim + prebuilt) [P064] (2026-06-09):**
- Sếp ratified the canonical deploy pattern for a fail-CLOSED hook binary (block-unsafe-merge) when the binary may be absent from PATH: **fail-closed shim** (`command -v claude-hooks || exit 2` then exec) + **prebuilt binary distribution** (curl|sh, = P064). NOT the bash-fallback wrapper (Option A) — it reintroduces the bash fragility the port exists to kill (P059 showed the bash gate silently failed open on Windows = the F-005 rot risk) and demands perpetual bash-parity maintenance. The 3 fail-open hooks wire the binary directly (absent = allow = their default). Applies the kit's own fail-closed security doctrine. Feeds P064 distribution build + a claude-hooks README update (adopter-choice → sos-kit canonical).

**Architect Write-envelope gap captured [P069] + claude-hooks sync loose-ends closed (2026-06-09):**
- Closed the claude-hooks sos-sync flags: merged P061 track-rule into worker.md (kept background:true); kept claude-hooks's deliberately-customized architect-guard.sh (its Rust-port reference). The architect-guard divergence surfaced a real KIT finding [P069]: `architect.md` says Architect may "only Write phiếu files" but NO hook enforces it — architect-guard fires on Read|Glob only, orchestrator-guard denylists product-source but allow-lists kit-maintenance (scripts/docs/*.sh/*.md), so an Architect could Write non-product non-phiếu files unguarded. The old combined architect-guard (tarot/claude-hooks) had a Write-allowlist branch the canonical split dropped. Latent (n=0), captured for a decision (re-add allowlist branch vs accept prompt-only).

**Code-poison probe pass on media [P068] (2026-06-09):**
- Ran the existing-tool battery on a media-rating-app clone (oracle = known privacy bug) to map catch-vs-miss before building any "adopt-poisoned flow." Confirmed the reframe: no giant flow needed. CAUGHT: a committed DB password in CI/compose (check-runtime-secrets). MISSED/limited: check-hardcoded scans staged-only (blind on an existing repo → needs a full-repo audit mode); validate-map needs an AGENT_MAP (sos map builds one); the **logic/authz privacy bug** (`detail.py` public `media_detail` route + unfiltered review query at L169) is invisible to ALL mechanical tools — but the kit already has the right detector (Giám sát boundary-check INV-3 cross-user-resource), it just reviews PR diffs not existing full code. So the code-poison half = orchestrate existing tools into a scan pass + run Giám sát/`/review` in full-repo audit mode for the logic class; ~2 small audit-mode flags, not a new flow. Logic-poison is irreducibly judgment → route to Giám sát, don't build a mechanical authz-checker. media-specific findings (DB password, privacy bug) routed to the media repo. Captured [P068].

**`sos sync` — KIT-LAG cure shipped + proven, n=2 [P067] (2026-06-09):**
- New `sos sync <repo>` command (`bin/sos.sh`) — re-syncs the kit spine into a repo adopted from an OLDER kit version. **Provenance oracle = sos-kit's own git history** (no manifest needed → works retroactively on already-adopted repos): a downstream spine file whose blob matches SOME historical version of its canonical path = unmodified stale-canonical → **take-newer** (overwrite); matches none = repo-customized → flag to `.sos-sync-incoming` for manual merge. Repo-identity files (CLAUDE.md/BACKLOG/.docs-gate.toml/INVARIANTS/AGENT_MAP) never touched. **Fire-tested on a media-rating-app clone (v2.0→v2.3): the 27-conflict manual-merge pile collapsed to 2** — 9 auto take-newer (incl. `pre-commit [1/3]→[1/7]`, ORCHESTRATION.md, phieu.sh), 18 already-current, 2 genuinely-customized agents flagged. `doctor verify-setup` exit 0 post-sync; identity files untouched. Closes the KIT-LAG half of Blocker C (the recurring "no upgrade path" gap). MVP caveats: history scan slow on huge repos; eol/gitattributes may affect cross-platform hash-match (mac/linux LF verified). Built directly + dogfood-verified (not via phiếu — kit-tooling, Sếp-directed).

**Tier-0 closed + kit-adopt stress finding [P067] (2026-06-09):**
- Tier-0 fully done: advisory-cron register ✓ (tarot) · doctor trigger ✓ · advisory-inbox 8-column schema ✓ (verified template + tool row).
- **First foreign-repo KIT stress test** — cloned media-rating-app (v2.0 kit: 4 agents, `[1/3]` pre-commit) to /tmp (real repo had 28 uncommitted → untouched) and ran current `sos adopt` (v2.3). Result: 35 clean adds + **27 conflicts to `.sos-adopt-incoming`**. Classification finding: **~all 27 conflicts are stale-canonical (take-newer), NOT repo customizations** (proof: `skills/qa/SKILL.md` 0-diff identical; `[1/3]` pre-commit just missing the newer sections). adopt is correct for fresh-adopt (additive) but **has no upgrade path** — it can't distinguish stale-canonical from customized, so it dumps 27 manual merges where ~26 are mechanical take-newer → stale-kit repos stay stuck (hard evidence for KIT-LAG, n=1). Direction: a `sos sync` with a **provenance manifest** (record each spine file's origin canonical-hash at adopt; at sync, unmodified-since-copy → safe take-newer) collapses 27 → ~1. Captured [P067]. Closes the KIT-LAG half of Blocker C; the code-poison half remains unprobed (adopt only touches kit files).

**Status correction — trigger-wiring largely DONE (2026-06-09):**
- Investigated "A" (trigger-wiring, Tier-0) before building → found the 2026-06-05 BACKLOG snapshot stale. `doctor` is wired at every point (binary installed, `.mcp.json` serve, handbook calls — orchestrator lane-check / worker runtime-scan / boundary-check validate-map, adopt's `verify-setup` which DOES exist). `advisory-cron` is registered for tarot (launchd daily). The "doctor không auto-fire / chưa nhét vào bootstrap" claim was outdated. Updated §B.2 + Tier-0 to reality; only residual = advisory-cron not auto-offered in adopt (arguably correct — opt-in per deps-heavy repo, not noise on thin repos). No wiring work manufactured; honest "it already runs."

**Capture — tarot tool-adoption KIT items [P064-P066] (2026-06-09):**
- **tarot tool-dogfood PASSED** — doc-rotate (P343/PR#627) + claude-hooks (P344/PR#628, Giám sát APPROVE) run OK in tarot prod. Tool-findings routed to the tool repos. KIT-level items captured (capture-only; Sếp's timing = kit work comes AFTER tools prove out on more projects). P064 (K-1, 🔴): the "1 lệnh" installer vision corrected — `cargo install` assumes Rust is present (like npm assumes Node), so distributing to non-coders needs **prebuilt binaries (GitHub Releases, 3 targets) + `curl|sh`**, with cargo/crates.io as the dev path; this is Tier-3 "đóng kit", after trigger-wiring + adopt-poisoned. P065 (K-2): tarot's `setup-dev.sh` per-machine bootstrap → make it a sos-kit golden template. P066 (K-3, minor): standardize the dogfood-feedback filename/format (the feedback channel is load-bearing to the loop). Source: `docs/TAROT_ADOPTION_HANDOFF.md`.

**Harvest P060-P062 shipped (2026-06-09):**
- **P060** (F01): `sos adopt` now copies `docs/ORCHESTRATION.md` into adopted repos — the banner + `agents/orchestrator.md` reference the full spec, but adopt never installed it, so downstream orchestrators ran "blind handbook." Verified via temp-adopt (298-line spec lands).
- **P061** (F-002/F03, Sếp-decided **option b = track**): phiếu are a git-tracked audit trail (NOT gitignored). `sos adopt` seeds `.phieu-counter` (0) so `phieu <slug>` works; `agents/worker.md` gains a "You MUST track the phiếu file (`git add`, not rewrite)" rule. Fixes the dogfood gap where a built phiếu was never staged → `git mv active→done` failed + debate trail invisible to history.
- **P062** (F08): the `hooks/pre-commit` DOCS-GATE trigger row now explicitly flags that a SECTION add/remove changes the **phase count `[N/M]`** → update M everywhere (every label, the `# Runs in order` header, and prose "Phase N"/"N phases" in CLAUDE.md + ARCHITECTURE). Guidance not gate (§0.1 — comparing a count to prose claims isn't soundly mechanizable). Fixes the silent 3-phiếu "Phase 5 vs `[8/8]`" drift.
- P063 (Pattern-C docs-only-Architect doctrine) remains capture-only → retro v2.3.

**Downstream build-dogfood harvest — 3 fixes + capture (2026-06-09):**
- claude-hooks + doc-rotate ran the full workflow on real Rust builds (doc-rotate port RP01→RP07b COMPLETE) and logged 11 frictions. **Verdict: dogfood passed** — the workflow caught a char-vs-byte corruption-class bug pre-ship (F04) via Worker CHALLENGE + oracle-3-field. Shipped 3 cheap fixes: **F-001** `session-start-banner.sh` orchestrator-contract line was missing `touch worker-active` (so a forgetful Quản đốc got the Worker's first Write blocked by orchestrator-guard) → now states the full 2-way marker lifecycle; **F-003** `hooks/pre-commit` `[3/7]` hardcoded `docs/CHANGELOG.md` → false-positive "CHANGELOG not staged" warning every code+phiếu commit in root-CHANGELOG repos + disagreed with `[2/7]` docs-gate → grep now `^(docs/)?CHANGELOG\.md$`; **F02** `sos adopt` now seeds `.gitignore` with build-artifact + runtime-marker dirs (`target/`, `__pycache__/`, `node_modules/`, `dist/`, `*.egg-info/`, `.sos-state/`, `.backup/`, `.sos-adopt-incoming/`) — RP01 had left a 167MB untracked `target/`, and the runtime markers had to be hand-ignored downstream. Remaining 7 frictions captured as BACKLOG **[P060-P063]** (adopt installs no `ORCHESTRATION.md`; phiếu lifecycle bootstrap + track-or-ignore; DOCS-GATE hook phase-number trigger; Pattern-C docs-only-Architect doctrine → retro v2.3). F04 + pilot scoreboard → retro evidence.

**Background-spawn doctrine CORRECTION (2026-06-09):**
- Fixed a wrong claim in the background-spawn doctrine (orchestrator.md + ORCHESTRATION.md Rule 13): the first draft said backgrounding was *only* a per-spawn `run_in_background` param and implied a `background:` frontmatter field was non-functional. **WRONG** — verified via claude-code-guide against official docs (`code.claude.com/docs/en/sub-agents.md`): `background: true` IS a documented subagent frontmatter field that makes the agent run background **by default every spawn**. Three mechanisms: frontmatter default (the kit's choice — Sếp's rollout marking the 4 spawnable agents), per-spawn param, user instruction. Added the **load-bearing permission caveat** newly learned: background subagents run with pre-granted perms and **auto-deny any tool call that would prompt** → read-only specialists safe, but Worker EXECUTE backgrounded is auto-deny-on-prompt risk (safe only under bypass-perms/allowlist). Companion: Sếp's `background:true` frontmatter rollout on the 4 agents (separate commit). Mea culpa: the kit's own first doctrine draft was the error, not Sếp's mechanism.

**Rule 9 — contract-surface edits → CHALLENGE pass (2026-06-09):**
- Added `[judgment]` Rule 9 to `CLAUDE.md`: editing a contract surface (`agents/*.md`, `docs/ORCHESTRATION.md`/`LAYERS.md`/`HANDOFF.md`, `phieu/TICKET_TEMPLATE.md`) should get one Worker CHALLENGE / second-eye even on a tiny diff — the orchestrator's default is to self-edit doctrine solo because no gate forces a phiếu for docs (only code-on-default is gated), the `model-self-invoke = coin-flip` gap at the process layer. **Guidance not gate** (§0.1; a contract-surface diff-detector would over-block legit small edits). Curation (BACKLOG/CHANGELOG/findings) stays correctly single-role. Born from Sếp's observation this session that sos-kit work ran "toàn 1 vai." Self-consistent caveat: this CLAUDE.md edit IS a contract-surface change — committed solo because Chủ nhà's direct request was the approval (the escape the rule itself names).

**Orchestrator — background subagent spawns doctrine (2026-06-09):**
- Added "Background subagent spawns (`run_in_background`)" section to `agents/orchestrator.md` + Hard rule 13 in `docs/ORCHESTRATION.md`. Documents the native Claude Code async-spawn capability + the discipline the kit governs: marker exclusivity (≤1 guarded agent in flight — two Workers clash on `worker-active`), keep `worker-active` set until the completion notification (not right after spawn), APPROVAL_GATE still precedes EXECUTE when backgrounded, never poll. 3 anti-patterns + 2 failure-mode rows added. Propagated `orchestrator.md` → `~/claude-hooks` + `~/doc-rotate` (capability is native everywhere; only the doctrine needed copying).

**Housekeeping — CHANGELOG trim (2026-06-09):**
- Archived older changelog entries (v2.1.10 → v1) to `docs/archive/CHANGELOG_pre-v2.2.md`; live `CHANGELOG.md` back under the 40k doc-size threshold (53k → ~36k). Banner had flagged the overflow every session. Keeps v2.3 forge + the full v2.2 wave live; pointer left at the tail. Mirrors the `docs/archive/DISCOVERIES_pre-2026-05.md` precedent.

**Backlog capture — P056-P058 doctrine-harvest batch (2026-06-08):**
- Captured 3 net-new doctrine lessons from the ket dogfood WORKFLOW retro ("câu-hỏi-vàng soi vào chính nghi thức"): P056 pre-merge dogfood-gate for UI-surface phiếu (Sếp-decided), P057 verify-cò DoD rule (gate/hook phiếu must ship a fire-test), P058 scope-question as Tầng-1 phiếu field. Capture-only — all 3 are doctrine changes that must fold through retro v2.3, not ad-hoc edits (`CLAUDE.md` "Edit Workflow doctrine"). 3/5 ket lessons already covered by v2.2; net-new 3 are all surface-gated (anti completeness-bias at the process layer).
- **Captured POD-agent finding (2026-06-09) — adopt Loại 3 (constellation/fleet)** under Blocker C inbound brief. New brownfield sub-case beyond Loại 1/2: N discrete tools, mixed-stack, mixed-maturity, one business purpose — breaks the kit's implicit "1 repo = 1 product" axiom. Documents 3 doctrine requirements (multi-stack `.sos-stack.toml`, per-tool phiếu-stream, mixed-maturity tiered adoption) + POD-agent as candidate pilot. Capture-only (mold not ripe, §E). 
- **Folded tarot P336 evidence (2026-06-08) into P056 + P057** — cross-project n=2 for the "agent-blind-to-rendered-output" disease: 906 tests green, copy-reviewer caught an off-by-one in displayed free-session count (render layer outside test coverage; logic duplicated across 2 files → drift). P056 gains a subclass split (a) text/number → agent-reviewer-readable / (b) visual → human dogfood; P057 gains a sibling "kéo cò applies to displayed derived values → harden with one shared source + boundary-sweep test {3,2,1,0,≥quota}, not patch-both-sites". Still capture-only (retro v2.3 fold pending).



**P052 — Git-level `.env*` commit block pre-commit gate (2026-06-08):**
- Added `scripts/block-env-commit.sh` — agent-agnostic pre-commit gate that blocks staging
  any `.env*` secret file, allowing only `.env.example` (the template). Matches on **basename**
  across the full staged path so `config/.env.docker` is caught (the SEC-SECRET-01 incident shape).
- Backstop to the Claude-only PreToolUse `block-env-edit.sh` (P046) — this gate survives any
  agent (Codex, opencode, human `git add`). Both layers share the same regex `^\.env($|\.)` so
  they cannot drift. `.envrc` deliberately excluded (direnv config committed on purpose; blocking = false-positive).
- Wired into `hooks/pre-commit` as `[7/7]` (existing `[N/6]` labels relabeled `[N/7]`).
- Override (rare, intentional): `touch .sos-state/allow-env-commit`. No self-opt-out (sos-kit also must never commit a `.env*`).
- Docs updated: `CLAUDE.md` scripts list + DOCS GATE table; `docs/SETUP.md` hook section.

**P053 — Sentinel-vs-silent merge deadlock fix (2026-06-06):**
- **Bug fixed:** security-surface PR with clean APPROVE review was permanently deadlocked — `/security-review` silent-when-clean suppressed the comment, so `block-unsafe-merge.sh` never saw the required `Verdict: APPROVE` sentinel → merge blocked with no legitimate exit except override marker (`--no-verify`-death pattern).
- **Option A shipped (emit-side only):** `.claude/commands/security-review.md` Step 3 now partitions by mode: PR mode (block-unsafe-merge-governed) ALWAYS posts sentinel comment including clean APPROVE; branch/range (advisory) mode retains silent-when-clean (P042 anti-approve-fatigue intact). Step 4 PR-mode path explicitly covers both APPROVE and NEEDS_REVIEW with comment explaining why. Hard rules updated to reflect scoping.
- **`scripts/block-unsafe-merge.sh` UNTOUCHED** (one-disease-one-mechanism: only emit-side patched).
- **Known limitation documented (not fixed):** `block-unsafe-merge.sh:102-106` greps ANY historical `Verdict: APPROVE` comment on the PR with no head-SHA binding → stale APPROVE on commit A can green-light later unreviewed commits B+C on multi-commit PR. Documented in `security-review.md` Step 4 (mirror of block-unsafe-merge.sh:15-16 bypass pattern). SHA-scoping tracked separately = **[P055]** (docs/BACKLOG.md Open backlog).
- **Docs updated (Tầng 1 — security-surface):** `agents/boundary-check.md` silent-when-clean rule clarified (PR mode always-post vs advisory-only silent); `README.md:74` boundary-check contract line updated (PR mode sentinel always-post; silent-when-clean = ADVISORY/branch/range only).
- Sentinel string match proof: `<!-- security-review-start -->` / `Verdict: APPROVE` identical in all 3 files (boundary-check.md, security-review.md, block-unsafe-merge.sh); dry-run grep simulation confirms hook EXIT 0 on clean APPROVE comment.

**P050 — no-code-on-default-branch pre-commit gate (2026-06-06):**
- Added `scripts/no-code-on-default.sh` — agent-agnostic pre-commit gate that blocks product code
  (`.ts`/`.rs`/`.py`/`.go`/`.swift`/etc.) committed directly on the default branch, forcing a feature
  branch for code. Docs-only (`*.md`) commits on default remain allowed. Harvest from ket P020 live failure.
- Pattern derived from `.sos-stack.toml` `type`; absent stack falls back to full extension-union + BLOCK
  (greenfield is the primary harvest target). MERGE_HEAD escape allows PR-merges of feature branches.
- Wired into `hooks/pre-commit` as `[6/6]` (existing sections relabeled `[N/5]` → `[N/6]`).
- sos-kit self-opts-out via committed `.sos-state/sos-kit-self` marker + `.gitignore` negation.
- Override: `touch .sos-state/allow-code-on-default` (NOT `--no-verify`).
- Docs updated: `CLAUDE.md` scripts list + DOCS GATE table; `docs/SETUP.md` hook section.

Forge rounds 1–4 of `docs/retro/WORKFLOW_V2.3_RETRO_doc-rotate.md` (doc-rotate pilot vòng 2 retro). Doctrine still in forge — only mechanical/path fixes shipped here; doctrine questions Q-D2…Q-D7 (Lane/Tầng, markers, inject location, vocab-consistency, verify-setup + quality-canary split) remain OPEN.

**Two mechanical guards born from Két dogfood (2026-06-03) — turning "agent should remember" into "harness blocks":**
- **`orchestrator-guard.sh`** (PreToolUse Edit|Write) — Quản đốc (main session) việt-vị: edited `project.pbxproj` + wrote `relaunch.sh` *before* spawning Worker (caught by Sếp, reverted). Memory is passive (agent may not read it) → fix is a hook on the physical Write. Blocks product-source (`*.swift`/`*.pbxproj`/`src/**`) Edit/Write unless `.sos-state/worker-active` is set (Worker mid-EXECUTE). Companion-inverse of `architect-guard.sh` (Architect can't READ source; Quản đốc can't WRITE product). New marker `.sos-state/worker-active` — orchestrator `touch` before Worker spawn / `rm` after (Hard rule 6). Scope NARROW so kit-maintenance (`bin/`/`scripts/`/`docs/`/`*.md` anywhere/`*.py`/`*.sh`/`bootstrap/` Rust CLI allow-listed) stays editable; sos-kit self-applies (near-no-op there — no swift/pbxproj, no top-level src/, bootstrap/ excluded — but dogfoods). Doctrine: `agents/orchestrator.md` HR9 + ORCHESTRATION HR12 + LAYERS access-matrix enforcement note. Permission template `templates/claude-settings.local.json` updated with `worker-active` marker ops (else every Worker spawn prompts — P037 regression class).
- **`check-case-collision.sh`** (pre-commit `[5/5]`) — `Scripts/` (per PROJECT.md/phiếu) collided with the kit's `scripts/`: on macOS case-insensitive they merge (git index thrash), on Linux/CI they split into 2 dirs. `grep` case-sensitive misleads (1 match → "1 file"). Gate scans every path + dir-prefix (tracked + staged), lowercases, flags any key with >1 casing. Validated by discrimination test (mixed-case index → fires `scripts <-> Scripts`).
- Both ride the existing freeze copy (`sos new` already copies `scripts/` + `.claude/settings.json` + `hooks/pre-commit`) → every spawn armed automatically. Validated via living cases (block/allow per marker; allow kit-maintenance + docs).
- **Post-merge fix (2026-06-03) — `orchestrator-guard.sh` 2 bugs caught by RUN, not review:** (A) the `bootstrap/*` allow-list + `src/*` rule matched only RELATIVE paths, but Claude Code delivers ABSOLUTE `file_path` → the kit's own `bootstrap/sos-rs/src/*.rs` was BLOCKED from the main session (the "near-no-op / bootstrap excluded" claim was false at runtime; PR #21's own discrimination test used relative paths so it passed green). Fix: normalize to repo-root-relative before the anchored globs. (B) `NotebookEdit` was added to the matcher (PR #21 `bug_005`) but path extraction read only `file_path` — notebook payloads use `notebook_path` → extract-blind, always allowed. Fix: read both. Re-validated with 6 absolute-path living cases (bootstrap ABS allow, product `.swift` ABS block, `NotebookEdit` `.swift` block, `.md`-under-`src/` allow, worker-active allow, bootstrap REL allow). Lesson: a multi-agent cloud review found the symptom and shipped a fix whose test used the wrong input shape (relative vs absolute) — only running the hook with the real payload caught the residual.
- **Két dogfood harvest (2026-06-03) — `hooks/pre-commit` 2 template bugs the kit's docs-only self-dogfood is structurally blind to:** surfaced by the ket spawn (a real compilable Swift stack) and harvested UPSTREAM here — not just patched in the spawn, else the next spawn re-inherits (the `memory_dependent_sync` collapse mode). (1) **Discovery check matched only `## P<NNN>` headings** while sos-kit's OWN DISCOVERIES.md (and every spawn) ships a `| P<NNN> | … |` TABLE index → the grep never matched its own format; invisible because sos-kit rarely stages a new phiếu through that path, but ket creates phiếu rapidly and hit it (P004 table entry wrongly blocked → forced `--no-verify`). Fix: grep accepts heading OR table row, `([^0-9]|$)`-guarded (P004 ≠ P0040). (2) **Type-check fall-through was silent** (`echo` grey skip, no `WARN_COUNT`) for any non-{cargo,npm,python} stack → the most expensive gate looked armed but caught zero compile errors on Swift/Go/Java/C/Ruby. Fix: loud `yellow` warn + `WARN_COUNT` (proportionate — no per-commit build; real build/test belongs to `ship check` / DoD). Verified by living cases (table/heading/inline/false-positive grep; loud fall-through on sos-kit's own stackless tree). **Still open (needs Sếp sign-off — changes install contract for all spawns):** `install-hooks.sh` copies into `.git/hooks/` instead of `git config core.hooksPath hooks`, so a tracked-hook edit isn't live until re-install — which bit this very harvest (a stray `git stash` reverted the edit mid-test, the running copy stayed stale).
- **`core.hooksPath` switch (2026-06-03, Sếp-approved) — kill the copy-method drift class:** `install-hooks.sh` now runs `git config core.hooksPath hooks` instead of `cp` into `.git/hooks/` → the tracked hook IS the running hook (edit-is-live; no stale untracked copy running the OLD hook while the fix sits dead in `hooks/`, witnessed live in the harvest above). NOT a 1-liner — `core.hooksPath` redirects ALL hook lookups to `hooks/`, so two latent traps had to be cleared first or pre-push would have died silently: (1) **pre-push lived at `scripts/pre-push-hook.sh`** → `git mv` to `hooks/pre-push` (canonical name) so core.hooksPath finds it; (2) **`hooks/pre-commit` was tracked `100644` (non-exec) since 659e0e7** — the copy-method masked it (chmod'd the copy), but git silently skips a non-exec hook under core.hooksPath → `git update-index --chmod=+x`. `bin/sos.sh` now ships + adopts `hooks/pre-push`; `install-hooks.sh` retires stale `.git/hooks/{pre-commit,pre-push}` to `.bak`. Also reconciled a pre-existing 3-way install-method drift (install-hooks.sh copied / INSTALL.md said `core.hooksPath hooks` / SETUP.md said `core.hooksPath .githooks`) → unified on `core.hooksPath hooks`. Verified: `git rev-parse --git-path hooks/{pre-commit,pre-push}` both executable + resolved, stale copies retired.

**`sos new` now wires ALL our own Rust tools as MCP, not just doctor (Két dogfood, 2026-06-03):**
- Spawn `.mcp.json` only wired `doctor` — but `ship`/`guard`/`vps`/`docs-gate` are equally *our* tools (in-control, no credential, each exposes `serve` stdio MCP), distinct from external MCP (supabase/context7/canva/gdrive) which the kit rightly omits. Per the freeze-tier doctrine, our tools are the kit's "hands" → wire them. (Two earlier mis-claims corrected: `docs-gate` IS an MCP — `docs-gate serve`; `ship` is not deploy-only — it does test/commit/push/PR, universal to any repo.)
- Now wires `doctor` + `docs-gate` + `ship` + `guard` + `vps`, all PATH-relative `serve` (no hardcoded `~/.cargo`, Rule #3). `guard`/`vps` are deploy/server-scoped — inert on a local-only app (nothing to act on) but harmless (serve idles until called); the repo activates them by adding config when it gains a deploy/VPS target.

**Hook cwd-fragility fixed (Két dogfood, 2026-06-03) — guards were dying silently:**
- `.claude/settings.json` invoked hooks via bare relative paths (`bash scripts/X.sh`), and the scripts themselves read internal state via relative paths (`architect-guard.sh` → `.sos-state/architect-active`, `session-start-banner.sh` → `docs/BACKLOG.md`). Claude Code may fire a hook from a cwd ≠ repo root → bash can't find the script (`No such file or directory`, seen live in the Két session) OR the script runs but reads state relative to the wrong cwd → **fail-open**: `architect-guard` / `block-env-edit` / `block-unsafe-merge` silently stop guarding. sos-kit's own dogfood never caught it because that session always launches from repo root.
- Fix (both layers): settings.json now uses `bash "$CLAUDE_PROJECT_DIR/scripts/X.sh"`; each hook script `cd`s to `${CLAUDE_PROJECT_DIR:-<script-dir>/..}` at the top so internal relative refs bind to the project regardless of caller cwd. Validated with the living repro (run from `/tmp` + subdir, both env-var and BASH_SOURCE-fallback paths: guard blocks `src/`, allows `.md`, banner reads the right BACKLOG).

**`sos new` spawn regression fixed (Két greenfield dogfood, 2026-06-03):**
- **Dangling agent symlinks (blocker).** The agents-drift cure (`3334a3a`) made `.claude/agents/*.md` symlinks → `../../agents/*.md`. `sos new` copied them with `cp -R` (preserves symlinks) but a spawned repo has no top-level `agents/` target → 4 of 5 subagents (architect/worker/advisory-watch/boundary-check) shipped as **dangling symlinks**, unreadable. `verify-setup` flagged it as `[BROKEN] J6 verdict-contract` → boundary-check DORMANT → `sos new`'s own acceptance test (fresh spawn → CONNECTED) failed. Fix: `cp -R` → `cp -RL` (dereference to real files) for `.claude/agents` + `.claude/commands`.
- **No `.gitignore` in spawn.** Golden ships one; `sos new` never copied it → spawned repo committed `.DS_Store` cruft on first commit. Fix: copy `.gitignore` in Category A.
- **Hooks never armed.** `git init` (user step) left `.git/hooks/` empty; nothing ran `install-hooks.sh` → the bootstrap commit (with the dangling symlinks + cruft) sailed through with the gate **silent** (enforce-via-mechanism violation). Fix: `sos new` now `git init -b main` + `install-hooks.sh` at the end (born-wired); the user's bootstrap commit then passes *through* the freshly-armed gate.
- **CLAUDE.md skeleton didn't point at the PRD.** Generated `CLAUDE.md` hardcoded the `--stack` value (lies when a non-`python/rust/ts` app borrows a placeholder) and never referenced `docs/PROJECT.md` → a spawned repo's auto-loaded context never taught agents the PRD exists. Fix: skeleton now always carries a `docs/PROJECT.md = PRD, read it first` pointer (wired by convention even before the file exists) + softens the stack line to a flagged-placeholder TODO.
- Validated end-to-end on a throwaway `/tmp` spawn: `verify-setup: CONNECTED`, agents readable, `.DS_Store` tracked count = 0, gated bootstrap commit passes clean.
- **Post-review hardening (ultrareview PR #20, 3 findings — all real):** (1) `git init -b main` needs git ≥ 2.28 → under `set -euo pipefail` it aborted `sos new` mid-bootstrap on older fleet hosts (Ubuntu 20.04 = git 2.25) with no `.git`/hooks/recovery; replaced with `git init && git symbolic-ref HEAD refs/heads/main` (works on every git). (2) Rust spawn was **blocked** by its own armed gate — the skeleton `Cargo.toml` + empty `src/` made `cargo check` fail "no targets" (the initial "4/4 clean" check only exercised python/ts, both of which *skip*, and the rust validation had deleted `Cargo.toml`); now ships a `src/main.rs` stub = valid crate. (3) `sos new --force` into a pre-existing `.git/` only *printed* a hint instead of arming hooks (memory-dependent, contradicts the thesis) → now calls idempotent `install-hooks.sh` in the else branch too.
- **Still OPEN (no `--stack swift`):** `sos new` only knows `python|rust|ts`; a Swift/macOS app (Két) must pick a placeholder stack + delete the bogus manifest, and `.sos-stack.toml` then lies (`rust` with no `Cargo.toml`). Feeds next retro — adopt-mode / stack-coverage harvest.

**Phiếu path unified to `docs/ticket/` (Q-D1 resolved):**
- Migrated sos-kit's own phiếu `phieu/active|done/` → `docs/ticket/` (active root) + `docs/ticket/done/` (14 phiếu archive). No more dogfood-exception — single path matches the fleet default (downstream repos already use `docs/ticket/`).
- `hooks/pre-commit` now **reads `ticket_dir` from `.docs-gate.toml`** instead of hardcoding `^docs/ticket/` — revives the Discovery-enforcement gate (was structurally dead on phieu/ repos) and makes it portable per-repo (single-source principle).
- Swept: `.docs-gate.toml` (`ticket_dir = "docs/ticket"`), `scripts/session-start-banner.sh`, `phieu/phieu.sh` (location-detect), `phieu/TICKET_TEMPLATE.md`, `agents/worker.md` (×2), `CLAUDE.md` structure, `docs/BOOTSTRAP_AUTOMATION_DRAFT.md` default, `docs/ORCHESTRATION.md`.
- Preserved: historical phiếu content + this CHANGELOG (no rewrite history); `phieu/` backbone (TICKET_TEMPLATE/phieu.sh/protocols/VISION_TEMPLATES) retained as the workflow component; legacy `phieu/active` fallback kept in `phieu.sh` + banner for older repos.

**Sentinel mismatch fixed (Step-1):**
- `scripts/block-unsafe-merge.sh` grepped `<!-- SECURITY_REVIEW_START -->` (UPPERCASE/underscore) while `agents/boundary-check.md` + `/security-review` emit `<!-- security-review-start -->` (lowercase/hyphen) — the merge-gate verdict block never matched, breaking the advisory→merge-block chain at its root. Fixed grep to match the emitted marker.

Verified: gate canary fires on `docs/ticket/`; `bash -n` clean on edited scripts. Open production-risk tracked in retro: tarot's Giám sát (boundary-check v166) runs live but has never been quality-canaried.

**Found — fix DEFERRED to next session (in-repo `.claude/agents/` drift, 2026-05-30):**
`scripts/sync-personal-agents.sh` (regen `.claude/agents/` from canonical `agents/`) is a **memory-dependent** sync step that **died ~1 month** → the run/propagated copy went stale → a major cause of the media collapse. = Sub-mech A (trigger-gap) living inside the kit. **Symptom patched** (re-ran sync, `796feb2`); **structural cure deferred** — it's next session's **TOP task, before the tarot→sos-kit sync**. Fix-direction: automate (pre-commit regen) / attach-to-an-action / or eliminate the 2-copy (SOUL: is the `agents/`-Chủ-nhà vs `.claude/agents/`-Sếp split worth a death-point?). Tracked: BACKLOG "🔝 IN-REPO drift" + `GAP_AUDIT_tarot_to_soskit.md`.

**Tầng = consequence (LOC removed) + un-staled `.claude/agents/` (2026-05-30):**
- **Removed the "≤3 files / ≤200 LOC" Tầng criterion from ALL live doctrine** — a small-LOC security fix is still Tầng-1; LOC mis-classifies (the exact bug that collapsed the media pilot). Tầng is now **single-sourced in `docs/LAYERS.md` §2-tier** by CONSEQUENCE (mistake **LAN** or **NOT-reversible** → Tầng-1; security/auth/schema/privacy/payment/`INV-LOCAL` → AUTO Tầng-1 even if 1 line; size is not a signal). `agents/orchestrator.md` (now **reads** the field, does NOT re-judge), `agents/architect.md`, `docs/ORCHESTRATION.md`, `phieu/TICKET_TEMPLATE.md`, `phieu/DISCOVERY_PROTOCOL.md` reference LAYERS.md instead of restating (closes the two-voice drift).
- **Un-staled `.claude/agents/`:** the generated copy (architect/worker) was last synced **2026-04-26 — ~1 month stale**, missing all v2.2 evolution (Oracle/AGENT_MAP/Humility/Bước-0). sos-kit **dogfooded + adopt propagated the stale April agents** — a major cause of the media collapse (media got month-old architect/worker). Re-ran `scripts/sync-personal-agents.sh` → `.claude/agents/` now == canonical (architect 191→276, worker 183→293). Same Sub-mech A: the regen mechanism existed but was never fired. **Flag (not built):** sync script covers only architect+worker; a pre-commit drift-check (`.claude/agents/` == `agents/` name-swapped) would prevent re-staleness.

**In-repo `.claude/agents/` drift CURED — eliminate via symlink + tên-vai/xưng-hô split (closes the deferred item above) — 2026-06-01:**
The 2026-05-30 deferred TOP task is resolved. Root finding: the `sed 's/Chủ nhà/Sếp/g'` swap was a **category error** — `Chủ nhà` is a **role name** (tên vai), `Sếp` an **address term** (xưng hô); grep confirmed 100% of handbook `Chủ nhà` usage is 3rd-person role-reference, zero vocative. The two-copy split personalized nothing the conversation layer doesn't already handle, so it was pure drift surface.
- **Cure = eliminate (not automate):** `.claude/agents/{architect,worker,advisory-watch,boundary-check}.md` are now **symlinks** → `../../agents/*.md`. One real file per agent → drift is structurally impossible (no second copy to diverge). Removed `scripts/sync-personal-agents.sh` + its SessionStart hook. Stronger than the "automate via hook" option first considered: a symlink has no sync step to fire at all.
- Cleaned stray `Sếp` referents → `Chủ nhà` in `agents/orchestrator.md` (×5) + `agents/worker.md` (×5); canonical handbooks are now pure role-name voice.
- Documented the **tên-vai vs xưng-hô** two-layer rule in `CLAUDE.md` (Language) + `agents/README.md`. The `Sếp`/`anh`/`em` register now lives only in live chat + UI (e.g. the SessionStart banner), never in handbook role-references.
- External adopters copy real files from `agents/` (per `INSTALL.md`), so the symlinks are sos-kit-internal and don't affect downstream copies.

**Install completeness — root-cause fix for the media-class gap (2026-06-02):**
The drift cure surfaced that `INSTALL.md` told adopters to copy only `architect.md` + `worker.md` — **not** `advisory-watch.md` + `boundary-check.md`. That is the concrete root cause media-rating had **no Giám sát / Trinh sát agent** (a major collapse factor): every adopter inherited the gap. Compounding it: copying the (now-cured) `settings.json` pulls in `block-env-edit`/`block-unsafe-merge` PreToolUse wiring whose scripts the install never copied → hooks pointing at missing scripts.
- `INSTALL.md` now copies **all 4 spawnable agents** + the `/security-review` + `/advisory-scan` commands + the `block-*` guard scripts; the merge-example shows all 3 PreToolUse matchers (with a note that `block-unsafe-merge` is the no-merge-without-Giám-sát backstop).
- Stale `sync-personal-agents.sh` reference removed from `CLAUDE.md` structure listing (script was deleted by the symlink cure). Closes BACKLOG "🔝 IN-REPO drift".

**Top-3 trigger-doctrine sync (gap-audit → kit, 2026-05-30):**
Closes the dominant gap-audit finding — the kit shipped GATES without the DOCTRINE/TRIGGER that fires them (Sub-mech A re-created inside the kit; what made the media pilot collapse).
- `docs/ORCHESTRATION.md` **Rule 10** (pre-merge security gate — `gh pr diff --name-only` before any merge; security surface → `/security-review` `APPROVE` required; `block-unsafe-merge.sh` = mechanical backstop) + **Rule 11** (advisory staleness auto-spawn). Condensed mirror in `agents/orchestrator.md`.
- `scripts/session-start-banner.sh` — advisory staleness check (reads `docs/security/.advisory-scan-state`; 🚨 ≥7d / ⚠️ 3–6d; cross-platform date GNU+BSD; gated on inbox presence so non-advisory repos stay silent) → Trinh sát now fires unprompted.
- `agents/architect.md` **Bước 0** — Layer-1 tool-capability verify (WebFetch GET-only, etc.); pairs with the already-present Worker Task-0 Layer-2 = the "ship≠chạy" 2-layer defense.
- NOT synced (deliberate): tarot product-specifics; sos-kit stays AHEAD on v2.2 (lane/sensor/rubric/multi-stack/INV-4). **Next:** the orchestrator/architect Tầng "≤200 LOC" contamination → single-source to `LAYERS.md` consequence-def (see `GAP_AUDIT_tarot_to_soskit.md` + retro Vòng 14).

**Gap-audit tarot→sos-kit + spawn-discretion findings (2026-05-29):**
- `docs/GAP_AUDIT_tarot_to_soskit.md` — the kit lagged its flagship (tarot). Read-only inventory of GENERIC-doctrine gaps. **Dominant disease:** kit ships the GATE but drops the DOCTRINE that fires it = Sub-mech A "trigger gap" re-created inside the kit (what made the media pilot collapse). **Top-3:** advisory auto-spawn (Rule 10 + banner staleness), pre-merge security-gate doctrine (Rule 9), Architect Layer-1 capability check. **Direction (Sếp):** tarot FROZEN — fix kit→media/new-projects as proving-ground; back-port to tarot later. sos-kit is AHEAD on v2.2 (lane/sensor/rubric/multi-stack/INV-4) — do NOT sync down.
- `docs/retro/WORKFLOW_V2.3_RETRO_doc-rotate.md` Vòng 13/14: spawn-discretion bug (mechanical proof — doc-rotate 23 spawns vs media 0, same delegation prompt) + corrected fix-direction (sound/partial: Tầng = human judgment via INV-LOCAL/INVARIANTS membership; hooks enforce field-existence + role-ran state-check, NEVER hook-detect-security; closes Q-D2 seedling-disease).

**Bootstrap Cat-A freeze completed — skills + doctor MCP wiring (2026-05-29):**
- `bin/sos.sh` `sos new` + `sos adopt` now copy the **13 generic SOS skills** (`skills/` → `.claude/skills/`) and wire **OUR `doctor` MCP gate** in `.mcp.json` (PATH-rel `doctor`, no hardcoded path). Closes a real freeze gap found dogfooding media-rating: adopt copied the role agents but not the skills they invoke (`/plan` `/verify` …) nor the `.mcp.json` that wires Giám sát's `mcp__doctor__*` tools → both were dead in the adopted repo.
- `adopt` is ADDITIVE/non-clobber: keeps a repo's own domain skills (Cat-C) + its own external MCP (`.mcp.json` flagged, never overwritten), adds only the generic spine.
- Doctrine (`docs/BOOTSTRAP_AUTOMATION_DRAFT.md` §7.2): freeze-tier is an **attribute filter** (same-across-repo + in-control = freeze; per-repo = survey/declare; external-dep = drop), not an item-type list. Skills split generic-freeze / domain-Cat-C; MCP split by ownership (our tools wired, external dropped — *LLM reasons, our tools are the hands*). The deeper per-repo survey step (same root as the map-lie gap) is tracked in BACKLOG, not built.
- Tested green: greenfield `sos new` (skills=13, `.mcp.json` valid PATH-rel, verify-setup CONNECTED) + 2 brownfield `sos adopt` (own skill/MCP kept, generic added, non-clobber confirmed).

## v2.2 Backport Group A — Security hooks + agents + commands — 2026-05-28

Phase 2 of `WORKFLOW_V2.2.md` §13 backport from tarot evolution (P230/P273/P297/P305/P306/#581). Genericized for sos-kit template.

**Hooks + scripts (7 new + 2 modified):**
- `scripts/block-env-edit.sh` — PreToolUse block Edit/Write to `.env*` (allow `.env.example`)
- `scripts/block-unsafe-merge.sh` — PreToolUse block `gh pr merge <N>` if security surface + no `/security-review` APPROVE sentinel (§7 Sub-mech A + §8)
- `scripts/security-gate.sh` — minimal template (INV-009 + INV-010 universal); per-repo extends
- `scripts/check-hardcoded-secrets.py` — INV-009 enforcer (8 prefix patterns + generic high-entropy)
- `scripts/check-runtime-secrets.py` — INV-010 enforcer Sub-mech F (dotfile token leak: `.git/config`, `.mcp.json`, `.claude/settings.local.json`, infra)
- `scripts/install-hooks.sh` — bootstrap pre-commit + pre-push installation
- `scripts/pre-push-hook.sh` — advisory warn (KHÔNG block) for security-surface push
- `hooks/pre-commit` — add SECTION 4 wiring `security-gate.sh --mechanical-only`
- `.claude/settings.json` — merge 3 PreToolUse hooks

**Agents + commands (4 changed):**
- `.claude/agents/boundary-check.md` (NEW snapshot) + `agents/boundary-check.md` — wire `mcp__doctor__runtime_scan` + `mcp__doctor__validate_map` per tarot #581
- `.claude/agents/advisory-watch.md` (NEW snapshot from `agents/`)
- `.claude/commands/advisory-scan.md` — rewrite to advisory-inbox binary form (P013-tarot)

**Tracked `.mcp.json`:** Doctor MCP server registration (binary serve mode for `lane-check`, `validate-map`, `rotate-check`, `runtime-scan`).

**Out of scope (Phase 3):** CLAUDE.md doctrine refactor + worker.md Task 0 matrix + architect.md §2 oracle checklist + `AGENT_MAP.example.yaml`.

**Verification:** `security-gate.sh --mechanical-only` on sos-kit itself → 2/2 INV PASS, 12 files scanned.

## Sprint close: Tarot port wave 1 — 2026-05-25

All 4 phiếu complete: P040 (stack-detect `sos init security`) + P041 (Trinh sát advisory-watch) + P042 (Giám sát boundary-check) + P043 (Quản đốc persona codify). Security pipeline both sides shipped: `/advisory-scan` (external CVE/GHSA scan via Trinh sát) + `/security-review` (internal invariant scan via Giám sát). Kit now ships: stack detection → advisory scan → boundary check → ADVISORY PR comment flow. CHANGELOG range: v2.2.0 → v2.2.3.

## [v2.2.3] — 2026-05-25

### Added
- **P042: Giám sát (boundary-check) specialist subagent — generic port from tarot, strips 7 INV → 5 generic (drop tarot's nginx + users.credits).** Adds `agents/boundary-check.md` (read-only-output specialist, tools: Read/Grep/Glob/Bash-scoped-to-git-and-grep), `templates/INVARIANTS-template.md` (5-INV skeleton + user-added section), `.claude/commands/security-review.md` (orchestrator-side spawn-only caller, posts PR comment in ADVISORY mode — KHÔNG block merge). 5 generic INV: env var template / external service timeout / cross-user binding / webhook signature / dep major changelog audit. Sentinel markers: `<!-- security-review-start -->` / `<!-- security-review-end -->`. Silent-when-clean rule preserved from tarot P275 lesson. `docs/LAYERS.md` Giám sát column filled. `docs/HANDOFF.md` stub expanded to full handoff entry. `README.md` boundary-check subagent row + Security section extended. `docs/SETUP.md` Security pipeline Step 5 added. `CLAUDE.md` repo tree updated. Wave 1 final phiếu shipped — P040+P041+P042+P043 complete.

### Files changed
- New: `agents/boundary-check.md`, `templates/INVARIANTS-template.md`, `.claude/commands/security-review.md`
- Modified: `docs/LAYERS.md`, `docs/HANDOFF.md`, `README.md`, `docs/SETUP.md`, `CLAUDE.md`, `CHANGELOG.md`, `docs/DISCOVERIES.md`

## [v2.2.2] — 2026-05-25

### Added
- **P041: Trinh sát (advisory-watch) specialist subagent — generic port from tarot, strips tarot-specific paths.** Adds `agents/advisory-watch.md` (read-only-output specialist, tools: Read/Grep/Glob/WebFetch/WebSearch/Bash-scoped-to-parsers), `templates/advisory-inbox.md` (queue with sentinel wrappers `<!-- advisory-start --> / <!-- advisory-end -->`), `.claude/commands/advisory-scan.md` (orchestrator-side spawn-only slash command — first file in `.claude/commands/`). Implements `scripts/parsers/pnpm_lock_v9.py` (PyYAML, 2-level importers layout, peer-suffix strip) + `scripts/parsers/package_lock_v3.py` (JSON flat layout, v2/v3 compatible). Other 4 parsers stay P040 stubs. `docs/LAYERS.md` Specialist subagents subsection. `docs/HANDOFF.md` appendix for specialist-subagent pattern. `README.md` advisory-watch subagent row + Security pipeline mention. `docs/SETUP.md` Security pipeline subsection (PyYAML install + sos init security + /advisory-scan flow). `CLAUDE.md` repo tree updated. State persistence + vendor-page expansion deferred to follow-on phiếu.

### Files changed
- New: `agents/advisory-watch.md`, `templates/advisory-inbox.md`, `.claude/commands/advisory-scan.md`, `docs/discoveries/P041.md`
- Modified: `scripts/parsers/pnpm_lock_v9.py`, `scripts/parsers/package_lock_v3.py`, `docs/LAYERS.md`, `docs/HANDOFF.md`, `README.md`, `docs/SETUP.md`, `CLAUDE.md`, `CHANGELOG.md`, `docs/DISCOVERIES.md`

## [v2.2.1] — 2026-05-25

### Changed
- **P043: Doc drift consolidate — Quản đốc persona codify + alignment engineering + deferred-tool loading.** Main-session orchestrator persona renamed from "Kiến trúc sư" → "Quản đốc" across all operational docs (disambiguates from Kiến trúc sư subagent). Layer 0 Quản đốc row added to `docs/LAYERS.md` access matrix + ASCII diagram. `docs/PHILOSOPHY.md` "alignment engineering" subsection expanded with 3 sub-headings (How envelopes are enforced / Why "share context" is the trap / Why role separation not just prompt discipline). `docs/ORCHESTRATION.md` greeting script + edge case updated; "Why Quản đốc persona" subsection rewritten + 3 new subsections added (Greeting turn template / Tier priority routing rationale / Session opening script). Deferred-tool loading instruction added to `agents/orchestrator.md` + `CLAUDE.md`. `CLAUDE.md:149` orchestrator handbook cap raised ≤90 → ≤105 (Sếp decision 2026-05-25). Cross-ref pass: README.md + CLAUDE.md + HANDOFF.md updated. Dangling staged files (agents/orchestrator.md + docs/BACKLOG.md) committed atomically.

### Files changed
- Modified: `docs/LAYERS.md`, `docs/PHILOSOPHY.md`, `docs/ORCHESTRATION.md`, `agents/orchestrator.md`, `CLAUDE.md`, `README.md`, `docs/HANDOFF.md`, `docs/BACKLOG.md`, `docs/DISCOVERIES.md`
- New: `docs/discoveries/P043.md`

## [v2.2.0] — 2026-05-25

### Added
- **P040: Bootstrap stack detection — `sos init security` subcommand** auto-detects Node/Python/Rust/Go via manifest files (`package.json`, `pyproject.toml`, `requirements.txt`, `Cargo.toml`, `go.mod`) and lock files, writes `.sos-stack.toml` schema at project root. Adds 6 parser skeleton stubs at `scripts/parsers/` (all return `[]`; P041 fills implementations). Foundation for advisory-scan (P041) + security-review (P042). Schema: `schema_version`, `detected_at`, `sos_kit_version`, `[[stack]]` with `type`/`manifest`/`lock_file`/`lock_format`/`parser`. `sos init` (no args) Phase 0 behavior unchanged. `sos help` updated.
- `templates/.sos-stack.toml.example` — example schema for inspection/manual authoring.
- `.gitignore` — `__pycache__/` + `*.pyc` (P040 adds first Python files to repo).

### Files changed
- New: `scripts/parsers/pnpm_lock_v9.py`, `scripts/parsers/package_lock_v3.py`, `scripts/parsers/requirements_txt.py`, `scripts/parsers/pyproject_toml.py`, `scripts/parsers/cargo_lock.py`, `scripts/parsers/go_sum.py`, `templates/.sos-stack.toml.example`, `docs/discoveries/P040.md`
- Modified: `bin/sos.sh`, `docs/SETUP.md`, `README.md`, `.gitignore`, `CHANGELOG.md`

## [v2.1.x → v1] — archived

Older entries (v2.1.10 down to v1, 2026-04-26 → earlier) moved to **`docs/archive/CHANGELOG_pre-v2.2.md`** on 2026-06-09 to keep this file under the 40k doc-size threshold. See that file for the full v2.1 wave, v2.0, and v1 history.
