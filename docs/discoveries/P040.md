# P040 Discovery Report — Bootstrap stack detection (`sos init security`)

**Date:** 2026-05-25
**Worker:** Thợ (Sonnet 4.6)
**Phiếu version at EXECUTE:** V2 (underscore filenames — Sếp-decided)

---

## Assumptions in phiếu — CORRECT

- `bin/sos.sh` dispatcher at line 319-333 with `init` case (Anchor #1). Verified at EXECUTE, no drift.
- `sos_init()` at line 78-100 does not case-split on args (Anchor #2). Confirmed — simple function body, safe to add dispatcher at top.
- `scripts/parsers/` does not exist (Anchor #3). Confirmed empty — directory was greenfield.
- `templates/` has exactly 2 files (Anchor #4). Confirmed: `BACKLOG_template.md` + `claude-settings.local.json`.
- No `.sos-stack.toml` anywhere in repo (Anchor #5). Confirmed.
- `date -u +%Y-%m-%dT%H:%M:%SZ` idiom already in use 5 places in bin/sos.sh (Anchor #10). Confirmed at lines 51, 52, 67, 170, 302.

## Assumptions in phiếu — WRONG

- None. All anchors held at EXECUTE.

## Scope expansions

1. **`.gitignore` entry for `__pycache__/` + `*.pyc`** — P040 introduces first Python files to the repo. When importing parser stubs during validate step, Python 3.14 auto-generated `scripts/parsers/__pycache__/`. Added `__pycache__/` + `*.pyc` to `.gitignore` (Tầng 2 self-decide — standard Python hygiene, no schema/API impact, no P041/P042 consumer impact). Logged here.

## Schema final shape

Worker kept all keys exactly as specified. Final `.sos-stack.toml` shape at EXECUTE:
- Top-level: `schema_version`, `detected_at`, `sos_kit_version`
- Per-stack: `[[stack]]` with `type` / `manifest` / `lock_file` / `lock_format` / `parser`

No key renamed, no key added, no key removed. P041 + P042 consumers can read exactly as specified.

## Parser filename convention

DECIDED V2: underscores. All 6 files landed as:
- `scripts/parsers/pnpm_lock_v9.py`
- `scripts/parsers/package_lock_v3.py`
- `scripts/parsers/requirements_txt.py`
- `scripts/parsers/pyproject_toml.py`
- `scripts/parsers/cargo_lock.py`
- `scripts/parsers/go_sum.py`

`bin/sos.sh` `parser = "scripts/parsers/<name>.py"` literals match. `templates/.sos-stack.toml.example` paths match. No drift.

## Anchor #8 (README) resolution

README had no subcommand table for `sos`. Worker found 2 insertion points:

1. **Row in Rust CLI Tools table** (line 68): updated sos row to include `sos init security` in the subcommand list. Text: `0→1 bootstrap — \`sos init\` / \`sos init security\` / \`blueprint\` / \`contract\` / \`apply\` / \`launch\`.`

2. **Prose sentence in Genesis section** (after line 194): added 2-sentence paragraph after the `sos contract` / P000-genesis.md blurb. Text: `After \`sos init\`, optionally run \`sos init security\` to bootstrap stack detection for the advisory-scan + security-review subagents (introduced in P040; consumed by P041 + P042). This writes \`.sos-stack.toml\` at the project root — a machine-readable manifest of which lock files and parser stubs to use per ecosystem.`

Both insertions are discoverable in the user flow (table for scanning, prose for reading).

## Detection priority order at EXECUTE

Priority order `package.json → pyproject.toml → requirements.txt → Cargo.toml → go.mod` felt correct. Multi-stack edge case (Python project with BOTH `pyproject.toml` AND `requirements.txt`) was detected as 2 separate `[[stack]]` entries — verified via manual dry-run. This matches phiếu spec: "detect all manifests, write 1 entry per stack." No ambiguity arose; the current approach is correct for the P041 consumer (it reads each `[[stack]]` entry independently to invoke the right parser). P041 may want to deduplicate if both point to the same ecosystem — flag for P041 author.

## Idempotency check feeling

Exit 1 on existing `.sos-stack.toml` feels correct for P040. The check is intentional: a hand-edited multi-stack TOML (e.g. user added custom `[[stack]]`) should not be silently overwritten. A `--force` or `--refresh` flag for "re-detect + merge" could be useful in P041+ if stacks change mid-project, but that's out of P040 scope. Current UX (delete + re-run) is low friction enough for a bootstrap-once command.

## CHALLENGE round value

Yes, the CHALLENGE round was valuable. The V1 phiếu had an inconsistency: parser filenames were described as dashes in the text but "Worker decision" note was vague about the P041 import compatibility constraint. The CHALLENGE Turn 1 [O1.1] surfaced this as a Tầng 1 issue (P041 will `import` these files — dashes break Python import syntax). Sếp resolved it out-of-band, phiếu was bumped to V2 with Constraint #8 explicitly load-bearing. Without the CHALLENGE round, Worker would have had to stop mid-EXECUTE to escalate, or worse, shipped with dashes and broken P041.

## Total time estimate

Architect estimate: half-day. Actual: sub-1-hour. Tasks were surgical and well-specified — phiếu V2 gave exact code blocks to insert. Tầng 1 dogfood confirms: well-specified phiếu with exact `Thay bằng` blocks + debate-resolved constraints = Worker executes fast with zero re-work loops.
