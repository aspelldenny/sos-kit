# Discovery Report — P082: lane-field template drift fix (OA-01)

## Anchor verification (Task 0, re-run in EXECUTE mode)

| # | Assumption | Result | Evidence |
|---|-----------|--------|----------|
| 1 | Orchestrator pre-CHALLENGE gate calls `doctor lane-check --ticket` | ✅ CORRECT | `agents/orchestrator.md:56` — `doctor lane-check --ticket docs/ticket/P<NNN>-<slug>.md` |
| 2 | Template header had `**Tầng:**` but no `**Lane:**` | ✅ CORRECT | `phieu/TICKET_TEMPLATE.md:12` (pre-fix); `grep '**Lane:**'` = 0 hits pre-fix |
| 3 | `phieu.sh` copies template verbatim, rewrites only line 1 | ✅ CORRECT | `phieu/phieu.sh:118-123` (`cp "$template"` + `sed -i.bak "1s|.*|..."`). Note: propagation to consumer repos happens via the documented copy step (`docs/SETUP.md`, `phieu/README.md`) — sos-kit itself has no local `docs/ticket/TICKET_TEMPLATE.md` mirror, master lives at `phieu/TICKET_TEMPLATE.md`. |
| 4 | `doctor lane-check` exit 2 = missing/unparseable Lane field | ✅ CORRECT | Oracle run pre-fix: `doctor lane-check --ticket phieu/TICKET_TEMPLATE.md` → `error: ticket missing Lane field (expected **Lane:** Normal|Guarded|Fast)`, exit 2 |
| 5 | Template stays under Normal budget (250 lines/5 anchors/5 constraints) after adding Lane field | ✅ CORRECT | Oracle run post-fix: `doctor lane-check --ticket phieu/TICKET_TEMPLATE.md` → `OK (Normal — 181 lines, 3 anchors, 2 constraints)`, exit 0. Verified twice: once via temp-copy dry run in CHALLENGE mode, once on the real fixed file in EXECUTE mode. |

**Anchor #4/#5 — Claim/Oracle/Soundness (§2 3-field, both self-closed via oracle in CHALLENGE mode):**
- #4 — Claim: "exit 2 fires on missing/unparseable Lane field." Oracle: `doctor lane-check` binary itself (SOUND — direct exit-code observation, not inference). Verdict: self-closed.
- #5 — Claim: "template + Lane line stays within Normal budget." Oracle: `doctor lane-check` count output (SOUND — same binary reports exact line/anchor/constraint counts). Verdict: self-closed.

## D1/D2 decision outcome (post-CHALLENGE, no objections raised — Worker accepted V1)

- **D1 (Lane value = `Normal` bare token):** CONFIRMED. Oracle proved budget-fit (181/250, 3/5, 2/5) — no need to consider Guarded. Architect's defend (Guarded-born-default defeats gate) holds unchallenged.
- **D2 (contract test placement = sub-check `3f` inside pre-commit `[3/8]`):** CONFIRMED. `hooks/pre-commit` section `[3/8]` spans lines 124-207 (pre-P082); sub-check `3e` block ends right before the closing `echo ""` at line ~200-201 (before `[4/8]` begins). Insertion point matched phiếu's citation exactly. Phase count unchanged — still `[8/8]`.

## Fallback F1/F2

**NOT activated.** Anchor #5 oracle confirmed exit 0 (well under budget) both pre-execution (CHALLENGE dry-run on temp copy) and post-execution (real file). No example-row trim (F1) or Chủ nhà escalation (F2) needed.

## Nghiệm thu — real command output

```
$ bash -n scripts/lane-check-contract.sh && bash -n hooks/pre-commit
syntax OK (both)

$ doctor lane-check --ticket phieu/TICKET_TEMPLATE.md; echo $?
OK (Normal — 181 lines, 3 anchors, 2 constraints)
0

$ doctor lane-check --ticket docs/ticket/P082-lane-field-template.md; echo $?
OK (Normal — 243 lines, 5 anchors, 5 constraints)
0

$ bash scripts/lane-check-contract.sh   # doctor present
✅ template Lane field parseable + within budget (phieu/TICKET_TEMPLATE.md): OK (Normal — 181 lines, 3 anchors, 2 constraints)
0

$ env PATH="/usr/bin:/bin" bash scripts/lane-check-contract.sh   # doctor absent (both ~/.cargo/bin and ~/.local/bin stripped)
⚠️  doctor absent — lane contract SKIPPED
0

# Regression test (temp-swapped copy, restored immediately after — no permanent file left stripped):
# stripped **Lane:** line from phieu/TICKET_TEMPLATE.md, ran contract script:
❌ template missing/unparseable Lane field — OA-01 regression (phieu/TICKET_TEMPLATE.md): error: ticket missing Lane field (expected **Lane:** Normal|Guarded|Fast)
1
# restored real file immediately after; re-verified doctor lane-check exit 0 + git diff --stat shows only the intended 1-line Lane addition remains.
```

## Tầng-1 docs updated

- `phieu/README.md` — added "Header fields — Tầng vs Lane (2 independent axes)" section (Task 4a)
- `docs/HANDOFF.md` Handoff 2 — header field list now includes Tầng, Lane (Task 4b)
- `CLAUDE.md` — scripts list entry for `lane-check-contract.sh` + hook-chain note on `hooks/pre-commit` line (Task 4c)
- `docs/SETUP.md` — new `[3/8]` sub-check `3f` subsection describing `lane-check-contract.sh` exit-code contract (Task 4d)
- `CHANGELOG.md` — P082 entry added

## Scope

No scope expansion. Files touched match phiếu's "Files cần sửa" list exactly: `phieu/TICKET_TEMPLATE.md`, `scripts/lane-check-contract.sh` (new), `hooks/pre-commit`, `phieu/README.md`, `docs/HANDOFF.md`, `CLAUDE.md`, `docs/SETUP.md`, `CHANGELOG.md`. `~/doctor` not touched.

## Edge cases / limitations

- `doctor` binary is installed at both `~/.cargo/bin/doctor` and `~/.local/bin/doctor` on this machine — the "doctor absent" simulation required stripping both from PATH, not just one. Not a phiếu concern, just a local-machine note.
