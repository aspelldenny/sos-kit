# Discovery Report — P053: Sentinel-vs-silent merge deadlock fix

**Date:** 2026-06-06
**Branch:** harvest-git-gates
**Phiếu:** docs/ticket/P053-sentinel-deadlock-fix.md (V2, Option A)

---

## What shipped

### Core fix

`.claude/commands/security-review.md` Step 3 now partitions by mode before applying silent-when-clean:

- **PR mode (block-unsafe-merge-governed):** BỎ QUA silent-when-clean → ALWAYS continue to Step 4 and post sentinel comment, including clean `Verdict: APPROVE`. Rationale embedded in Step 3 text: hook at `block-unsafe-merge.sh:103-109` requires the sentinel comment to allow merge; silence = deadlock.
- **Branch / range mode (advisory):** silent-when-clean GIỮ NGUYÊN — `APPROVE + 0 FLAG → Do NOT post/write`.

Step 4 PR-mode section updated to cover both clean APPROVE and NEEDS_REVIEW explicitly, with inline rationale citing `block-unsafe-merge.sh:103-109`.

Hard rules section updated: silent-when-clean rule now explicitly scoped to branch/range mode; PR mode always-post documented.

### Stale-sentinel known limitation documented (Task 4)

Added `> **Known limitation — APPROVE sentinel is NOT SHA-scoped.**` block in `security-review.md` Step 4, immediately after the PR-mode post path. Text mirrors `block-unsafe-merge.sh:15-16` documented-bypass pattern as the phiếu required. Points to [P055] in BACKLOG.

### Doc-only clarifications (Tasks 3, 5)

- `agents/boundary-check.md` line 194: silent-when-clean rule updated to distinguish PR mode (caller ALWAYS posts) from advisory/branch/range mode (caller may apply silent-when-clean). Giám sát behavior UNCHANGED — still always returns sentinel block.
- `README.md` line 74: contract description updated from "posts an ADVISORY comment to the PR (silent when clean — KHÔNG block merge)" to PR mode always-post + silent-when-clean = advisory/branch/range only.
- `CHANGELOG.md`: P053 entry added under v2.3 forge section.

---

## Task 0 anchor verification results

| # | Assumption | Result |
|---|-----------|--------|
| 1 | block-unsafe-merge.sh:103-109 sentinel grep | ✅ confirmed — grep -q `<!-- security-review-start -->` at :103; `grep -E '^Verdict:'` at :105; `grep -q 'APPROVE'` at :106 |
| 2 | Sentinel strings `<!-- security-review-start -->` / `<!-- security-review-end -->` | ✅ confirmed — boundary-check.md:174,189; block-unsafe-merge.sh:103,105; security-review.md (multiple refs) |
| 3 | Verdict line `Verdict: APPROVE | NEEDS_REVIEW` at boundary-check.md:188 | ✅ confirmed — `grep -n 'Verdict:' agents/boundary-check.md` → :188 |
| 4 | Silent-when-clean suppress in slash command Step 3 (caller), not Giám sát | ✅ confirmed — security-review.md:62 "Do NOT post comment"; boundary-check.md:194 "caller's slash command applies silent-when-clean rule" |
| 5 | Giám sát always returns sentinel block regardless of verdict | ✅ confirmed — boundary-check.md:194 "Em vẫn return sentinel block in final report luôn" |
| 6 | PR mode = block-unsafe-merge-governed; block-unsafe-merge.sh:47 only matches `gh pr merge[[:space:]]+[0-9]+` | ✅ confirmed |
| 7 | Step 4 already has `gh pr comment <N> --body` PR path | ✅ confirmed — security-review.md:68 |
| 8 | End-to-end runtime allow after clean APPROVE (runtime) | Static dry-run PASS (see sentinel match proof below); live PR test deferred (runtime) |
| 9 | README line ~74 boundary-check contract (line number drift check) | ✅ EXACT LINE 74 — no drift from V1 cited. Updated as Task 5. |
| 10 | block-unsafe-merge.sh has 0 sha/SHA/head_sha/created_at scoping | ✅ confirmed — grep 0 hit (only regex pattern string in :65-66 which is SECURITY_SURFACE_PATTERN, not scoping logic) |

---

## Sentinel string match proof (critical correctness check — anchor #8 static)

**What block-unsafe-merge.sh greps (lines 103-106):**
```bash
grep -q '<!-- security-review-start -->'   # sentinel presence
grep -A 50 '<!-- security-review-start -->' | grep -E '^Verdict:' | head -1  # extract verdict line
grep -q 'APPROVE'                          # check for APPROVE in verdict line
```

**What security-review.md Step 4 now posts (after P053):**
```
<!-- security-review-start -->
...
Verdict: APPROVE
<!-- security-review-end -->
```

(The emitted format is defined by `agents/boundary-check.md:174-189` output format chuẩn — unchanged.)

**Dry-run simulation:**

A mock COMMENTS string containing the exact sentinel block was piped through the identical grep logic (`grep -q '<!-- security-review-start -->'` → `grep -A 50 | grep -E '^Verdict:' | head -1` → `grep -q 'APPROVE'`):

- VERDICT_LINE extracted: `'Verdict: APPROVE'`
- Result: `APPROVE detected → hook would EXIT 0 (allow merge)`

**Conclusion:** Sentinel string matches exactly. Deadlock is resolved — a clean APPROVE sentinel posted in PR mode will satisfy `block-unsafe-merge.sh:103-106` and allow merge.

---

## Assumptions in phiếu — CORRECT

- Anchor #4: silent-when-clean IS in slash command Step 3 (caller), not Giám sát. ✅
- Anchor #5: Giám sát always returns sentinel block — Option A does not require changing Giám sát emit. ✅
- Anchor #6: Mode distinction (PR vs branch/range) is cleanly detectable via Step 0 + block-unsafe-merge.sh:47 pattern. ✅
- Anchor #9: README line 74 is exactly the right line to update, with no drift. ✅

## Assumptions in phiếu — WRONG / deviations

None. All assumptions held. No Tầng 1 escalations mid-execute.

---

## LAYERS.md check (Docs Gate — `agents/boundary-check.md` change → LAYERS.md specialist subsection)

LAYERS.md specialist subsection (lines 38-44) lists Giám sát output as "Sentinel-wrapped verdict block (5 INV + APPROVE/NEEDS_REVIEW) → caller posts as PR comment (or local fallback file)" — structural description only, NO mention of silent-when-clean contract. Per phiếu Task 5 note: **N/A — LAYERS.md chỉ liệt kê output format, không mô tả silent-when-clean contract**. No update required.

---

## Stale-sentinel limitation

Documented in `security-review.md` Step 4. [P055] confirmed present in `docs/BACKLOG.md` (line 183) — Architect appended in Turn 2. No duplicate needed.

---

## Tầng 1 docs updated

- `agents/boundary-check.md` — silent-when-clean rule clarified (PR mode always-post scope)
- `README.md:74` — boundary-check contract line updated (PR mode sentinel always-post; silent-when-clean = ADVISORY/branch/range only)
- `CHANGELOG.md` — P053 entry added
- `CLAUDE.md` DOCS GATE table: per mapping, `agents/boundary-check.md` change → `docs/LAYERS.md` specialist subsection + `README.md` row. LAYERS.md: N/A (no contract description to update). README.md: DONE (Task 5).

## Tier escalations

None. Phiếu remained Tầng 1 throughout; no 2→1 trigger fired (no schema/API/dependency/auth-boundary changes beyond the documented security-surface doc contract).

## Edge cases / limitations found

- Anchor #8 (end-to-end runtime allow) remains unverified at runtime (no live PR available in execute context). Dry-run string simulation PASS is strong evidence for correctness given the exact string match, but live PR test should be done when next security-surface PR is created.
- Stale-sentinel hole (pre-existing, made easier to hit by P053): documented, mitigations noted, [P055] filed. Not a regression introduced by P053.
