# P078a Discovery Report — core adapter-shared serialization contract (`core/STATE.md`)

## Anchors CORRECT / WRONG

All 15 Task 0 anchors re-verified in EXECUTE (re-grepped; no drift since CHALLENGE Turn 1):

- #1-#13: CORRECT as Architect drafted (Codex under-spec list at `docs/adapters/CODEX_ADAPTER_DISCOVERY_2026-07-22.md:26-28`; ticket path `phieu/README.md:100-118`; TICKET_TEMPLATE schema; `core/WORKFLOW.md:49,52-56,17-19`; `core/POLICY.md:67-76,33-44`; `agents/orchestrator.md:83`; `core/ASSETS.md:16-18`).
- #14 (symlink normalization): CORRECT — `scripts/architect-guard.sh` + `scripts/orchestrator-guard.sh` have zero `realpath`/`readlink` calls; matching is a string-strip on `NORMALIZED_PATH` (`./` + `$PWD` prefix). Claude does not resolve symlinks before allowlist matching. `core/STATE.md`'s Edit allowlist section uses the fail-safe wording from the phiếu verbatim ("matching is performed on the repo-relative logical path as written… symlink resolution is integration-defined… MUST report" residual bypass risk).
- #15 (CLAUDE.md core file list location): WRONG as assumed — `grep -n "core/{README\|ASSETS.md\|core/README\|core/ROLES\|core/WORKFLOW\|core/POLICY" CLAUDE.md` returned zero hits pre-edit. CLAUDE.md's repo-structure tree did not list `core/` as a directory at all (pre-existing drift, not caused by this phiếu). Fixed in this phiếu: added a full new `core/` tree entry (5 existing files + `STATE.md`) between `configs/` and `crates/` (alphabetical placement), rather than appending one line to an existing list.

## Coverage checklist (11-item Codex under-spec map)

| Codex-report item | Disposition |
|---|---|
| ticket storage path/filename/active-selection | Lifted — `core/STATE.md` "Ticket storage and schema" |
| ticket Markdown/TOML schema | Lifted — same section |
| machine-readable state-file format | Lifted — "Lifecycle state artifact" |
| approval-record marker+mutation-authority | Lifted — "Approval record" |
| edit-allowlist glob/normalization/symlink/amendment | Lifted — "Edit allowlist and verify scope" |
| review-trigger map | Lifted — "Review trigger map" |
| BLOCKED storage+resume format | Lifted — "Blocked state format" |
| backlog serialization | Deferred — "Deferred scope" (→ P078a2) |
| tier-classifier owner | Deferred — "Deferred scope" (→ P078a2) |
| concurrent-ownership lock/worktree | Deferred — "Deferred scope" (→ P078a2) |
| publish-actor division | Deferred — "Deferred scope" (→ P078a2) |

7 lifted (across 6 sections, ticket storage+schema combined into one) + 4 explicitly deferred = 11/11, no silent drop.

## Neutrality grep — 0 hit

```
grep -niE 'claude|codex|\.sos-state|asktuserquestion|pretooluse|launchd|\bgh\b|quản đốc|kiến trúc|thợ|worker-active|architect-active' core/STATE.md
```
Exit 1 (no match). Confirmed also with the narrower coordinator-specified pattern (`Claude|Codex|\.claude|\.sos-state|AskUserQuestion|PreToolUse|worker-active|architect-active`) — same result, 0 hit. File content stays at the "what" level (canonical fields/paths/shapes); every mechanism-level detail is phrased as "an integration renders this through its own mechanism."

## Backward-compat diff (per format vs Claude source)

| Format | Claude source | Result |
|---|---|---|
| Ticket schema | `phieu/TICKET_TEMPLATE.md` (header/context/Task0/debate-log/nhiệm-vụ/files/constraints/nghiệm-thu) | khớp 1:1 |
| Lifecycle state / debate-log authority | `core/WORKFLOW.md:49` "ticket debate log is the authoritative record" | khớp — STATE.md explicitly keeps state-file as OPTIONAL derived projection, does not make it authoritative |
| Approval record | `core/WORKFLOW.md:52-56` + `core/POLICY.md:33-44` (exact-version bind, owner/delegate-only, silence≠approve) | khớp |
| Edit allowlist | `core/POLICY.md:67-76` (narrow edit_allow / broader verify_read / stop-until-amend) | khớp |
| Review trigger map | `agents/orchestrator.md:83` (auth/session/permission/privacy, `src/|app/|lib/`, schema/migrations, `.env*` except `.env.example`, middleware, webhook, `INV-LOCAL-*`) | khớp — abstracted to neutral surface classes, no glob leaked |
| BLOCKED format | `core/WORKFLOW.md:17-19` (previous state, reason, unblock owner, safe resume state) | khớp |

No mismatch found requiring a change to Claude's existing behavior. No FLAG raised beyond the pre-existing CLAUDE.md drift (anchor #15, already fixed as part of Task 9).

**State-file authority confirmation:** `core/STATE.md` "Lifecycle state artifact" section states explicitly: "the ticket's debate log remains the authoritative human record… A machine-readable state artifact is a derived projection… An integration is not required to materialize a separate file." This does not conflict with `agents/orchestrator.md`'s "state truth = phiếu Debate Log" hard rule.

## Docs updated (Tầng 1)

- `core/STATE.md` — NEW.
- `SOS.md` — canonical map +1 row (`core/STATE.md`) + 1-sentence precedence note (STATE.md governs serialization only, subordinate to WORKFLOW/POLICY/ROLES).
- `core/README.md` — "What belongs here" +1 bullet.
- `CLAUDE.md` — repo-structure: added full `core/` tree entry (was entirely missing — anchor #15 finding), including `STATE.md`.
- `CHANGELOG.md` — new `[P078a]` entry at top of the in-progress v2.3 section.
- `core/ASSETS.md` — verified, no edit needed: `core/**` already listed under `PORTABLE` (`core/ASSETS.md:16-18`), covers `STATE.md` automatically (anchor #13).
- `core/WORKFLOW.md` / `core/POLICY.md` / `core/ROLES.md` — verified unchanged (`git diff` shows no modification to these 3 files), per Constraint 3.

## Tier escalations

None. Stayed Tầng 1 as classified (core contract surface); no scope expansion beyond the phiếu's declared "Files cần sửa" list.

## Deferred scope (4 items → P078a2)

Confirmed present in `core/STATE.md`'s "Deferred scope" section: tier-classifier ownership, concurrent-ownership lock/worktree isolation format, publish-actor division, backlog serialization format. Each notes "integration-defined until a follow-up ticket lifts them."

## Trust-gate

`core/STATE.md` is a new doc file, not an auto-exec surface (no script/hook/binary changed) — `scripts/trust-gate.sh` baseline diff not expected to flag. Verified at commit time (pre-commit `[8/8]` ran clean, no rebaseline needed).

## Handoff note

P078a done → `core/STATE.md` is now the canonical serialization spec P078b (Codex adapter) must render against. Codex adapter render should reference the 6 lifted sections directly; the 4 deferred items remain integration-defined for now.
