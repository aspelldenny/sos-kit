# Discovery Report — P083: doc-drift cleanup (README install + LAYERS v1-residue + tagline)

## Anchor verification (Task 0, re-run in EXECUTE mode — recap from CHALLENGE)

| # | Assumption | Result | Evidence |
|---|-----------|--------|----------|
| 1 | `README.md:240-254` install block copies 12 skills | ✅ CORRECT | confirmed pre-fix |
| 2 | 5 living (`apply forge idea init retro`) / 8 attic (`decide insight plan qa review route ship verify`) | ✅ CORRECT | `ls -d skills/*/` and `ls -d skills/attic/*/` — exact match |
| 3 | LAYERS v1 residue at `:48,77,84-85,92,99,115,166,195-198` | ✅ CORRECT (with nuance: `:77` and `:99` are NOT drift — see F2 disposition below) | grep confirmed |
| 4 | `agents/architect.md` grants Read/Write/Glob, no Bash/Grep/Edit | ✅ CORRECT | `agents/architect.md:4,19-31` — envelope is a declared tool grant, no mention of "Web" anywhere in the file |
| 5 | `README.md:3` vs `docs/PHILOSOPHY.md:26` conflict | ✅ CORRECT | confirmed |
| 6 | `INSTALL.md:100-102` only copies `idea`, no 12-skill drift | ✅ CORRECT | confirmed, no fix needed |
| 7 | LAYERS ASCII layer-boxes (`:76,91,104`) list attic skills as living | ✅ CORRECT | confirmed |
| 8 | LAYERS has a dedicated skill table separate from the boxes | ✅ CORRECT | `docs/LAYERS.md:216-222` — table already correct (5 living); the drift was ONLY in the ASCII boxes 130+ lines above, same doc |
| 9 | `phieu/RELAY_PROTOCOL.md:3` frames Architect as v1 Claude Web | ✅ CORRECT | confirmed |

## F1 — tagline (Sếp chốt Option A)

`README.md:3` → `"One person. No team. Full operating system from code-ready to production health."` (dropped "from inbound request"). Matches `docs/PHILOSOPHY.md:26` scope exactly.

## F3 — README + SETUP.md install block (5 living only)

- `README.md:238-255` install block trimmed to `init forge apply retro` (global) + `/idea` project-local note kept.
- `docs/SETUP.md:34-46` had the **identical 12-skill drift** (not flagged as a hard task in the phiếu, but covered by the phiếu's Docs Gate mirror requirement) — fixed the same way.
- Verified all 4 remaining `cp -r skills/<name>` paths exist: `init forge apply retro` — 4/4 OK (`idea` intentionally excluded — project-local, not global).
- Cross-checked 3 skill listings now consistent: `README.md:134-142` (skill table), `docs/LAYERS.md:216-222` (skill table), install blocks in `README.md` + `docs/SETUP.md`. All 5 living, no attic references remain in any install command.
- `INSTALL.md` — verified clean, no fix needed (only ever copied `idea`).

## F2 — LAYERS.md v1→v2 re-frame

**Reframed (v1 residue removed):**
- `:48` "Kiến trúc sư lives in Claude Web Project" → subagent in-session, docs-only envelope is a tool grant (cites `agents/architect.md`).
- `:76` skill list (Chủ nhà box) → `/init /idea` + attic note for insight/route/decide.
- `:84-85` "Relay... Chủ nhà is the courier" → Quản đốc automates in-session (v2 default); Chủ nhà only courier in v1/cross-session.
- `:91` skill list (Architect box) → `/forge` + attic note (plan/verify inlined in agent handbooks).
- `:92` "Tools: Claude Web Project" → tool grant (Read/Write/Glob, no Bash/Grep/Edit-src), spawned in-session by Quản đốc.
- `:104` skill list (Worker box) → `/apply /retro` + attic note.
- `:115` "Ping Architect directly (goes through Chủ nhà)" → routed by Quản đốc in-session; Chủ nhà only in v1/cross-session.
- `:166` (Chủ nhà's 7 responsibilities, #7) → Quản đốc automates relay in-session (v2 default); Chủ nhà only courier in v1/cross-session.
- `:195-198` (Anti-pattern #3) → rewritten: reality is Quản đốc routes in-session (Debate Log CHALLENGE/RESPOND), only falls to Chủ nhà manual paste in v1/cross-session mode.

**Kept as-is (verified NOT drift, per phiếu instruction):**
- `:77` "Tools: Claude Code OR Claude Web (usually wherever the human is)" — this is **Chủ nhà's** row (the human), not Architect's. Correctly left untouched.
- `:99` "Grep code directly (no shell access)" — states the envelope reason only, no location claim; left as-is.
- Tầng-def single-source `:145-154` — untouched, confirmed unrelated.

**Post-fix verification:** `grep -ni "claude web project" docs/LAYERS.md` → 0 hits.

## F2 — RELAY_PROTOCOL.md

Added a v2-mode banner at top (`:1-3` area): states v2 default = Quản đốc automates relay in-session, this doc does NOT apply; protocol remains valid for v1/cross-session mode (separate worktrees/sessions). Protocol body NOT deleted (still has value for that mode) — matches phiếu instruction "KHÔNG xoá protocol."

## HANDOFF.md residue disposition (BẮT BUỘC per CHALLENGE + this EXECUTE instruction)

Found lines with unlabeled v1-default framing: `:31, 35, 47, 70, 146, 163, 208, 258`.

**Fixed:**
- `:146` (Handoff 3, "Key constraint") — the single most load-bearing line (this IS the escalation flow v2 changed most). Rewrote to point at the in-session Debate Log path (Handoff 2.5, Quản đốc-routed) as v2 default, with v1/cross-session Claude-Web path as fallback.

**Deferred (left as-is), with reason:**
- `:31, 35, 47, 70` (Handoff 0/1) — describe the v1 raw-brief/insight workflow specifically. The document's own top banner (`:7-9`, already dual-labeled "v1 Web Project mode" vs "v2.1 Subagent mode" — pre-existing, not touched by this phiếu) scopes these sections as the v1 baseline flow; Handoff 2.5 (`:91`, pre-existing) is the documented v2 supplement for the debate/relay portion. Not touching Handoff 0/1 body text is lower-risk than a broad rewrite of two full handoff sections outside phiếu's declared file/task list — flagging here per Docs Gate rather than expanding scope silently.
- `:163` (Handoff 3, "Chủ nhà's response... forwards to Architect in Claude Web") — same handoff as the fixed `:146`; left because Handoff 2.5 already documents the v2 alternate path, and this line is explicitly under the "if v1 cross-session" continuation that follows the fixed key-constraint line.
- `:208` (Handoff 4, transport line) — describes Discovery Report commit mechanics ("Architect reads DISCOVERIES.md directly in the next Claude Web session") — v1/v2-agnostic in substance (Architect reads DISCOVERIES.md regardless of session mode); low drift risk, left as-is.
- `:258` (closing paragraph) — general framing sentence about why formalized handoffs matter ("two of the layers... live in separate sessions") — historically accurate for v1, and the document's opening banner already scopes the whole doc; not touched to avoid unbalancing the doc's existing dual-mode structure with a partial rewrite.

**Not additional-tasked:** given the scope is 1 targeted fix (most load-bearing) + explicit written disposition for the rest, no new phiếu task was opened. If Chủ nhà wants full HANDOFF v1-purge, recommend a dedicated follow-up phiếu (HANDOFF.md is a contract-surface doc per CLAUDE.md Rule #9 — deserves its own CHALLENGE pass, not a drive-by inside P083).

## Docs Gate (Tầng 1 — per phiếu Nghiệm thu)

- `README.md` sync check: skill table (`:134-142`), install block, and LAYERS skill table (`:216-222`) — all 3 now list exactly the 5 living skills. ✅
- `docs/SETUP.md` mirror: found + fixed identical 12-skill drift (not explicitly named as a phiếu task but caught by the phiếu's own "docs-gate mirror" gate item). ✅
- `docs/HANDOFF.md` Handoff 3 checked against LAYERS re-frame — fixed the key-constraint line; rest deferred with reason (above). ✅
- `CHANGELOG.md` — P083 entry added above P082.

## Nghiệm thu — real command output

```
$ ls -d skills/*/
skills/apply/ skills/attic/ skills/forge/ skills/idea/ skills/init/ skills/retro/

$ ls -d skills/attic/*/
skills/attic/decide/ skills/attic/insight/ skills/attic/plan/ skills/attic/qa/ skills/attic/review/ skills/attic/route/ skills/attic/ship/ skills/attic/verify/

$ for s in init forge apply retro; do [ -d "skills/$s" ] && echo "OK skills/$s"; done
OK skills/init
OK skills/forge
OK skills/apply
OK skills/retro

$ grep -ni "claude web project" docs/LAYERS.md; echo "hits: $?"
(no output) hits: 1   # grep exit 1 = 0 matches

$ grep -n "One person. No team." README.md
3:One person. No team. Full operating system from code-ready to production health.
```

## Scope

No scope expansion beyond phiếu + this EXECUTE instruction's explicit HANDOFF residue directive. Files touched: `README.md`, `docs/LAYERS.md`, `phieu/RELAY_PROTOCOL.md`, `docs/HANDOFF.md` (1 line, per this EXECUTE instruction), `docs/SETUP.md` (mirror fix, per phiếu's own gate item), `CHANGELOG.md`. `docs/PHILOSOPHY.md`, `INSTALL.md`, `agents/architect.md`, `CLAUDE.md` — read/verified only, not edited (per phiếu "Files KHÔNG sửa").

## Edge cases / limitations

- HANDOFF.md is only partially reconciled (1 of 8 flagged lines fixed, 7 deferred with written reason) — see disposition table above. This is a known partial-fix, not an oversight.
