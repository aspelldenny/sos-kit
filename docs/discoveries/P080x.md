# P080x Discovery Report — Dev pre-commit hook fail-OPEN parity

## Assumptions in phiếu — CORRECT

- Anchor #1: `hooks/pre-commit:282-291` — `[7/8]` else-branch was bare `echo`, no `FAIL_COUNT++`/`exit` (fail-OPEN). Confirmed.
- Anchor #2: `hooks/pre-commit:262-271` — `[6/8]` same pattern. Confirmed.
- Anchor #3: `hooks/pre-commit:319-328` — summary `FAIL_COUNT>0` → `exit 1`, else warn/pass. Confirmed, and this is exactly the mechanism the fix wires into (no need to add a standalone `exit 1` per-phase — the existing summary already does it once `FAIL_COUNT` is bumped).
- Anchor #4: `crates/sos-install/src/templates/backstop-pre-commit.sh:25-36` — both guards do `[ ! -f ... ]` → print `BLOCKED:` + `exit 1`. Fail-CLOSED semantics confirmed to port (dev hook uses `FAIL_COUNT` + single summary-exit instead of an immediate per-phase `exit 1` — same *outcome*, different mechanical shape, both fail-closed).
- Anchor #6: `crates/sos-install/src/templates/backstop-pre-commit.sh:25,32` — exactly `scripts/block-env-commit.sh` + `scripts/no-code-on-default.sh`, matching the phiếu's scoped [7]/[6] targets 1:1.
- Anchor #7: `CLAUDE.md:24,117,222` + `docs/SETUP.md:146,180` — phase count `[8/8]` referenced consistently; edit only changes prose describing missing-script semantics for 2 phases, not the count.

## Assumptions in phiếu — WRONG

None. Anchor #5 (D1 live-proof on the P080-dogfood-round1 branch) was not re-verified this session — not needed, the Context section already describes the finding in enough detail to act on, and this session's own pristine fixture independently re-proved the same gap (see Nghiệm thu below) rather than relying on the historical branch.

## Scope expansions

None. Exactly [6/8]+[7/8] else-branches changed, plus the optional Task 3 header-comment cosmetic (added a short fail-CLOSED note per phase, no reordering, no count change).

## Oracle results (pristine fixture, `/private/tmp/.../scratchpad/p080x-fixture`)

Fixture: fresh `git init -b main`, both guard scripts copied in, fixed `hooks/pre-commit` installed as `.git/hooks/pre-commit`, no `.sos-kit-self` self-opt-out marker (simulating a downstream `sos new` project, not sos-kit itself).

| Case | Setup | Expected | Actual exit |
|---|---|---|---|
| A | delete `block-env-commit.sh`, stage real `.env` | BLOCKED | **1** (was 0 pre-fix — reproduced separately, see below) |
| B | delete `no-code-on-default.sh`, stage `app.js` on default | BLOCKED | **1** |
| C | both guards present, clean commit on feature branch | ALLOWED (no false-block) | **0** |
| D | both guards present, stage `.env` (regression) | BLOCKED (unchanged old behavior) | **1** |
| Negative (2-directional) | reverted fix (stashed) + case A repeated | fail-OPEN reproduces | **0** (confirmed regression reproduces), then fix restored → case A re-run → **1** |

`bash -n hooks/pre-commit` — clean.

## Edge cases / limitations found

- [4/8] security-gate and [8/8] trust-gate remain warn-skip by design (deferred, matches phase-decision table) — a fresh `sos new` checkout without `scripts/install-hooks.sh` run yet would otherwise brick on those two. Flagged as a follow-up, not expanded here.
- The dev hook's fail-closed mechanism is "bump FAIL_COUNT, let the existing summary exit" rather than an immediate per-phase `exit 1` (unlike the install-path backstop). This is equivalent in outcome and consistent with every other FAIL branch already in this hook (see `[1/8]`/`[2/8]`/`[4/8]`/`[5/8]`/`[8/8]` FAIL branches, all use the same `FAIL_COUNT` pattern) — no new mechanism introduced.

## Docs updated to match reality

- `CLAUDE.md` — repo-structure line for `hooks/pre-commit` now notes `[6/8]`/`[7/8]` fail-CLOSED semantics.
- `docs/SETUP.md` — `[6/8]` and `[7/8]` phase descriptions each gained a fail-CLOSED bullet.
- `SECURITY.md` — new section "Dev `[8/8]` hook — `[6]`/`[7]` ported to fail-CLOSED (P080x)".
- `CHANGELOG.md` — new `[P080x]` entry under v2.3 forge (in progress).
- Phase count `[8/8]` confirmed UNCHANGED everywhere (no section added/removed) — only missing-script semantics of 2 existing phases changed.

## Follow-up

After merge, P080 round-2 must re-run the D1 repro (delete guard → commit `.env`/code) and confirm it is now green, unblocking P080 → P081.
