# Discovery Report — P038: phieu-cleanup-and-safety

> Per-phiếu file pattern (P038 eat-own-dogfood). Architect Reads selectively (only when next phiếu references same component as this one).

## Assumptions in phiếu — CORRECT

- Anchor #1 (`[verified]`): `phieu/phieu.sh:160-174` — `_phieu_done_impl` confirmed: only `git worktree remove` + echo. Line 173 literal: `"✅ Worktree removed: ${wt_parent}/$name (branch still exists)"`. No strip, no branch delete, no backup. Replaced entirely with extended implementation.
- Anchor #2 (`[verified]`): `agents/orchestrator.md:40-46` — Tier routing section intact at lines 40-46. Logic unchanged. Worker did NOT touch these lines. Verified post-edit by re-reading.
- Anchor #3 (`[verified]`): `agents/worker.md:12-26` — Hard envelope rules section confirmed. New subsection "Destructive op safety rails (P038)" inserted after line 26 (before "## Why this envelope"), exactly as specified.
- Anchor #4 (`[verified]`): `scripts/session-start-banner.sh` — 85 lines pre-edit, 145 lines post-edit. Size warn + cleanup nudge blocks both inserted between FALLBACK_USED block and Orchestrator contract block. Structure preserved.
- Anchor #5 (`[verified]`): `phieu/TICKET_TEMPLATE.md:32-44` — Task 0 section confirmed. Pre-phiếu snapshot subsection inserted after "If Result column has ❌" note. Discovery Report path updated to per-phiếu pattern. Line 4 dual-path note added.
- Anchor #6 (`[verified]`): `hooks/pre-commit` not modified. Size warn lives in banner. Pre-commit's existing `docs/DISCOVERIES.md` references (check for existence, not content) still work with index-only content.
- Anchor #7 (`[verified]`): `docs/ORCHESTRATION.md` — "## Failure modes + recovery" table ended with "Worker silently demoted Tầng 1 → Tầng 2" row. New "Phiếu lifecycle" section inserted between Failure modes and Concrete example session, as specified.
- Anchor #8 (`[verified]`): `.gitignore` — `.sos-state/` was last line (13 lines total). `.backup/` appended with comment.
- Anchor #9 (`⚠️ DRIFT CONFIRMED`): `phieu/TICKET_TEMPLATE.md:4` literal was `docs/ticket/P<NNN>-<slug>.md`. Fixed to dual-path note per V3 [O1.2] ACCEPT. `phieu.sh:89` deliberately unchanged (downstream-first, DEFEND stance per Architect V3 response).

## Assumptions in phiếu — WRONG

- None. All 9 anchors held (8 ✅ + 1 ⚠️ drift handled exactly as Architect specified in V3).

## Scope expansions

- **Tầng 2 self-decide — BACKLOG.md recurring routines update.** Phiếu spec'd updating BACKLOG.md "v2.2 wave" entry and "Recently shipped". Worker also updated the "Per phiếu" recurring routine (line 80) to reference `docs/discoveries/P<NNN>.md` instead of `docs/DISCOVERIES.md`. Not specified in phiếu but directly follows from Task 4 (Discovery decoupling) and clearly needed to avoid stale recurring reminder. Zero new files. Tầng 2 judgment: anyone maintaining the repo later needs the routine to match the new convention.
- **Tầng 2 self-decide — `agents/worker.md` TaskCreate mention.** Phiếu updated step 7 wording in EXECUTE workflow; Worker also updated the corresponding TaskCreate description in MANDATORY section (step 4: "Append Discovery Report to docs/DISCOVERIES.md" → "Write Discovery Report to docs/discoveries/P<NNN>.md + append index entry"). Consistent, zero new files.

## Edge cases / limitations found

- **awk strip pattern: `### Turn N — Architect Response` with Vietnamese em-dash.** Phiếu uses Unicode em-dash (`—`) in "### Turn 1 — Worker Challenge" headers. The awk pattern `/^### Turn [0-9]+ — Worker Challenge/` uses literal em-dash byte sequence. Tested: strip correctly matched the real P038 phiếu Debate Log headers (UTF-8 shell environment on Linux). Cross-platform note: should work on macOS too (both are UTF-8 by default). No issue found.
- **`git branch --list "*/${name}" "*/${phieu_id}-*"` glob behavior.** The `*/${name}` glob matches exact branch name `feat/P038-phieu-cleanup-and-safety`. The `*/${phieu_id}-*` glob matches any `feat/P038-*`, `fix/P038-*` etc. On testing: `git branch --list` with globs returns correct results in bash and zsh (both expand within git, not shell). No shell glob expansion issue.
- **Banner cleanup nudge detection: phiếu with `[date]` placeholder skipped correctly.** The `case` match on `*"[date]"*` catches the template placeholder literal. Tested: P-test-tang2 with `[date]` in approval line → skipped. Real date → detected. However, edge case: if Sếp writes "Approved by Chủ nhà: [2026-05-02]" (brackets around real date), it would also be skipped. Acceptable — bracket syntax is non-standard; the check targets the literal `[date]` placeholder.
- **`docs/DISCOVERIES.md` pre-existing size.** sos-kit's DISCOVERIES.md was ~9k bytes (well under 40k threshold) — size warn correctly did NOT fire for it in live test. Only CHANGELOG.md triggered the warn during padding test.
- **orchestrator.md line count after edit.** Pre-edit: 88 lines. After inserting 2 lines (new H2 header + 1-body-line) after line 61, and existing blank at line 62 remaining, final count = 90. Verified by `wc -l agents/orchestrator.md` = 90. Exactly at CLAUDE.md:106 cap.
- **`phieu/done/` directory pre-existence.** phieu.sh `_phieu_done_impl` now creates `phieu/done/` via `mkdir -p` before moving the stripped file. `phieu/done/` already existed in sos-kit (P036, P035, P037 archived there). `mkdir -p` is idempotent — no issue. For fresh repos where `phieu/done/` doesn't exist, the `mkdir -p` creates it cleanly.
- **`docs/archive/` and `docs/discoveries/` directories.** Neither existed pre-P038 in sos-kit. Created via `mkdir -p` in Bash during Task 4. Both now present on disk. `.gitkeep` not needed — `docs/archive/DISCOVERIES_pre-2026-05.md` and `docs/discoveries/P038.md` provide non-empty content immediately.

## Docs updated to match reality

- `phieu/phieu.sh` — `_phieu_done_impl` extended (Task 5)
- `scripts/session-start-banner.sh` — doc size warn + cleanup nudge (Tasks 1 + 6)
- `agents/worker.md` — safety rails subsection + Anti-patterns section + step 7 path + TaskCreate label (Tasks 2 + 4)
- `phieu/TICKET_TEMPLATE.md` — Pre-phiếu snapshot subsection + line 4 dual-path + Discovery Report path (Tasks 3 + 4)
- `docs/DISCOVERIES.md` — converted to index file (Task 4)
- `docs/archive/DISCOVERIES_pre-2026-05.md` — new (old monolithic content) (Task 4)
- `docs/discoveries/P038.md` — new (this file, eat-own-dogfood) (Task 4 + Nghiệm thu)
- `docs/ORCHESTRATION.md` — Phiếu lifecycle section added (Task 7)
- `agents/orchestrator.md` — Phiếu cleanup nudge section added (Task 7)
- `docs/BACKLOG.md` — v2.2 wave closed + P038 Recently shipped + recurring routine updated (Task 7)
- `CHANGELOG.md` — v2.1.7 entry (Task 7)
- `.gitignore` — `.backup/` added (Task 3)

## Tier escalations

None. All 7 tasks executed per phiếu V3 spec. No mid-execute Tầng 2→1 escalation triggered.

## Cross-platform issues

- `wc -c < file` for byte count: POSIX-compliant, works on bash 4+ and zsh 5+ on Linux and macOS. No GNU `stat -c` or BSD `stat -f` used.
- awk em-dash handling: UTF-8 literal in pattern works correctly on Linux (WSL2). Expected to work on macOS (also UTF-8). No `LANG` or `LC_ALL` override needed.
- `grep -oE '^P[0-9]+'`: POSIX ERE, works on both GNU grep (Linux) and BSD grep (macOS).
- `sed 's/^[* ] //'`: POSIX sed, works cross-platform.
- `bash -n` syntax check passed. No zsh-specific syntax introduced.
