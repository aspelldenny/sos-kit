# P078b1 Discovery Report — Codex adapter foundation

## Anchor verification (final, EXECUTE mode re-check)

All 8 `[needs Worker verify]` anchors from Task 0 re-confirmed correct during EXECUTE (same findings as CHALLENGE Turn 1):

- **#1 CORRECT** — `crates/sos-core/src/adapter.rs:118-136`, trait `Adapter` 5 methods + all 9 types present.
- **#2 CORRECT** — `ManagedOperation.content` present (`adapter.rs:56`, unchanged by this ticket).
- **#3 CORRECT, CRITICAL** — `struct Finding` had **NO status field, no enum** before this ticket (`target_path`, `message` only). Added `pub enum FindingStatus { Sound, Partial, Missing }` + `status: FindingStatus` field on `Finding`, both in `crates/sos-core/src/adapter.rs`. This is a genuinely new addition, not a duplicate of an existing mechanism.
- **#4 CORRECT** — `crates/sos-cli/src/commands/install.rs:26` (pre-change) had the exact string `"codex adapter not yet available (P078) — use --runtime claude for now"`. Confirmed removed post-change (`grep -n "not yet available" crates/sos-cli/src/commands/install.rs` → 0 hits).
- **#5 CORRECT** — `sos-cli/Cargo.toml` deps matched exactly as described; added `sos-adapter-codex = { path = "../sos-adapter-codex" }`.
- **#6 CORRECT** — `dep_direction.rs:18` `FORBIDDEN_TOKENS` list scans `sos-core/src/**` only; `"sos_adapter"` substring-matches `sos_adapter_codex` too — the new crate + `FindingStatus` enum introduced **zero** forbidden tokens into `sos-core/src`, guard stayed green with **no changes to the guard file itself**.
- **#7 CORRECTION (non-Tầng-1, self-closed in CHALLENGE Turn 1)** — root `Cargo.toml` uses `members = ["crates/*"]` (a glob), NOT an explicit list. Per Task 1's own instruction ("workspace `Cargo.toml` (root, tùy glob)"), the "Lưu ý: thêm crate vào workspace members" sub-step was **skipped** — `crates/sos-adapter-codex/` was auto-picked-up by the glob with zero edits to root `Cargo.toml`. Verified: `git status --short` never showed root `Cargo.toml` as modified at any point.
- **#8 CORRECT** — `ClaudeAdapter` (`crates/sos-adapter-claude/src/lib.rs:1-56`) stub pattern confirmed exactly; `CodexAdapter` mirrors it (`Capabilities`/`Plan`/`Artifact`-passthrough/`Findings`/`RemovalPlan` shapes), diverging only where the ticket required (`detect()` and `verify()` are NOT stubs — they're the foundation's live logic).

## CodexAdapter type/method exact names

- `pub struct CodexAdapter;` (unit struct, `crates/sos-adapter-codex/src/lib.rs`)
- `impl Adapter for CodexAdapter` — all 5 trait methods implemented.
- Private helpers: `CodexAdapter::static_features() -> Vec<String>`, `CodexAdapter::probe_codex_version() -> Option<String>`.

## detect() runtime-probe

**Shell probe, fail-safe.** `probe_codex_version()` runs `Command::new("codex").arg("--version").output()`. On any failure path (`codex` not on PATH, non-zero exit, non-UTF8 stdout) it returns `None` via `.ok().filter(...).and_then(...)` chaining — never panics, never propagates an error (the trait's `detect()` signature has no `Result`, so this MUST be absorbed here). Static features (`hooks`, `multi_agent`, `sandbox_mode_read_only`, `apply_patch`) are always present regardless of probe outcome. Verified by dedicated unit test `detect_is_fail_safe_without_codex_installed` (green on this dev machine, which does not have `codex` installed).

## PARTIAL mechanism: 5 gaps declared, verify()↔CAPABILITY.md sync

`verify()` returns exactly 5 `Finding`s (asserted by test `verify_reports_exactly_five_gaps_none_sound`, which also asserts `status != FindingStatus::Sound` for every item):

1. per-role built-in tool allowlist — **Partial**
2. repo-distributed slash commands — **Missing**
3. skill-level `allowed-tools` — **Partial**
4. native ticket-version approval — **Missing**
5. architect Read/Glob path interception (+ enforcement-weakness note folded in) — **Partial**

`adapters/codex/CAPABILITY.md` was authored to match each `Finding`'s message 1:1 (what/why-status/backstop), citing `docs/adapters/CODEX_ADAPTER_DISCOVERY_2026-07-22.md:15-22` as ground truth for all 5. **Note on status assignment vs orchestrator's EXECUTE-mode paraphrase:** the orchestrator's spawn message suggested a slightly different status mapping ("no-native-approval=PARTIAL(guard-built)", "hook-bypass=PARTIAL" as a distinct item) than the phiếu's own Task 4 text (which the CHALLENGE-approved V1 phiếu specifies as MISSING for native ticket-approval, and folds enforcement-weakness into gap #5 rather than as a 6th item). Worker followed the **phiếu's Task 4 text** as the authoritative contract (5 items exactly, as anchor #10 lists them a-e) rather than the paraphrase, since the phiếu is the audit-trail contract and the paraphrase appears to be a compressed recap, not a scope change. Flagging here for Chủ nhà visibility in case the paraphrase was intentional and should be a phiếu amendment for b2/b3.

## Symmetry flag (for P078c Claude-render reconcile)

- `Finding.status: FindingStatus` is a plain, host-neutral enum — no Codex-specific variant. Applies equally to a future `ClaudeAdapter::verify()` real implementation (currently `Findings::default()` stub, untouched).
- No Codex-only field was added to any shared core type (`Capabilities`, `Plan`, `ManagedOperation`, `Artifact`, `RemovalPlan` all unchanged).
- `CodexAdapter::render()` stub uses the exact same passthrough shape as `ClaudeAdapter::render()` — no divergence introduced that would need reconciling later.

## Cross-ref flag (founder-facing, non-blocking)

This ticket adds a new **product-source** crate (`crates/sos-adapter-codex/**`) with real logic (`detect()`/`verify()`), not just a skeleton. `docs/BACKLOG.md` already has an open doctrine question (from P077f) about whether `scripts/orchestrator-guard.sh`'s kit-maintenance allow-list should eventually gate `crates/**/src` as Worker-only instead of the current allow-list approach. This ticket's new crate is additional surface under that same open question — noted for Chủ nhà, not resolved here.

## Structural oracle: exit-0 caveat (environment, not regression)

`sos install --runtime codex --dry-run` no longer returns the "not yet available" error and reaches the **identical engine code path** as `--runtime claude` (both call `engine::resolve_tools()` → `engine::dry_run()`). On this dev machine, `resolve_tools()`'s step-5 tool-manifest pin check (P077d3) currently fails for **both** `codex` and `claude` runtimes identically (`doctor`/`claude-hooks`/`docs-gate`/`ship`/`advisory-inbox` DRIFT, `inv-gate` MISSING) — a pre-existing environment condition unrelated to this ticket, confirmed by running `--runtime claude --dry-run` and observing byte-identical failure output. This is NOT a regression: the codex-specific bail string is gone (grep-confirmed), and CodexAdapter constructs + drives the engine correctly up to the point both runtimes hit the same unrelated gate. Full "exit 0" on THIS machine would require the sister tools to be upgraded first — orthogonal to P078b1 scope.

## Docs updated

- `docs/PORTABILITY_ARCHITECTURE.md` — P078b1 status paragraph added (after P077f); deviation-list item 2 marked RESOLVED; migration table P078 row marked IN PROGRESS.
- `crates/README.md` — module-layout tree updated (removed "not created"), deviation-list item 2 RESOLVED, new "Codex adapter foundation (P078b1)" subsection added mirroring d1/d2/d3 pattern.
- `core/ASSETS.md` — new "Codex adapter (P078b1 — foundation live)" note under Adapter-owned asset classes. §32-33 (agents/skills migration owner "P076-P078") left as-is — N/A explicit: P078b1 does not render `AGENTS.md`/`.agents/skills/**` for Codex (that's P078b2), so the existing range is still accurate, no bump needed.
- `CHANGELOG.md` — P078b1 entry added at top of v2.3 forge section.
- New: `adapters/codex/README.md`, `adapters/codex/MAPPING.md`, `adapters/codex/CAPABILITY.md`.

## Tier escalations

None. Ticket stayed Tầng 1 throughout (as declared); no additional Tầng 1 issue surfaced during EXECUTE beyond what CHALLENGE Turn 1 already covered (anchor #7 correction, self-closed, non-blocking).

## Self-caught regression (mid-EXECUTE)

First draft of the `core/ASSETS.md` "Codex adapter" note literally referenced `adapters/codex/{README,MAPPING,CAPABILITY}.md` by path — this is exactly the `core/** → adapters/**` reference the Regression checklist forbids (dep-direction is doc-level too, not just code-level). Caught by running the checklist's own `grep -rn 'adapters/' core/` before commit (not skipped), rewritten to describe the artifact classes generically without naming the `adapters/` directory. Lesson: the regression check is not just for `crates/sos-core/src/**` — Worker must also self-run it against any prose it adds to `core/**` during the same ticket, not only trust that "docs are safe by default."

## Trust-gate

N/A — no auto-exec surface touched. `scripts/trust-gate.sh`'s `SURFACE_GLOBS` (`.claude/settings.json`, `.mcp.json`, `bin/sos.sh`, `hooks/pre-commit`, `hooks/pre-push`, `scripts/*.sh`, `install.sh`, `phieu/phieu.sh`, `templates/setup-dev.sh`) match none of this ticket's changed files (Rust crates + Markdown docs only). No rebaseline needed.

## Next

b1 done → **P078b2 (declarative render)** and **P078b3 (enforcement + guard-rewrite)** are next per the 3-phiếu decompose declared in this ticket's Context section.
