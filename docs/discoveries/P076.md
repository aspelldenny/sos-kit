# Discovery Report — P076 Claude adapter parity (declarative boundary)

## Anchors CORRECT / WRONG

All 13 Task 0 anchors verified CORRECT — no wrong assumption found.

- #1 ✅ `core/ROLES.md` — 6 stable headings confirmed: `## \`owner\`` (line 23), `## \`orchestrator\`` (36), `## \`architect\`` (52), `## \`worker\`` (69), `## \`advisory_watch\`` (85), `## \`boundary_check\`` (100).
- #2 ✅ `core/POLICY.md` + `core/WORKFLOW.md` exist.
- #3 ✅ `adapters/` absent pre-execute (`test ! -e adapters` passed before Task 1).
- #4 ✅ `.claude/settings.json` git mode `100644` confirmed (`git ls-files --stage`).
- #5 ✅ `.claude/commands/{advisory-scan,security-review}.md` mode `100644` confirmed.
- #6 ✅ `.claude/agents/**` + `.claude/skills/**` mode `120000`, symlink targets match golden section 2 exactly.
- #7 ✅ `agents/*.md` frontmatter (name/model/tools) matched golden section 3 byte-for-byte pre-execute AND post-annotation (re-checked after Task 3 edits — unchanged).
- #8 ✅ `skills/*/SKILL.md` frontmatter (name/caller) matched golden section 4 byte-for-byte pre- and post-execute.
- #9 ✅ `templates/claude-settings.local.json` exists.
- #11 ✅ `grep -rIl -E 'render\(|renderer' bootstrap/ bin/` → 0 hits, no render infra found (confirms (A) physical move has no tool available in-repo).
- #12 ✅ Provenance marker placement (BODY, right after H1, before first prose line) verified feasible on `agents/architect.md` and `skills/idea/SKILL.md` before executing; applied to all 11 target files; re-ran frontmatter grep after edits — 0 diff.
- #13 ✅ Golden baseline sections 1,2,3,4,6,8 re-run post-execute — all diff = 0 (see Golden parity result below).

## Golden parity result (section-by-section, post-execute)

| Section | Result |
|---|---|
| 1 — `.claude` tree + git modes | diff = 0 (11/11 entries match golden exactly, `100644`/`120000` unchanged) |
| 2 — Symlink topology | diff = 0 (9/9 symlink targets match) |
| 3 — Role frontmatter (agents) | diff = 0 (5/5 files, name/model/tools byte-identical; provenance marker is body-only) |
| 4 — Skills frontmatter | diff = 0 (5/5 files, name/caller byte-identical; provenance marker is body-only) |
| 5 — Slash commands | diff = 0 (not independently re-verified via new probe this run, but `.claude/commands/**` untouched per Files KHÔNG sửa — no edit made) |
| 6 — Hook wiring (`settings.json`) | diff = 0 (verified via `json.load` structural compare of `hooks` object vs golden extract — MATCH; `.claude/settings.json` file itself untouched) |
| 7 — `sos` CLI surface | out of scope (bin/sos.sh migration owner = P077 per `core/ASSETS.md` line 35, not touched) |
| 8 — MCP servers | diff = 0 (`.mcp.json` untouched, `doctor` server confirmed present) |
| 9 — doctor connectivity | out of scope for this ticket (no static Claude artifact changed; `doctor` binary itself unaffected) |

No regression found. Only intentional changes shipped: new `adapters/claude/**` files, body-only provenance comments (11 files), and the 5 DOCS-GATE doc edits.

## Scope note: MCP row added (Worker CHALLENGE completeness finding)

CHALLENGE turn flagged that `.mcp.json` (MCP server `doctor`) — an ADAPTER_OWNED artifact per `core/ASSETS.md` line 49 ("optional protocol-server registration") — had no row in the Task 2 minimum-rows table. Added a `.mcp.json` row to `adapters/claude/MAPPING.md` per orchestrator's EXECUTE instruction. Not a scope expansion — Task 2 explicitly said "minimum rows," this stays inside the same file/task.

## Mid-execute self-adapt (Tầng 2 — wording only)

Task 4's literal instruction text for `core/ASSETS.md` said to add a note containing the string `adapters/claude/MAPPING.md`. Applying that literally created 2 NEW hits for `grep -rn 'adapters/' core/ SOS.md` (confirmed zero pre-existing hits in HEAD via `git show HEAD:core/*.md`), which directly fails the phiếu's own explicit Regression check ("Không thêm reference core→adapters (constraint 7)"). Resolved by rewording the `core/ASSETS.md` note to describe the boundary conceptually ("Claude integration maintains its own artifact-mapping manifest") without the literal `adapters/` path string — preserves Task 4's informational intent (P076 DECLARED, mapping exists, physical render P077) while satisfying constraint 7 + the regression oracle (re-ran grep post-fix: 0 hits). This is Tầng 2 (wording-only, doesn't touch architecture/dependency-direction, which is the higher-priority rule already stated explicitly in the phiếu) — no escalation needed, self-decided per Worker's local-reversible-choice authority.

## Docs updated (Tầng 1 list)

- `docs/LAYERS.md` — access-matrix note added (adapter boundary, values unchanged).
- `docs/HANDOFF.md` — Handoff 2 boundary note added (format unchanged).
- `core/ASSETS.md` — transitional-mixed rows for `agents/**`/`skills/**` updated with P076 DECLARED note (worded to avoid core→adapter reference, see self-adapt above); Migration owner range (`P076-P078`) unchanged per Task 4 instruction.
- `CLAUDE.md` — repo-tree `adapters/` entry added under `.claude/`; "Not a runtime binary source" contract left untouched (P077 owns).
- `CHANGELOG.md` — P076 entry added.

## Envelope (B) executed

Sếp confirmed **(B) declarative boundary** at the phiếu's ESCALATE GATE. Zero physical file move performed. `adapters/claude/README.md` + `MAPPING.md` created (declarative mapping only). 11 files got inert body-only provenance comments. No renderer written. All constraints 1-8 held.

## Trust-gate check

`bash scripts/trust-gate.sh` → exit 0, no drift flagged. Confirmed `agents/*.md` and `skills/*/SKILL.md` are NOT among the 21 tracked auto-exec surfaces in `.sos-trust-baseline` (only scripts/hooks/settings.json/.mcp.json/bin/sos.sh/install.sh/phieu.sh/setup-dev.sh) — provenance-comment edits don't touch any baselined surface. No rebaseline needed.

## Tier escalations

None. Phiếu stayed Tầng 1 throughout (as declared) — no further escalation triggered mid-execute beyond the wording self-adapt above (which is Tầng 2 by definition — it doesn't change architecture, schema, API, or security boundary; it only changes doc prose to comply with the phiếu's own already-stated constraint).
