# P043 — Doc drift consolidate — Discovery Report

**Date:** 2026-05-25
**Phiếu:** P043-doc-drift-consolidate
**Commit:** `docs(P043): consolidate Quản đốc persona codify + alignment engineering + deferred-tool loading`

---

## Tarot Read access at EXECUTE

Worker had access to `~/tarot/docs/PHILOSOPHY.md` + `~/tarot/.claude/agents/orchestrator.md` (tarot orchestrator is in `.claude/agents/`, not `agents/`).

**PHILOSOPHY.md:** Tarot's version is largely identical to sos-kit's — same "alignment engineering" section verbatim. No additional concrete failure examples beyond the skeleton Architect provided. Architect skeletons were complete standalone. **Path taken: Architect skeleton used** (Tầng 2 self-decide — tarot content added no new substance for sos-kit's public docs).

**orchestrator.md (tarot):** 142 lines. Key additions in tarot vs sos-kit current: Chị Hạ domain triggers, prompt-reviewer gate, security boundary gate — all tarot-specific, out of scope for sos-kit. The 3 new subsections (Greeting turn template / Tier priority routing rationale / Session opening script) had no tarot-specific equivalents. Architect skeletons used and self-contained.

---

## Persona discriminator edge cases

Worker encountered the following ambiguous "Kiến trúc sư" references during cross-ref pass:

1. **phieu/active/P043-doc-drift-consolidate.md** — The phiếu itself quotes old strings (e.g. `"Em là Kiến trúc sư project <name>"`) as find-targets in Task 3/4 "Tìm" blocks. These are historical context inside the phiếu contract, not operational docs. Default rule applied (do not rename). Phiếu is immutable per constraint.

2. **docs/ticket/P003-backlog-format-flexibility.md** — Historical archived phiếu contains old greeting literal. Per scope constraint ("Bất kỳ `phieu/done/*.md` archive — không rewrite history"), not touched.

3. **docs/HANDOFF.md line 7 (v1 context):** "Kiến trúc sư (Claude Web Project)" — correctly refers to subagent identity in Web Project mode. NOT renamed (persona discrimination correct).

**Nghiệm thu note:** `grep -rn "Em là Kiến trúc sư" .` returns matches in the phiếu archive and P043 phiếu file. These are expected/correct; all **operational** docs (ORCHESTRATION.md, orchestrator.md, HANDOFF.md) return 0 for the greeting literal.

---

## ToolSearch syntax verified

Worker could not test `ToolSearch query="select:..."` via Bash (tool invocation syntax is Claude Code runtime, not shell-testable). The syntax documented in `agents/orchestrator.md` and `CLAUDE.md` matches the pattern documented in the system-reminder context for this session (`ToolSearch query="select:AskUserQuestion,TaskCreate,TaskUpdate"`). **Assumed correct** per session context evidence. If a future session finds syntax differs, update both files.

---

## Line cap decision applied

- **Task 5 Tìm 3 + Task 6b Tìm 3 (same edit):** Applied once — `CLAUDE.md:149` now reads `≤105 cap`.
- **Final `wc -l agents/orchestrator.md`:** 100 lines (within ≤105 cap).
- Initial insertion produced 108 lines → Tầng 2 self-adapt: compressed deferred-tool section from 17 to 10 lines (removed blank lines, merged rationale bullets into tighter prose). No Tầng 1 content changed — only whitespace/verbosity. Logged here per Discovery protocol.

---

## Cross-ref completeness

Worker spot-checked out-of-scope docs for blocking inconsistency:

- **docs/SETUP.md:** No "Em là Kiến trúc sư" greeting literals. References "Kiến trúc sư" as subagent role name (correct). No change needed.
- **INSTALL.md:** No main-session-persona literals found.
- **docs/COMPARISON.md:** References "Kiến trúc sư" as role name only. No change needed.
- **docs/GENESIS.md:** No persona literals found.
- **skills/plan/SKILL.md + skills/init/SKILL.md:** Reference "Kiến trúc sư" as subagent layer. Correct — skills belong to role, not persona.

**No P044-candidate silent fixes.** All out-of-scope references to "Kiến trúc sư" are subagent-identity references (correct per constraint: NOT renamed).

---

## Dangling staged files commit

`agents/orchestrator.md` (Sếp's 2-line edit, staged pre-P043) + `docs/BACKLOG.md` (Quản đốc's sprint update, staged pre-P043) committed together with P043 new edits in single PR.

Final commit message: `docs(P043): consolidate Quản đốc persona codify + alignment engineering + deferred-tool loading`

---

## CHALLENGE round value

**Yes — CHALLENGE caught a real Tầng 1 gap.** Worker CHALLENGE (Turn 1) identified that the phiếu V1 had no concrete `Thay bằng` for `CLAUDE.md:149` cap raise, leaving Worker to self-decide on a Sếp-decided matter. Architect RESPOND (V2) added concrete Tìm 3 / Thay bằng 3 block. CHALLENGE investment: 1 turn, caught 1 real O1.1. **Signal: positive** — Tầng 1 docs-only phiếu with Sếp-decided parameters benefited from CHALLENGE loop.

---

## Docs updated to match reality

- `CHANGELOG.md` — new v2.2.1 entry added.
- `docs/DISCOVERIES.md` — index row for P043 added.
- All 7 target docs per phiếu scope updated (LAYERS.md, PHILOSOPHY.md, ORCHESTRATION.md, agents/orchestrator.md, CLAUDE.md, README.md, docs/HANDOFF.md).
