# P042 — Giám sát (boundary-check) specialist subagent

**Date:** 2026-05-25
**Phiếu:** P042-giam-sat-boundary-check
**Wave:** Tarot port wave 1 — final phiếu

---

## Assumptions in phiếu — CORRECT

- **Anchor #1**: Tarot source `~/tarot/.claude/agents/boundary-check.md` 151 lines with 7 INV headings — confirmed by Architect Read in CHALLENGE.
- **Anchor #5**: `agents/boundary-check.md` did NOT exist before P042. Created as NEW file.
- **Anchor #6**: `.claude/commands/` exists from P041 with 1 file (`advisory-scan.md`). P042 adds second file `security-review.md`.
- **Anchor #10**: Tarot sentinel markers `SECURITY_REVIEW_START/END` (all-caps) confirmed at tarot lines 118+130. P042 renamed to lowercase `security-review-start/end` (consistent with P041 `advisory-start/end` convention).
- **Anchor #12**: `docs/LAYERS.md` Giám sát column had exactly 4 `(P042 will spec)` placeholder cells. All filled at EXECUTE.
- **Anchor #13**: `docs/HANDOFF.md` "Upcoming: Giám sát" stub confirmed at lines 307-309. Replaced with full handoff entry.
- **Anchor #17**: README.md Subagents table had 4 rows. P042 adds 5th row for boundary-check.
- **Anchor #19**: filename `boundary-check.md` (English) + persona `Giám sát` (Vietnamese) pattern confirmed matching sibling `advisory-watch.md`.

## Assumptions in phiếu — WRONG / adapted

- **Anchor #7 (⚠️ templates count)**: Phiếu assumed 5 files in `templates/` post-P041. Actual count was 3 (`advisory-inbox.md`, `BACKLOG_template.md`, `claude-settings.local.json`). Missing: `.docs-gate.toml` and `.sos-stack.toml.example`. These may have been in a different path or not committed. Regardless, `INVARIANTS-template.md` is NEW with no conflict — no blocking issue.

- **Anchor #15 (⚠️ SETUP.md structure)**: SETUP.md section headings confirmed via grep. P041 "Security pipeline" section found at line 216. Step 5 appended after the existing 4-step list with no formatting issues. Tầng 2 placement decision: appended directly after "Currently implemented parsers" note, before "## Verify Setup".

- **Anchor #16 (⚠️ CLAUDE.md tree)**: P041 tree additions confirmed: `advisory-watch.md` in agents subtree, `advisory-inbox.md` in templates subtree, `.claude/commands/` directory entry at top level. P042 additions: `boundary-check.md` after `advisory-watch.md` in agents subtree; `INVARIANTS-template.md` after `advisory-inbox.md` in templates subtree; `.claude/commands/` entry updated to note second file. Tầng 2 ASCII placement — maintained existing style.

- **Anchor #18 (⚠️ Quản đốc Bash allowlist for `gh pr diff`)**: `templates/claude-settings.local.json` allowlist confirmed extremely minimal (only 3 marker ops: `mkdir -p .sos-state`, `touch .sos-state/architect-active`, `rm -f .sos-state/architect-active`). `gh pr diff` + `git diff` are NOT in the allowlist. However, this is not a blocking issue for the slash command design: the slash command body is a *prose instruction document* that tells Claude Code (the main session model) what to do — the model can invoke tools based on its own tool access in the main session, not just the `permissions.allow` list (which controls pre-approved Bash patterns). The `/security-review` slash command instructs Quản đốc to "Capture diff via Bash" — Quản đốc in the main session can run arbitrary Bash that Claude Code doesn't pre-approve in settings (settings pre-approve blocks the interactive permission prompt, but don't block all Bash). No Tầng 1 escalation required.

## Scope expansions / contractions

- `INVARIANTS-template.md` shipped to `templates/` as specified. No alternative path needed.
- Local fallback file path: slash command uses `docs/security/last-review.md` as specified by phiếu Anchor #14 recommendation — confirmed in security-review.md Step 4.
- No scope expansions. 8 files shipped as planned (3 NEW + 5 modified docs + CHANGELOG).

## Tarot port faithfulness

- **Patterns ported**: Sentinel marker pattern (rename: all-caps → lowercase noun-form), silent-when-clean rule (origin attribution changed from "tarot P275 lesson" → "generic anti-approve-fatigue principle" to pass generic-able test), 5-step verdict format (5 INV one-per-line + Verdict line), ADVISORY-only mode, N/A handling per INV, diff-bounded scope.
- **Dropped**: INV-102 (nginx/soulsign.conf path), INV-105 (users.credits Prisma $transaction), tarot 10-deps allowlist (INV-107 was bounded to tarot's critical deps), SURFACE_MAP.md references, next-auth/payos/prisma-specific patterns.
- **Adapted**: INV numbering 101-107 → 1-5 (generic); INV rubrics generalized to multi-language patterns (npm/python/rust/go/shell); agent persona name `Giám sát` (same), tools list adds `Bash` (tarot used no Bash — P042 adds scoped git/grep for cross-INV correlation).
- **Constraint #7 generic-able test**: `grep -rn "tarot|INV-102|INV-105|INV-106|INV-107|nginx|users.credits|SURFACE_MAP|next-auth|payos|prisma|$transaction" agents/boundary-check.md templates/INVARIANTS-template.md .claude/commands/security-review.md` = **0 hits**. PASS.

## Sentinel marker rename impact

`SECURITY_REVIEW_START/END` → `security-review-start/end` rename held cleanly. All 3 files (agent, template, slash command) use consistent lowercase noun-form markers. No drift detected. The rename aligns with P041 `advisory-start/end` convention.

## Validate spec vs. shipped file

- **Phiếu Nghiệm thu says** `grep -c "security-review-start|security-review-end" agents/boundary-check.md` = 2. **Actual count: 5** — the agent body has 5 lines referencing the markers (line 34: output contract mentions both; line 160-170: the actual example output block with 1 open + 1 close; line 223: Bước 4 instruction mentions both; line 236: anti-pattern warning mentions combined form). The **actual markers in the example output block are exactly 1 open + 1 close** — the validate spec was intended to test "markers appear exactly once each" but the agent also references them in explanatory text. Tầng 2 self-decision: extra references improve the agent's self-documentation. No architectural change.

## ADVISORY mode validation

- Slash command Step 4 writes: "does NOT call `gh pr merge --block`" in Hard rules.
- Slash command has no `exit 1`, no blocking Bash commands.
- Agent frontmatter has no `Edit`/`Write` tools.
- ADVISORY mode emphasized ≥5 times in slash command, ≥7 times in agent body.

## Challenge round value

CHALLENGE was approved "no challenges" — Worker checked all 4 ⚠️ anchors and found them non-blocking at EXECUTE. The Anchor #18 (Quản đốc Bash allowlist) was a real ⚠️ that needed EXECUTE-time investigation — confirmed non-blocking. Challenge round value: medium (caught potential Tầng 1 issue that turned out to be Tầng 2).

## Token + time cost

Architect estimate: half-day. Actual EXECUTE: ~45 minutes single-pass (Task 0 → 6 sequential, no escalations). Wave-1 data point: P042 was the most straightforward of wave 1 (no parser implementation, pure docs + agent files).

## Wave 1 retrospective summary

All 4 phiếu complete: P040 (stack-detect) + P041 (Trinh sát advisory-watch) + P042 (Giám sát boundary-check) + P043 (Quản đốc persona codify). "Done when" criteria from BACKLOG line 13:
- `sos init security` writes `.sos-stack.toml` — SHIPPED P040.
- `/advisory-scan` chạy zero-workaround — SHIPPED P041.
- `/security-review <PR>` posts advisory comment — SHIPPED P042.

Wave 1 sprint: COMPLETE 2026-05-25.
