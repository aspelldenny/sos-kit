# P078d2b Discovery Report — Codex in-subagent enforcement declared MISSING (#4, probe-confirmed); SubagentStart-marker deprecated best-effort

## Anchor verification (Task 0, all 8)

| # | Anchor | Result |
|---|---|---|
| 1 | Codex 0.145.0 does NOT fire `SubagentStart`/`SubagentStop` for custom-role spawn; only `agent_type="default"` fires. Upstream `openai/codex#21753`. | ✅ CORRECT — `docs/adapters/SUBAGENT-HOOK-PROBE-2026-07-22.md` (live probe payload capture). Cross-confirmed by `docs/adapters/P079-CODEX-DOGFOOD-FINDINGS-2026-07-22.md` #4 (direct dogfood evidence, not just inference: forbidden `apply_patch` succeeded in-subagent). |
| 2 | `verify()` returns 5 `Finding`s with `FindingStatus` | ✅ CORRECT — `crates/sos-adapter-codex/src/lib.rs:116-171` (pre-change). |
| 3 | Count-oracle test asserts exactly 5 Findings | ✅ CORRECT — `verify_reports_exactly_five_gaps_none_sound` (pre-change, renamed to `_six_` post-change). |
| 4 | SubagentStart/Stop marker render-site in `templates.rs` | ⚠️ DRIFT confirmed as predicted — actual site is `templates.rs:335-341` (inside the `hooks_json()` raw JSON string literal, function starts `:320`), NOT `:302`/`:315-317` cited in the brief (those were pre-P078d2a line numbers, since shifted). Comment placed as a Rust `//` block immediately above the `r#"{` literal start (line ~320-338), not inside the JSON itself (JSON has no comment syntax) — zero JSON byte change. |
| 5 | `agents_md()` orchestrator guidance insertion point | ⚠️ DRIFT confirmed as predicted — function is `templates.rs:164-182` (not `:145-162`); insertion landed at line 177-178, right after the P079 #7 spawn-caveat bullet and before `Full role contract:`. |
| 6 | `CAPABILITY.md` seeded from `verify()`, 5 gap sections + P078b3 status | ✅ CORRECT (Architect's read). New §6 inserted after the existing "Enforcement-weakness note" (end of §5 area), before "## P078b3 enforcement status" — matches the phiếu's requested placement. |
| 7 | `SECURITY.md` has an existing "Codex adapter enforcement (rendered to target, PARTIAL)" section | ✅ CORRECT — found at `SECURITY.md:88`. New in-subagent-MISSING paragraph appended at the end of that section (after the approval-gate self-bootstrap paragraph), before "## Install: tool-version drift (OA-07)". |
| 8 | `docs/PORTABILITY_ARCHITECTURE.md` enforcement section needs "MISSING (in-subagent)" note | ✅ CORRECT (exact section) — found the P078b1/b2/b3/c status-paragraph block at lines 58-64; no prior P078d1/d2a paragraphs existed there (pre-existing gap, out of this phiếu's scope — those phiếu evidently didn't add a paragraph here). Added a new P078d2b status paragraph after the P078c paragraph (line 64), before "## Ownership và dependency direction" (line 66), matching the existing style. |

**No anchor was ❌.** No re-probe performed (Worker does not have Codex 0.145.0 installed on this dev machine) — relied on the committed probe + dogfood evidence per Task 0 instructions; premise held (no escalation triggered).

## Assumptions in phiếu — CORRECT
- Root cause (custom-role hook non-dispatch, `#21753`) — confirmed by probe file, and independently corroborated by direct dogfood evidence (P079 #4), which is stronger support than "inference from a probe" — MISSING classification is not an over-claim.
- `FindingStatus` enum already has `Missing` variant — no enum change needed.
- `hooks_json()` is a raw JSON string literal — comment must go as a Rust `//` block before the literal, not inside the JSON (which has no comment syntax). Zero-byte-change approach chosen (no `description` field edit) — this keeps all existing d1 schema-shape tests (`hooks_json_is_valid_json_with_expected_events`, `hooks_json_top_level_keys_are_description_and_hooks_only`) passing unmodified, and no re-run of the d1 regression suite beyond the normal `cargo test` was needed.
- Main-thread guards + Git pre-commit/pre-push + AGENTS.md guidance are the 3 real backstops — all independently verified: main-thread guards dogfood-confirmed (P079 SOUND section: "MAIN-THREAD guards ENFORCED"), Git hooks exist at `hooks/pre-commit`/`hooks/pre-push` (agent-agnostic, git-boundary — fires regardless of which CLI/runtime authored the diff), AGENTS.md guidance correctly declared prose-only.

## Assumptions in phiếu — WRONG / adjusted (Tầng 2, self-adapted)
- Line-number anchors for Task 2 (`templates.rs:302`/`:315-317`) and Task 5 (`templates.rs:145-162`) were stale (both phiếu and dogfood-finding citations predate P078d2a's edits, which shifted lines). This was explicitly anticipated by the phiếu's own `[needs Worker verify]` markers and Worker's CHALLENGE Turn 1 — no surprise, no escalation. Correct sites used instead (`:320-338` for the hooks_json comment, `:164-182`/`:177-178` for agents_md).
- No Tầng 1 escalations — premise held throughout.

## Scope expansions
None. Strictly the 6 files listed in the phiếu ("Files cần sửa") plus this Discovery Report and the DISCOVERIES.md index line — no other file touched.

## Edge cases / limitations found
- `docs/PORTABILITY_ARCHITECTURE.md` had no existing P078d1/P078d2a status paragraphs (only up through P078c) — pre-existing gap from prior phiếu, not something P078d2b was scoped to backfill. Noted here for future visibility; not fixed (out of scope, additive discipline).
- The deprecation comment on the SubagentStart/Stop marker is Rust-source-only (not embedded in the rendered `.codex/hooks.json` `description` field) — a target-project adopter reading only the rendered JSON output will NOT see the deprecation note; they'd need to read `CAPABILITY.md`/`SECURITY.md`/`verify()` output for that. This is the deliberate trade-off chosen by the phiếu (zero-byte-change priority over in-artifact visibility) — flagging honestly, not silently.

## Deprecate-marker: final choice
**GIỮ (kept) + Rust-comment-only** (not a JSON `description` edit). Rationale matches the phiếu's stated priority order: (a) additive/least-destructive, (b) auto-activates if `#21753` is fixed upstream, (c) DEFAULT-subagent marker still genuinely fires so the code isn't fully dead. Zero JSON bytes changed — `hooks_json_is_valid_json_with_expected_events` and `hooks_json_top_level_keys_are_description_and_hooks_only` both pass unmodified, confirming no d1 regression.

## #4 handoff CLOSED
Probe (`SUBAGENT-HOOK-PROBE-2026-07-22.md`) definitively identified root cause as an upstream Codex CLI regression/gap (`openai/codex#21753`), not a sos-kit bug. Marker-via-`SubagentStart`-hook is confirmed dead for custom-role spawns. Honest MISSING declaration landed on 3 surfaces: machine (`CodexAdapter::verify()` Finding #6), human-capability (`adapters/codex/CAPABILITY.md` §6), and threat-model (`SECURITY.md`). **P078d is now DONE** (d1 startup-schema + d2a multi-path-guard + d2b MISSING-declaration).

## trust-gate SECURITY.md rebaseline
**Not run — not required.** `SECURITY.md` is in `scripts/trust-gate.sh`'s `UNICODE_SCAN_DIRS` (hidden-unicode scan only), but NOT in `SURFACE_GLOBS` (the sha256 baseline-diff list) — confirmed by reading `scripts/trust-gate.sh:36-45` directly. `bash scripts/trust-gate.sh` (the default check subcommand) run after all edits: exit 0, no drift/unicode flags. No `.sos-trust-baseline` change needed or made.

## Tier escalations
None. No ❌/⚠️ anchor blocked implementation; no architectural conflict encountered mid-EXECUTE; premise (probe root cause) held throughout — no re-probe contradicted it.

## Docs updated (Tầng 1 mapping)
- `adapters/codex/CAPABILITY.md` — new §6.
- `adapters/codex/MAPPING.md` — "5 Findings" → "6 Findings" (line 31).
- `SECURITY.md` — new in-subagent-MISSING paragraph in the existing "Codex adapter enforcement" section.
- `docs/PORTABILITY_ARCHITECTURE.md` — new P078d2b status paragraph.
- `CHANGELOG.md` — new `[P078d2b]` entry.
- `docs/DISCOVERIES.md` — index line appended (this phiếu).

## Oracle results (structural, declaration-only — NOT behavioral)
- `cargo test -p sos-adapter-codex`: 48/48 pass (was 46 pre-change + 2 new: `verify_reports_exactly_six_gaps_none_sound`, `verify_declares_in_subagent_enforcement_missing`).
- `cargo build --workspace` + `cargo test --workspace`: green, no regressions in `sos-core`/`sos-adapter-claude`/`sos-install`/`sos-hooks`/`sos-cli`.
- ×20 loop `cargo test -p sos-adapter-codex`: 20/20 "test result: ok" — 0 flaky.
- Dep-direction guard: `grep -rn "adapters/|\.codex|apply_patch" crates/sos-core/src/` → 0 hits.
- Additive verified: `git diff --stat crates/sos-install/src/engine.rs crates/sos-core bin/sos.sh install.sh` → empty. `git diff crates/sos-adapter-codex/src/templates.rs` shows ONLY the 2 added comment/guidance blocks (AGENTS.md bullet + hooks_json comment) — no d2a guard content (`architect-guard.sh`/`orchestrator-guard.sh`/`block-env-edit.sh`/`approval-gate.sh` render-fns, multi-path fix logic) touched.
- d1 render regression: `hooks_json_is_valid_json_with_expected_events` and `hooks_json_top_level_keys_are_description_and_hooks_only` both PASS unmodified — declaration did not break render.
- **No behavioral test performed** — `openai/codex#21753` (custom-role hook non-dispatch) is upstream and out of scope; no live Codex re-probe attempted or required (premise already probe- and dogfood-confirmed).
