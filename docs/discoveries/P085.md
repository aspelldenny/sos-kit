# P085 — Codify phiếu-decomposition heuristic in ORCHESTRATION.md

**Status:** shipped (docs-only, additive, guidance).

## Assumptions — Task 0 anchors

All 5 CORRECT:
1. `WORKFLOW_V2.2.md` §1 Lane budgets `[gate]` at line 59, §0.1 Luật 3 at line 50-55 — confirmed.
2. `core/WORKFLOW.md:75-81` one-delivery-unit — confirmed verbatim.
3. `ORCHESTRATION.md` "Tier routing (P036)" 157-170, "Trigger phrases" at 172 — confirmed, section inserted exactly between the two.
4. Example phiếu IDs (P077c1-c6, P078a, P078b3, P078d2a, P078d2b) all exist in `docs/ticket/` and their real content matches the cited framing:
   - P077c: c1-c4 assert parity (`Rust == Bash`), c5 asserts correctness (`Rust != buggy Bash`) — the "incompatible oracles" example holds.
   - P078d2a/d2b: d2a ships now (#5/#6/#7), d2b explicitly deferred pending a `SubagentStart` probe result — the "external-input blocker" example holds.
   - P078b3, P078d2a: both headers explicitly declare "AUTO Tầng 1... security surface" — the "security-surface isolation" example holds.
   - P078a: single cohesive spec doc, no internal split — the "keep-whole" example holds.
5. `agents/orchestrator.md` Tier routing 45-51, Hard rules 155 — confirmed; pointer inserted after line 51 (not into Hard rules list).

## Tầng 1 docs updated

ORCHESTRATION.md (new section) + agents/orchestrator.md (1-line pointer) + CHANGELOG.md (P085 entry) — contract-surface add per CLAUDE.md Rule #9 (not cosmetic). CLAUDE.md itself: N/A, no row maps to "ORCHESTRATION add guidance section" and no phase-count/gate-inventory changed.

## Example citation check

No sing ID fabricated. All 4 examples verified against real phiếu file content (not just filename existence) before shipping — see Anchor #4 detail above.

## §1-consistency

Section heading itself carries "(guidance, not a gate)"; item #1 explicitly labeled "the only criterion with a hard gate"; closing paragraph restates "Lane budget is the only hard gate." No second gate implied anywhere in the new text. `agents/orchestrator.md` mirror is pointer-only (not added to numbered Hard rules list) — single-source stays ORCHESTRATION.md.

## Tier escalations

None — stayed Tầng 1 (as Architect set), docs-only, no code/schema/API touched.

## Iterability note

Per Sếp's 2026-07-22 decision: this heuristic is explicitly "fix nhẹ sau" / iterate freely — not frozen doctrine, no retro process required to adjust (lives in ORCHESTRATION.md, not WORKFLOW_V2.X.md).
