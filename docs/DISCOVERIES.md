# Discoveries — sos-kit

> **Index file.** Per-phiếu Discovery Reports live at `docs/discoveries/P<NNN>.md` (one file per phiếu). This file = chronological index, newest on top.
>
> **Why per-file:** monolithic file phình to 100k+ bytes after ~30 phiếu. Architect's auto-Read of full file wastes tokens on irrelevant content. Per-phiếu = Architect Read selective (only when current phiếu references the same component).
>
> **Pre-2026-05 entries:** archived to `docs/archive/DISCOVERIES_pre-2026-05.md`. Migration date: 2026-05-02 (P038).

> **P077d3 → P076 entries** (2026-07-22 wave) archived to `docs/archive/DISCOVERIES_P077d3-to-P076.md` on 2026-07-23 to keep this file under the 40k doc-size threshold.

## 2026-07-24 — P088: Windows checkout/EOL layer — CRLF-safe parity goldens + symlink-stub detection — SHIPPED
ITEM 1: `.gitattributes` extended to force LF for `*.golden` (direct fix, `parity_sync/new/adopt_enforced`) + `*.toml`/`*.json`/`*.md`/`*.yaml` (closes P087 BUG 2's CRLF-hash-mismatch class structurally). Every git shell-out in `new.rs`/`adopt.rs` (init/symbolic-ref/add) now passes `-c core.autocrlf=false` — stops the `warning: ... LF will be replaced by CRLF` line leaking into `run_rust()`'s combined stdout+stderr capture, the 2nd cause of the same 3 parity fails. Existing checkouts need `git add --renormalize .` then, if files still show CRLF on disk, delete + `git checkout --` to force re-materialize (renormalize alone only updates the index, not working-tree bytes — documented in SETUP.md/INSTALL.md). **Discovery mid-EXECUTE:** `parity_adopt_enforced` had 2 EXTRA native-path-separator bugs beyond CRLF, same class as P087 BUG 1 — `is_noise()`/skills-remap's `/attic/`+`/__pycache__/` substring checks never matched Windows's `\` separator (fixed via `Path::components()`), and the "diff staged wiring" hint line rendered `PathBuf::display()` (native sep) instead of the raw `&str` target arg. Both fixed — Task 0 anchor #3's "CRLF-only" framing was only partially right. ITEM 2 (Chủ nhà-decided option (c) hybrid at APPROVAL_GATE): kept `.claude/{agents,commands,skills}` symlink convention, added mechanical `find_symlink_stubs`/`warn_symlink_stubs` (in `new.rs`, reused by `adopt.rs`) scanning both the `SOS_KIT_DIR` source and scaffolded target for text-stub-instead-of-symlink checkout, printing the Developer Mode + `core.symlinks true` fix. Debate Log O1.1 re-verified at EXECUTE: its cited `new.rs:310` evidence was imprecise (that line sources canonical `skills/`, not `.claude/skills` — no symlinks there); the REAL exposure is `.claude/agents`/`.claude/commands` via `copy_tree(deref=true)` in both `new.rs` and `adopt.rs` — check scope corrected accordingly (still scans all 3 subdirs). Final: `cargo test --workspace` full green, all 10 `parity.rs` tests pass, zero regressions. Full report: `docs/discoveries/P088.md`.

## 2026-07-24 — P087: Windows runtime bug-fix wave — `sos map` scanner dead + `trust-gate.sh` false-BLOCK — SHIPPED
Windows dogfood 2026-07-24 (45 pass / 6 fail, all 6 in `crates/sos-cli/tests/parity.rs`) — P087 closed 2 of 6. **BUG 1** (`map.rs`): `path_str` (native `\` separator) fed straight into POSIX-style substring match (`SURFACES[].path_substrs`, `NOISE_EXCLUDE`) — never matched on Windows, so noise dirs (`.git/`, `node_modules/`) were never excluded and no dir-pattern surface was ever detected. Fixed by forward-slash-normalizing `path_str` at both call sites (`scan_surface`, `detect_present_stacks`) before `.contains()`/`is_noise()` (`is_noise()` stays `&str`-typed — normalized at callers, not inside). New `display_out_path()` helper renders the stdout confirmation line forward-slash after the `target` arg while keeping `target` itself byte-identical (parity harness substring-replaces the literal CLI arg with `<TARGET>` — blanket-normalizing broke that substitution). **BUG 2** (`trust-gate.sh`): GNU coreutils `sha256sum` on Windows/Git Bash emits binary-mode `HASH *PATH` while the baseline was seeded text-mode `HASH  PATH` — every hash line diffed regardless of content, false-BLOCKing `sos new`'s first commit at hook `[8/8]`. Fixed inside `hash_file()` (single wrapper both generate/compare branches call, per Debate Log Turn 1's DRY-er single-point option) with an awk strip of the leading `*`; `.sos-trust-baseline` rebaselined. **Discovery mid-EXECUTE:** fixing BUG 1 unmasked a 2nd, previously-hidden assertion failure in `parity_map_enforced` — pure `.gitattributes`/CRLF golden-checkout mismatch (byte-identical content, `\n` vs `\r\n` only), same class as the phiếu's own named-excluded `parity_new/adopt/sync_enforced` (deferred to P088). Did NOT touch `.gitattributes` (explicit scope boundary) — documented as a 4th CRLF-only expected-red test. Final: `cargo test --workspace` 6 pass / 4 fail in `parity.rs` (all 4 = CRLF class), zero other regressions; `oa02_templates_excluded_from_frontend` + `new_first_commit_passes_all_hooks_zero_seed` now green. Full report: `docs/discoveries/P087.md`.

## 2026-07-24 — P086: seed `.sos-trust-baseline` in born-wire (fix first-commit deadlock) — SHIPPED
Linux dogfood L3 (HIGH): a freshly `sos new`'d or `sos adopt`ed repo could never make its FIRST commit — `hooks/pre-commit [8/8]` trust-gate fail-CLOSED on `.sos-trust-baseline not found`, and nothing nudged the user toward the manual `scripts/trust-gate.sh rebaseline` fix. `new.rs`/`adopt.rs` (+ bash oracle `bin/sos.sh` `sos_new`/`sos_adopt`) now stage → rebaseline → stage-baseline in born-wire (order load-bearing: trust-gate's enumerator is `git ls-files`). `adopt` scopes staging to a threaded `added_paths: Vec<PathBuf>` accumulator (never `git add -A` on brownfield — could sweep the user's own WIP) and only seeds when hook-arming actually succeeded; F09-decline gets a manual-rebaseline heads-up line instead. Found + fixed mid-EXECUTE: seed step was initially placed before `.sos-stack.toml` existed on disk (arm-hooks block runs before `init_security()`) — moved after, gated on a `hooks_armed` bool. 3 small fixes riding the same branch: **L1** order-determinism (`adopt_item`/`adopt_skills` dir-walks now sorted, both Rust and bash — fixes Linux `parity_adopt_enforced` FAIL from unsorted readdir order); **L4** `.sos-stack.toml`'s `sos_kit_version` stale `"P040"` tag → real crate version (`env!("CARGO_PKG_VERSION")` / grepped `Cargo.toml`); **L5** `docs/ORCHESTRATION.md` added to `new`'s spine (was already in `adopt`'s/`sync`'s, missing from `new`'s). Reproduced the deadlock live pre-fix (both `new` and `adopt` paths, exit 1 BLOCKED), all workspace tests green incl. 2 new acceptance tests against the REAL kit spine (`new_first_commit_passes_all_hooks_zero_seed`, `adopt_does_not_stage_users_untracked_file`), 3 manual scenarios verified (new+commit exit 0, adopt+commit exit 0, F09-decline nudge line present). Full report: `docs/discoveries/P086.md`.

## 2026-07-23 — P081b Stage 2: npm wrapper — BUILT + DOGFOODED, PENDING-publish
`package.json` (`sos-kit` 0.1.0, thin, `bin.sos`→`bin/sos-npm`+`sos-kit-setup` fallback, os darwin/linux only); `scripts/npm-postinstall.sh` (pin tag `v0.1.0` + sha256-verify-then-exec `install.sh`, fail-CLOSED, no forked logic); `bin/sos-npm` (thin delegate, missing-target → guidance + exit 1). Anchor #7 blocking-check resolved: `install.sh@v0.1.0` confirmed HAS route-A sos-bin, `git diff v0.1.0 main -- install.sh` = NO DIFF (zero tag-drift risk). Real bug found+fixed mid-execute: npm installs `bin` as symlinks, `dirname "$0"` alone resolved wrong dir — added symlink-resolving `_resolve_self()`. Nghiệm thu ALL GREEN in TRUE-isolated prefix (careful 2nd pass caught a PATH-leakage false-positive from the first pass): pack→install rc=0, 6 required tools + sos-bin + wrapper present (3 of 4 optional tools MISSING as EXPECTED — those repos still private, non-fatal per install.sh design), `sos tools status` rc=0, `sos-bin --version`→`0.1.0`, checksum-tamper→abort exit 1, `--ignore-scripts`→clean no-op + fallback `sos-kit-setup` verified working. Docs: INSTALL.md/README.md npm path, SECURITY.md new npm threat-model subsection, `.sos-trust-baseline` rebaselined, CHANGELOG, BACKLOG (un-parked + publish split to its own Park item). **NOT published/pushed/tagged** (Worker boundary). Full report: `docs/discoveries/P081b.md`.

## 2026-07-23 — P081 Stage 1: release pipeline + checksum + curl|sh — CODE DONE, PENDING-release
`.github/workflows/release.yml` (tag `v*` → macOS arm64 + Linux build-only, sha256/asset, draft→publish, reused `~/ship` fleet pattern); `tool-manifest.toml` FULL checksum fill (all 10 sister tools real sha256, only `advisory-cron` Windows triple TODO by design); `install.sh` route A (sidecar `sos-bin` fetch fail-CLOSED + wrapper `SOS_RUST_BIN` precedence-safe export, `bin/sos.sh` untouched); `sos-cli` version confirmed already `0.1.0`, no bump needed. Local nghiệm thu green (cargo check/build, `sos --version`→`0.1.0`, actionlint, fixture fail-CLOSED + precedence tests). Docs updated: SECURITY.md, `.sos-trust-baseline` rebaselined, INSTALL.md/docs/SETUP.md, BACKLOG. **PENDING-release** (Worker boundary — no push/tag/release; Quản đốc/Sếp to merge branch `P081-distribution-stage1` + push tag `v0.1.0` + verify real Actions run). Full report: `docs/discoveries/P081.md`.

## 2026-07-23 — P080 round-2: dual-runtime brownfield dogfood — PASS, P080 DONE, P081 UNGATED
Re-ran on pristine fixtures post P080x merge (`1821dca`). D1 fixed: missing `block-env-commit.sh`/`no-code-on-default.sh` now fail-CLOSED (exit 1), negative control (both guards, clean commit, feature branch) exit 0, no new regression. A2 (dual render) + B1 (brownfield non-clobber) re-smoked clean. Task 3 (cross-runtime state, real `codex exec` 0.145.0) run by Quản đốc — C1/C2/C3 all PASS (main-thread write, actor-marker BLOCK, reverse-direction consistency). 3 operational caveats logged (sandbox default read-only, hook enforcement silent-fails on untrusted repos, `trusted_hash` is content-based). E2 (Linux) formally DEFERRED per Sếp decision (not a gap). **Verdict: PASS. P080 DONE. P081 distribution UNGATED.** Full report: `docs/adapters/P080-FINDINGS-2026-07-23.md` (ROUND-2 section), `docs/discoveries/P080.md`.

## 2026-07-23 — P080 round-1: dual-runtime brownfield dogfood — FAIL, D1 (HIGH) fail-open regression found
Ran `[Thợ-local]` Nhóm A (fresh-dual incl. A2-rev+A5) + B (brownfield) + D (regression+sync/map) on fresh git fixtures. Dual-render (Claude `sos new`/`sos adopt` + Codex `sos install --runtime codex`) co-exists cleanly both orders — non-clobber holds, `core.hooksPath` arms once, no cross-runtime file destruction. **D1 FAIL:** the dev `[8/8]` `hooks/pre-commit` (Claude path, `sos new`) is fail-OPEN on missing `block-env-commit.sh`/`no-code-on-default.sh` — the exact bug P078i fixed, but the fix only landed in the Codex-side backstop-minimal hook, never ported to the dev hook. Proved live: removed the script + rebaselined trust-gate (removing the confound) → real `.env` secret committed clean (exit 0). A5 (uninstall dual) reclassified N/A not FAIL — `uninstall()` is an honest empty stub on both adapters with zero CLI wiring, same class as `ClaudeAdapter::plan()`. Task 3 (cross-runtime state, real `codex exec`) and E2 (Linux) still PENDING `[Sếp+Codex]`. Next: `P080x-hook-fail-open-parity` (Tầng 1, Debate), then P080 round-2. P081 stays gated. Full report: `docs/adapters/P080-FINDINGS-2026-07-23.md`, `docs/discoveries/P080.md`.

## 2026-07-23 — P078i: FIX (SECURITY) `sos install` renders a self-contained, fail-CLOSED backstop hooks/pre-commit (not the dev [8/8] hook) — closes P079 round-4 A3/A4
P079 round-4 ("structural-oracle-gap lần 3"): P078g correctly rendered+armed `hooks/pre-commit`/`pre-push`, but the content it rendered was the kit's own dev `[8/8]` hook, which delegates every phase to `scripts/*.sh`+`docs-gate` and fail-OPENs when a script is absent — a fresh brownfield `sos install` never renders that `scripts/` tree, so `.env`/code-on-default commits silently passed. Fix, route (a) (Debate Log Turn 1 recommendation): new purpose-built minimal `crates/sos-install/src/templates/backstop-pre-commit.sh` enforcing ONLY 2 invariants, fail-CLOSED (missing guard → `exit 1`, never "allowed") — replaces the dev hook on the INSTALL path only. `render_embedded_hooks()` (`crates/sos-install/src/engine.rs`) co-renders the backstop hook + its 2 delegated guard scripts (embedded verbatim, single-source with the kit's real `scripts/block-env-commit.sh`/`no-code-on-default.sh`) in one pass — 4 artifacts total, all chmod+x. `sos new` dev path untouched (zero overlap, confirmed via call-site grep: `render_embedded_hooks`'s only caller is `commands/install.rs`). New pristine-repo (zero seeding) test `pristine_install_renders_closed_dependency_set_and_blocks_real_env_and_code_commits` closes the 3-round recursion mechanically: greps the rendered hook's `scripts/*.sh` refs, asserts each exists in that SAME install's own render output — plus real `.env`/code-on-default commit BLOCK, clean-commit negative control, and a companion fail-closed-when-guard-deleted test. `cargo test -p sos-install`: 19/19 pass (15 pre-existing + 4 new); `cargo test --workspace` green; `cargo build --release` clean. Docs: `SECURITY.md` new section (gap-history 2→3 fixes), `adapters/codex/CAPABILITY.md` gap #4 correction (P078g's "genuinely blocked" claim was conditional, now unconditional), `docs/BACKLOG.md` P078i flipped DONE. Shipped on branch `P078i-selfcontained-backstop-hook`, not yet merged. Full report: `docs/discoveries/P078i.md`.

## 2026-07-23 — P078g: FIX (SECURITY) `sos install` RENDERS real hooks/pre-commit+pre-push before arming; never arms an empty hooksPath
P079 round-3 (Test A2/A3/A4 FAIL): P078f armed `core.hooksPath=hooks` but neither runtime's adapter `plan()` ever rendered the hook scripts to disk — false-armed empty backstop, real `.env`/code-on-default commits went through unblocked. Worker CHALLENGE Turn 1 confirmed the phiếu's own `new.rs`-copy-route assumption WRONG (Anchor #4: `sos install` has no `SOS_KIT_DIR`, runs standalone) and self-resolved a route correction within Constraint 1's authority — no Architect respond turn needed. Route: EMBEDDED-ARTIFACT, `include_str!` bakes `hooks/pre-commit`+`pre-push` into the `sos-install` crate at compile time; new `render_embedded_hooks()` runs in `engine::apply()`'s success path right before `arm_git_hooks()` (single engine call-site, symmetric both runtimes, no per-adapter-plan route needed — `ClaudeAdapter::plan()` is a stub anyway). Hardened `arm_git_hooks()` (now `pub`, was private) with a new `hooks_present_and_executable()` check right before `git config --local core.hooksPath hooks` — refuses to arm (loud `Err`) if either script is absent/non-executable, never arm-empty. 5 new fresh-no-seed tests in `crates/sos-install/tests/install.rs` (all use a `Plan` with ZERO hook ops, fixing P078f's own structural-oracle-gap where its 5 tests seeded hook content into the synthetic `Plan`): render-fresh byte-match+chmod+arm-after-render, **genuine end-to-end `.env` commit block** (real `git commit` runs the real armed `hooks/pre-commit`, which runs the real `scripts/block-env-commit.sh` seeded into the fixture — blocked; clean commit passes as negative control), refuse-when-absent (`arm_git_hooks()` called with nothing rendered → `Err`, hooksPath stays unset), render non-clobber (pre-existing customized hook preserved). `cargo test -p sos-install`: 15/15 pass (10 pre-existing incl. all P078f regressions + 5 new); `cargo check --workspace` clean. Known asymmetry, pre-existing, out-of-scope: `ClaudeAdapter::plan()` still a stub (0 rendered assets) — unrelated to this fix, which happens at the engine level below any adapter's plan. Docs: `SECURITY.md` rewrote the hook-arming section with full gap-history, `adapters/codex/CAPABILITY.md` Gap #2 updated with the P078f→FAIL→P078g story, `docs/BACKLOG.md` P078g flipped DONE + resume pointer. Shipped on branch `P078g-install-render-real-hooks`, not yet merged (Quản đốc/Sếp gate merge). Full report: `docs/discoveries/P078g.md`.
## 2026-07-23 — P078h: FIX (SECURITY) Codex adapter actor-check advance-block (gap #3) + patch-path normalize (gap #4)
Closes the P079 round-3 §B4 self-approve **advance** hole (distinct from P078e's create/first-approval fix): a marked actor (`.sos-state/worker-active`/`architect-active` present) writing `.sos-state/ticket-state.env` no longer falls through to the pre-patch-only version-match check — it BLOCKs outright for create/update/advance alike, closing the `V3/V3 -> V4/V4` self-approve-advance found live on Codex. Also normalizes repo-root-absolute `apply_patch` paths to relative (conservative string-prefix strip, no symlink/`..` resolution) before both the state-file-allow and multi-path checks, fixing a live false-block round-3 found. 8 new tests + 2 negative-tests performed-and-reverted (both confirmed red without the fix). `crates/sos-adapter-codex/src/{templates.rs,lib.rs}` only — 60/60 pass, ×20 = 0-flaky. Codex custom-role subagent-spawn markers remain absent (upstream `openai/codex#21753`, unaffected) — fix is SOUND on Claude + manual-marker Codex repro, best-effort on real Codex subagent spawn. Branch `P078h-actor-check-state-write`, not yet merged. Full report: `docs/discoveries/P078h.md`.

## 2026-07-23 — P078f: FIX (SECURITY) `sos install` arms Git hooks by default (core.hooksPath + F09 hijack-guard)
Gap #2 from P079 round-2 (split from P078e per Worker CHALLENGE O2.1) — `sos install` used to render `hooks/{pre-commit,pre-push}` but never activate them (`core.hooksPath` stayed unset), rendering the declared Git backstop off-by-default. Ported `scripts/install-hooks.sh`'s 4-step arming (chmod +x tracked hooks, F09 hijack-guard TTY-confirm/non-TTY-abort, `git config --local core.hooksPath hooks`, rename stale `.git/hooks/*` to `*.pre-hookspath.bak`) engine-natively into `crates/sos-install/src/engine.rs`'s `apply()` (new `arm_git_hooks()`, runs on success path only, after manifest commit) — decision: engine-native not shell-out, per P059/P072 Windows-portability precedent; `git config`/`git rev-parse` via `std::process::Command`, `chmod` via Rust std Unix perms `#[cfg(unix)]`. Symmetric for `--runtime claude`/`codex` by construction (`run_adapter()` shared call-site, `crates/sos-cli/src/commands/install.rs`, Task 3 no-op). Non-git-repo → warn-skip; idempotent (own prior `hooks` value never re-trips guard). 5 new tests in `crates/sos-install/tests/install.rs` against REAL temp `git init` repos: hooksPath+chmod, stale-hook `.bak` rename with content preserved, non-clobber abort on foreign hooksPath (non-interactive), non-git warn-skip success, idempotent re-install. `cargo test --workspace` green (11/11 in install.rs, 0 regressions). Docs: `SECURITY.md` new section + updated "not yet shipped" pointer, `adapters/codex/CAPABILITY.md` Gap #2 pointer updated, `docs/plans/P078d-decomposition.md` status header, `docs/BACKLOG.md` P078 row flipped DONE. Shipped on branch `P078f-install-arm-git-hooks`, not yet merged (Quản đốc/Sếp gate merge). Full report: `docs/discoveries/P078f.md`.

## 2026-07-23 — P078e Task 1: FIX (SECURITY) Codex adapter approval-transition deadlock closed + self-approve actor-check (Gap #2 arm-hooks split to P078f)
P079 round-2 found bootstrap (`version=V1, approved_version=empty`) BLOCKed the first legit approval write (`version=V2, approved_version=V2`) — circular deadlock. `scripts/codex/approval-gate.sh` state-file-alone exemption (P078d2a #5, create-when-missing) extended to create+update, gated on a NEW actor-check (`.sos-state/worker-active`/`architect-active` both absent, mirrors `orchestrator-guard.sh:596`) so only the orchestrator main-thread can exercise it — worker/architect subagent turns still fall through to version-match BLOCK, closing the self-approve hole Worker CHALLENGE (O1.1) flagged pre-EXECUTE. d2a all-path check (#6) unchanged, still blocks bundled patches. Codex caveat honest (actor-check best-effort/not-SOUND on Codex per d2b `#21753`; real backstop = human-review-at-commit). Gap #2 (`sos install` arm-hooks) split to **P078f** per Worker's O2.1 objection (Architect accepted — `install-hooks.sh` carries real F09 security-arming beyond bare `git config`, meeting the phiếu's own pre-declared split threshold). Tests: round-2 repro ALLOW, self-approve BLOCK ×2 (worker/architect marker), pre-approval-code-edit regression BLOCK, bundle-BLOCK preserved; negative-test (actor-check removed → self-approve test flips ALLOW) confirmed then reverted. `cargo build/test --workspace` green, 53/53 `sos-adapter-codex` (was 48), ×20 = 0 flaky, dep-direction green, additive (engine/install/core/adapter-claude untouched). Docs: `CAPABILITY.md` §4 + `SECURITY.md` updated, trust-gate no-rebaseline-needed (SECURITY.md unicode-scanned only, not a baseline surface). Full report: `docs/discoveries/P078e.md`.

## 2026-07-23 — P085: codify phiếu-decomposition heuristic in ORCHESTRATION.md (guidance, not a gate)
Added section "Phiếu decomposition heuristic (guidance, not a gate) (P085)" between "Tier routing (P036)" and "Trigger phrases" in `docs/ORCHESTRATION.md` — 5 SPLIT signals harvested from real sprint evidence: (1) lane budget exceeded — the only mechanical gate, `docs/WORKFLOW_V2.2.md` §1; (2) incompatible oracles (VD P077c: c1-c4 parity `Rust==Bash` vs c5 correctness `Rust≠buggy-Bash`); (3) external-input blocker (VD P078d2a ships now vs d2b waited on `SubagentStart` probe); (4) security-surface isolation (VD P078b3, P078d2a — both AUTO Tầng-1 security surfaces); (5) delivery/rollback clarity (one-ticket-one-delivery, `core/WORKFLOW.md:75-81`). KEEP-WHOLE caveat (VD P078a — single cohesive spec, no split gain). Framing explicit throughout: lane budget (§1) is the sole hard gate, everything else judgment/guidance (§0.1 Luật 3) — no second gate created. `agents/orchestrator.md` mirrors with a 1-line pointer only (not added to numbered Hard rules), single-source stays ORCHESTRATION.md. All 4 example phiếu IDs verified against real file content before shipping (not just filename existence). Docs-only, Tầng 1 (contract surface per CLAUDE.md Rule #9), no code/schema/API touched. Iterable per Sếp's 2026-07-22 "fix nhẹ sau" decision — lives outside `docs/WORKFLOW_V2.X.md`, no retro process required to adjust. Full report: `docs/discoveries/P085.md`.

## 2026-07-22 — P078d2b: DOCS + DECLARATION Codex adapter in-subagent role-envelope enforcement declared MISSING (upstream openai/codex#21753); SubagentStart-marker deprecated best-effort; P078d DONE
Probe (`docs/adapters/SUBAGENT-HOOK-PROBE-2026-07-22.md`) confirmed: Codex 0.145.0 does NOT dispatch `SubagentStart`/`SubagentStop` (nor in-subagent `PreToolUse`) for custom-role spawns (`architect`/`worker`) — only `agent_type="default"` fires; upstream `openai/codex#21753`. Dogfood-confirmed directly (`docs/adapters/P079-CODEX-DOGFOOD-FINDINGS-2026-07-22.md` #4): a spawned architect's forbidden `apply_patch` on Rust `src/` succeeded in-subagent, marker never created. `CodexAdapter::verify()` (`crates/sos-adapter-codex/src/lib.rs`) gains Finding #6 (`FindingStatus::Missing`, cites `#21753`) — 5→6 Findings, count-test + dedicated content-test added; Findings #1/#3/#5 stay PARTIAL (main-thread + Git/CI backstops still real), 1 clarifying clause added to each ("main thread only, NOT in-subagent"). `templates.rs` `hooks_json()` SubagentStart/Stop marker GIỮ (kept, byte-identical render — Rust-comment-only deprecation note, zero JSON change); `agents_md()` gains 1 orchestrator boundary-review guidance bullet. `adapters/codex/CAPABILITY.md` new §6 (Claude-vs-Codex containment table + 3 real backstops: main-thread `PreToolUse` dogfood-confirmed / universal Git pre-commit-push agent-agnostic / AGENTS.md prose); `SECURITY.md` new threat-model paragraph ("trust the Git gate, not the subagent"); `MAPPING.md` "5→6 Findings". NOT a behavioral fix — `#21753` is upstream, no hack/workaround attempted. `cargo build/test --workspace` green (48/48 `sos-adapter-codex`), ×20 = 0 flaky, dep-direction green, additive verified (d2a guards/engine/core untouched), d1 render regression green. trust-gate: no rebaseline needed (SECURITY.md not in baseline `SURFACE_GLOBS`, only unicode-scanned — clean). **P078d = DONE (d1+d2a+d2b).** Full report: `docs/discoveries/P078d2b.md`.

## 2026-07-22 — P078d2a: FIX (SECURITY) Codex adapter multi-path guard bypass CLOSED + approval bootstrap deadlock + spawn caveat (#5/#6/#7)
`#6` (SECURITY HOLE, CLOSED): all 4 allow-list guards (`architect-guard.sh`/`orchestrator-guard.sh`/`block-env-edit.sh`/`approval-gate.sh`, `crates/sos-adapter-codex/src/templates.rs`) extracted only the FIRST `*** ... File: <path>` of a multi-file `apply_patch` (`head -n1`) — a ticket-allowed path first + a forbidden path second exited ALLOW without inspecting the rest. Fixed: extract every path (removing `head -n1` is sufficient — `grep -oE` already emits one line per match), loop with **block-if-any** semantics (was first-path-exempt-allow-all). Loop built as a herestring `while read` into an array (bash-3.2-compatible, macOS ships 3.2 — `readarray` isn't available) + a plain `for`, deliberately not a piped `while` (would swallow `exit 2` in a subshell). `#5` (fixed, coupled hard with #6): approval-gate's fail-CLOSED-on-missing-state-file used to deadlock fresh installs (no way to ever create `.sos-state/ticket-state.env`); fixed via a narrow self-bootstrap exemption (state file missing + patch touches ONLY that file → ALLOW) plus a rendered install-time skeleton (new 18th Asset `STATE_SKELETON`) whose non-clobber safety reuses the ALREADY-generic `sos-install::engine` checksum/manifest `Decision::Conflict` logic — zero engine change. Exemption is safe only because #6 lands in the same patch (a bundled second path is caught by the all-path check, not the bootstrap ALLOW) — verified by a dedicated coupling test. `#7`: `AGENTS.md` spawn-guidance line added (full-history fork inherits parent `agent_type` — the observed P079 first-spawn failure). Tests: 5 multi-path bypass BLOCK + 1 no-regress ALLOW + 2 bootstrap coupling tests, 47/47 total pass. **Negative-test performed and reverted**: re-adding `head -n1` flips exactly these 6 tests BLOCK→ALLOW, confirming the fix (and its subshell-safe loop construction) genuinely closes the hole. `cargo build/test --workspace` green, ×20 = 0 flaky, dep-direction green, `bash -n` clean. Additive: `#4` SubagentStart/Stop marker lifecycle deferred to **P078d2b** (needs live probe) — untouched. Full report: `docs/discoveries/P078d2a.md`.

## 2026-07-22 — P078d1: FIX Codex adapter 3 startup-schema blockers (config.toml table-scope, rules list-pattern, hooks.json field-set) + schema-shape oracle
3 startup-blockers from P079 live-dogfood (`crates/sos-adapter-codex/src/templates.rs`): config.toml root-keys now emit before the FIRST table header (not just before `[agents]` — Worker CHALLENGE caught the original fix-wording gap); rules `pattern` now token-list not bare string; hooks.json drops `_provenance`/`_partial_note` into `description`. 3 new schema-shape tests (real `toml`/`serde_json` parsers for config.toml/hooks.json, documented structural-string oracle for `.rules` since it's Starlark not TOML/JSON), all negative-tested (revert→FAIL, restore→PASS). Honest oracle-gap note: schema-shape PASS ≠ Codex-accepts, only live Codex 0.145.0 is ground-truth (same lesson P079 itself demonstrated against the old generic-valid oracle). Additive, enforcement content-fns untouched (P078d2 next). Full report: `docs/discoveries/P078d1.md`.

## 2026-07-22 — P078c: FIX install render-before-toolgate reorder — unblock P079 Codex dogfood
`sos install --runtime <codex|claude>` used to run tool-manifest check (`resolve_tools()?`, OA-07) BEFORE `apply()`/`dry_run()`, hard-aborting render on any required-tool drift — blocking Codex dogfood on a machine with real drift (`doctor` 0.1.1 < pinned 0.1.3, `inv-gate` MISSING) despite render being tool-version-independent. Reorder: render runs first unconditionally, tool-check runs after as a report — required drift/missing prints a loud WARNING (reusing `tools::describe_failure()`'s message) and exits **3** ("installed, tools-not-ready"), never silently swallowed; render/apply failure stays exit 1; all-ready stays exit 0. New opt-in `--require-tools` flag restores the pre-P078c fail-closed order verbatim for CI/production. `--dry-run` shows both plan + drift warning; `--require-tools --dry-run` still gates before printing anything. **Zero `sos-install` engine/tools change** — `apply()`/`dry_run()` never depended on tool state (confirmed by the engine's own doc comment) and `tools::check_tools()`/`required_drift()`/`describe_failure()` were already `pub` and non-`Result`; `install.rs` only changed which functions it calls and when. Two near-duplicate functions (`run_claude()`/`run_codex()`) collapsed into one shared `run_adapter()` — symmetric behavior is now structural. Stale comment fixed (`CodexAdapter.plan()` was claimed "still a b1 stub", false since P078a/b's real 17-artifact render). Live smoke on this drift machine: `install --runtime codex` → 17 real files written + loud warning + exit 3; `--require-tools` → exit 1, zero files; `--runtime claude` symmetric; `sos tools status` regression-verified unchanged (exit 1 on drift). `cargo build/test --workspace` green ×20 = 0 flaky, clippy 0 new warnings, additive-diff (`bin/sos.sh`/`install.sh`/`crates/sos-install/`) empty. OA-07 preserved (drift always loud + non-zero), not weakened. **P079 Codex dogfood now unblocked.** Full report: `docs/discoveries/P078c.md`.

## 2026-07-22 — P078b3: ADD Codex adapter enforcement (7 artifacts, fail-CLOSED apply_patch parse, mock-payload oracle) — P078b DONE
`all_assets()` extends 10 → 17 (`crates/sos-adapter-codex/src/templates.rs`): `.codex/hooks.json`, `.codex/rules/exec-policy.rules`, and 5 REWRITTEN `scripts/codex/*.sh` guards (architect/orchestrator/block-env/approval-gate/idea-smell). Anchor #1 CRITICAL (apply_patch marker syntax) — Worker CHALLENGE Turn 1 escalated (no SOUND oracle for a training-data prior); Sếp ran a live Codex CLI (gpt-5.6) probe, captured 4 real payloads (fixture: `crates/sos-adapter-codex/tests/fixtures/codex-apply-patch-payloads.jsonl`), confirming the V4A envelope (`*** Begin Patch\n*** Add|Update|Delete File: <path>\n...*** End Patch`). fail-CLOSED (inverted from Claude's fail-open precedent): unparseable `apply_patch` path → BLOCK on every guard. Approval-gate (E5, Codex native gap #4) is a from-scratch guard-build reading a `.sos-state/ticket-state.env` projection, read-compare only, never mutates. `block-unsafe-merge` semantic gate DEFERRED (mechanical force-push covered via `.codex/rules`). PARTIAL-honest declared 3 places consistently (hooks.json field, guard headers, CAPABILITY/MAPPING/SECURITY docs). Oracle: hooks.json valid JSON, all guards `bash -n` clean, 20-test `#[cfg(unix)]` mock-payload oracle (real + path-substituted-real fixtures) asserting block/allow correctness. `cargo build/test --workspace` green ×20 = 0 flaky, dep-direction green, additive-diff empty, `.codex`/`scripts/codex` never appear in sos-kit's tree. **P078b (b1+b2+b3) is now DONE** — remaining P078 scope = P079 behavioral live-dogfood. Full report: `docs/discoveries/P078b3.md`.

## 2026-07-22 — P078b2: ADD Codex adapter declarative render (10 artifacts, structural oracle)
`CodexAdapter::render()`/`plan()` (`crates/sos-adapter-codex/src/lib.rs` + new `src/templates.rs`) now LIVE — generates 10 declarative Codex-native artifacts (`AGENTS.md`, 4× `.codex/agents/*.toml`, 4× `.agents/skills/{idea,forge,apply,retro}/SKILL.md`, `.codex/config.toml`). Worker CHALLENGE Turn 1 confirmed trait shape sufficient (no escape hatch): `render()` is per-`Asset`, `plan()` enumerates a fixed 10-`Asset` set internally and calls `render()` on each, mapping to `ManagedOperation` — engine consumes generically, zero engine change. Content-source = crate-embedded template (`templates.rs`), no `core/**` filesystem read at render time; every artifact carries a core-ID pointer string, never a semantics copy. PARTIAL-honest: `architect.toml` carries the same PARTIAL marker as `verify()` Finding #1/`CAPABILITY.md`; `advisory-watch.toml`/`boundary-check.toml` render honest `sandbox_mode="read-only"`. Skill set = 4 — `init` deliberately deferred (declared gap in MAPPING.md, not silently dropped: Claude's `init` caller is CLI-bound, no Codex-native trigger yet). Open escape hatch: advisory-watch needs network but renders read-only sandbox — Codex network-gating semantics unconfirmed from the discovery report, documented in-template + MAPPING + this report, resolution deferred to P079 (behavioral). Oracle STRUCTURAL (Decision 5): 13 new unit tests (TOML parse validity, frontmatter validity, core-ID pointer presence, PARTIAL-marker presence/absence, artifact-count==10), run without Codex CLI installed. `cargo build/test --workspace` green ×20 = 0 flaky, dep-direction guard green, additive-diff empty on `bin/sos.sh`/`install.sh`/`engine.rs`/`sos-core`, zero `.codex`/`AGENTS.md`/`.agents` ever appeared in sos-kit's working tree. 2/3 of the b decompose — P078b3 (enforcement) follows. Full report: `docs/discoveries/P078b2.md`.

## 2026-07-22 — P078b1: ADD Codex adapter foundation (crate + detect/verify + PARTIAL-declaration mechanism + CLI wire)
New `crates/sos-adapter-codex` (deps CHỈ `sos-core`) — `CodexAdapter` implements the core `Adapter` trait: `detect()` structural (static Codex 0.145.0 facts + fail-safe `codex --version` probe, absence never panics), `verify()` reports exactly 5 known capability gaps with an explicit `FindingStatus` (new additive `Sound`/`Partial`/`Missing` enum on `Finding`, `crates/sos-core/src/adapter.rs`) — never `Sound` for a gap (`core/ROLES.md` sep-inv #5). `plan()`/`render()`/`uninstall()` stay minimal-honest stubs (P078b2/b3). Wired `sos-cli`: `install --runtime codex` no longer errors "not yet available" — drives the same engine as `--runtime claude`, zero engine change (`run_codex` mirrors `run_claude` 1:1). New `adapters/codex/{README,MAPPING,CAPABILITY}.md` mirroring `adapters/claude/`. CHALLENGE Turn 1: no Tầng-1 objections; anchor #7 self-closed correction (root `Cargo.toml` is a `crates/*` glob, no explicit-members edit needed). `cargo build/test --workspace` green ×20 = 0 flaky, dep-direction guard green, additive-diff empty on `bin/sos.sh`/`install.sh`/`sos-install/engine.rs`. Structural oracle (Decision 5): `--dry-run` reaches identical engine path as `claude`, full exit-0 blocked only by a pre-existing unrelated tool-manifest pin drift on this dev machine (affects both runtimes identically). Behavioral verification = P079. 1/3 of the b decompose — P078b2 (render) + P078b3 (enforcement) follow. Full report: `docs/discoveries/P078b1.md`.

## 2026-07-22 — P078a: ADD `core/STATE.md` portable serialization contract
New `core/STATE.md` codifies 6 adapter-shared formats (ticket storage/schema, lifecycle state artifact, approval record, edit allowlist, review trigger map, blocked format) lifted from the Codex Adapter Discovery Report's 11-item under-spec list (`docs/adapters/CODEX_ADAPTER_DISCOVERY_2026-07-22.md:26-28`); 4 remaining items (tier-classifier owner, concurrent-ownership lock/worktree, publish-actor division, backlog serialization) explicitly deferred to follow-up P078a2. Worker CHALLENGE Turn 1: no Tầng-1 objections, accepted V1. Anchor #14 (symlink) confirmed Claude guards don't normalize symlinks — spec kept the proposed fail-safe wording; anchor #15 found CLAUDE.md's repo-structure tree was missing `core/` entirely (pre-existing drift) — fixed by adding the full `core/` tree block. Neutrality grep 0-hit (no host token in `core/STATE.md`); backward-compat diff confirmed every format matches existing Claude behavior (TICKET_TEMPLATE/WORKFLOW/POLICY/orchestrator), with the state-file explicitly kept as an OPTIONAL derived projection — debate log stays authoritative per `core/WORKFLOW.md:49`, no conflict with orchestrator's "state truth = Debate Log" rule. `docs-gate --all` 28/28 PASS. Unblocks P078b (Codex adapter render). Full report: `docs/discoveries/P078a.md`.

## 2026-07-22 — P077f: RELOCATE — Rust workspace `bootstrap/sos-rs/` → repo-root, P077 fully DONE (a-f)
Pure `git mv` + path-fix, no logic/behavior change (reversible via `git revert`). `Cargo.toml`+`Cargo.lock`+`crates/` moved to repo-root; `bootstrap/` removed. Worker CHALLENGE Turn 1 found 2 Tầng-1 objections beyond the original anchor-grep, both ACCEPTED → V2: (O1.1) `crates/sos-install/src/tools.rs:29` `include_str!` had a hardcoded 5-hop relative path OUTSIDE the `CARGO_MANIFEST_DIR` family the anchors searched — fixed 5→3 hop (Task 2b); (O1.2) `scripts/orchestrator-guard.sh:71`'s `bootstrap/*` allow-list glob is a functional security-gate carve-out, not cosmetic — left unfixed it would have silently made the guard MORE restrictive on the moved Rust source (blocking Quản đốc edits without `worker-active` marker) — fixed by adding `crates/*`/`Cargo.toml`/`Cargo.lock` to the allow-list, `bootstrap/*` kept as dead glob (Task 3b). Also fixed: `crates/sos-cli/tests/parity.rs:246` CARGO_MANIFEST_DIR depth (4→2 hop); `bin/sos.sh` resolver `_sos_workspace_root` → repo-root; `.gitignore` `bootstrap/*/target/` → `/target/`; trust-baseline rebaselined (`bin/sos.sh` + `orchestrator-guard.sh` hash changed). New edge case found (not anticipated): `cargo test --workspace` initially failed post-move — a stale cached `sos-core` test binary embedded the OLD `CARGO_MANIFEST_DIR` via `env!()` (cargo doesn't invalidate on directory move when source content is unchanged); fixed via `cargo clean -p sos-core`. Verified: `cargo build/test --workspace` from repo-root green, ×20 parallel = 0 flaky, cutover smoke (launcher byte-identical to direct Rust binary, both shared-cache and non-redirect resolution paths), guard smoke (`crates/*/src/*.rs` exit 0 no-marker, other `src/*.rs` still exit 2), `bash -n` clean both scripts, `docs-gate --all` 27/27 PASS. Open doctrine question captured in `docs/BACKLOG.md` (not resolved here): should the guard eventually gate Rust CLI source as Worker-only product, not kit-maintenance allow-list? **P077 fully DONE (a-f).** Full report: `docs/discoveries/P077f.md`.

## 2026-07-22 — P077e: CUTOVER — Rust `sos` canonical for 6 heavy subcommands, `bin/sos.sh` becomes thin launcher
Approach (A), founder-confirmed. Dispatch flip in `bin/sos.sh`: 4 arms flipped (`new`/`adopt`/`sync`/`map` — previously Bash `sos_<cmd>`, now `exec` the Rust binary), 2 arms ADDED new (`install`/`tools` — Anchor #2 reconcile found the dispatcher had only 11 named arms, not 13; `install`/`tools` had ZERO prior Bash fn/arm to flip). New resolver `_sos_rust_bin()`: `SOS_RUST_BIN` override → build-dir candidates (`CARGO_TARGET_DIR`/workspace `target/`) → `cargo build --bin sos` on-demand → fail-LOUD (exit 127, no silent Bash fallback — dormant `map` still carries bug OA-02). Deliberately never calls `command -v sos` (confirmed via `install.sh:151-158`: the launcher installs itself literally AS `sos` on PATH — would recurse). Old `sos_new`/`sos_adopt`/`sos_sync`/`sos_map` kept DORMANT (commented, not deleted — rollback safety + parity-test oracle). Contract flip: root `CLAUDE.md` "What this repo is NOT"#1 + Rules#1 + repo-structure now describe sos-kit as runtime monorepo for `sos` (sister CLIs still external); `bootstrap/sos-rs/README.md` ownership flipped (canonical, not extracting; relocate=P077f deferred); `docs/PORTABILITY_ARCHITECTURE.md` + `docs/SETUP.md` status notes added. `install.sh` zero-touch (verified no diff; prebuilt-binary bootstrap = P081). Verified: cutover smoke (heavy cmd via launcher byte-identical to direct Rust binary invocation, incl. `tools status` live-drift table match), fail-LOUD (corrected test — stripped `cargo` from PATH + invalid override + fake build dir → exit 127, no dir created, no Bash fallback ran), 7 guidance cmds unchanged (`status`/`init` verified), `bash -n` clean, `cargo test --workspace` ×20 = 0 flaky, `docs-gate --all` 26/26 PASS, trust-gate rebaselined (auto-exec surface `bin/sos.sh` changed). No Tầng-1 objections in CHALLENGE; no escalations in EXECUTE. **P077 core cutover DONE. Next: P077f (relocate, deferred) → P078 (Codex adapter).** Full report: `docs/discoveries/P077e.md`.

## Index

| Phiếu | Date | 1-line summary |
|---|---|---|
| [P075](discoveries/P075.md) | 2026-07-20 | Portable core: six neutral contract files, capability-based roles, lifecycle/policy/asset ownership, one-ticket/one-remote-delivery rule; zero runtime-token oracle passes |
| [P074](discoveries/P074.md) | 2026-07-20 | Runtime boundary inventory; ownership A locked: one monorepo/version/`sos`, modular core + Claude/Codex adapters; managed tool manifest preserves one-command UX without big-bang source merge |
| [P073](discoveries/P073.md) | 2026-06-15 | Trust gate: 20-surface baseline-diff + hidden-unicode gate; 2 U+FEFF escaped pre-enable; BSD grep-P gap → byte-pattern fallback; 4 discrimination tests PASS; [8/8] phase wired; SECURITY.md written |
| [P072](discoveries/P072.md) | 2026-06-15 | Fleet Node20 bump: all 10 repos rc-oracle PASS (3/3 green, 0 annotations, prerelease=true, latest unchanged); guard/vps/doc-rotate/advisory-cron upgraded to full draft/publish+sha256 template (P071+P072 combined for optionals); P009 oracle gap documented (github-script@v7 invisible to P009 because P071 added it after P009) |
| [P071-stage1](discoveries/P071-stage1.md) | 2026-06-15 | Stage 1 fan-out: .sha256 publish pattern applied to 5 REQUIRED repos (claude-hooks 0.9.2, docs-gate/ship/advisory-inbox/inv-gate 0.1.1); all CI GREEN; all 3 targets carry .sha256; inv-gate @v3 draft/publish confirmed working; Stage 2 gate condition met |
| [P052](discoveries/P052.md) | 2026-06-08 | git-level .env* block gate — `scripts/block-env-commit.sh` + pre-commit [7/7]; 9/9 fire-test cases PASS; .envrc deliberately excluded (O1.1 locked as test); regex verbatim with block-env-edit.sh; Tầng 1 docs updated |
| [P053](discoveries/P053.md) | 2026-06-06 | sentinel deadlock fix — PR mode always-post sentinel incl. clean APPROVE; dry-run grep simulation PASS; stale-sentinel documented + [P055] filed; Tầng 1 docs updated (boundary-check.md, README.md) |
| [P050](discoveries/P050.md) | 2026-06-06 | no-code-on-default gate — `scripts/no-code-on-default.sh` + pre-commit [6/6]; 17/17 test cases PASS; .gitignore dir-pattern vs glob-form self-adapted (Tầng 2); all 6 ket-harvest constraints present; Tầng 1 docs updated |
| [P042](discoveries/P042.md) | 2026-05-25 | Giám sát (boundary-check) — 5 generic INV port from tarot (drops nginx+credits), sentinel rename, ADVISORY-only; Anchor #18 Bash allowlist confirmed non-blocking; 0 tarot refs; Wave 1 sprint COMPLETE |
| [P041](discoveries/P041.md) | 2026-05-25 | Trinh sát (advisory-watch) — generic port from tarot; pnpm+npm parsers shipped; CHALLENGE caught 3 real V1→V2 issues (PyYAML, parser location, Bash scope); 0 tarot refs in delivered files |
| [P043](discoveries/P043.md) | 2026-05-25 | Doc drift consolidate — Quản đốc persona codify + alignment engineering + deferred-tool loading; tarot skeletons self-contained; CHALLENGE caught concrete Thay bằng gap V1→V2 |
| [P040](discoveries/P040.md) | 2026-05-25 | Bootstrap stack detection — `sos init security` + 6 parser stubs; .gitignore __pycache__ expansion; CHALLENGE caught dash/underscore Tầng 1 issue pre-EXECUTE |
| [P005](discoveries/P005.md) | 2026-05-10 | Skills are Orchestrator-only — option B locked (frozen-artifact pattern in phiếu Context) |
| [P006](discoveries/P006.md) | 2026-05-10 | docs-gate bootstrap: missing-config guard in hook + dogfood config + template |
| [P039](discoveries/P039.md) | 2026-05-05 | Doc drift + symmetry sweep (10 surgical edits, Tầng 2; orchestrator-fetch lesson) |
| [P038](discoveries/P038.md) | 2026-05-02 | Phiếu lifecycle cleanup + safety rails + DISCOVERIES decoupling |
| [P037](archive/DISCOVERIES_pre-2026-05.md#p037----2026-04-27----pre-approve-marker-bash-ops-permission-prompt-fix----first-skip-challenge-phiếu) | 2026-04-27 | Marker file pre-approve template (skip-CHALLENGE first dogfood) |
| [P036](archive/DISCOVERIES_pre-2026-05.md#p036----2026-04-27----tier-routing--architect-humility-markers--path-drift-fixes-v2) | 2026-04-27 | Tier routing + humility markers + path-drift fixes |
| [P035](archive/DISCOVERIES_pre-2026-05.md#p035----2026-04-27----orchestrator-handbook--bulk-input-rule--install-anti-patterns-v3) | 2026-04-27 | Orchestrator handbook + bulk-input rule |
| [P004](archive/DISCOVERIES_pre-2026-05.md#p004----2026-04-26----vision-doc-naming-flex-charactermd-glob) | 2026-04-26 | Vision doc naming flex (CHARACTER*.md glob) |
| [P003](archive/DISCOVERIES_pre-2026-05.md#p003----2026-04-26----backlog-format-flexibility-banner-fallback--architect-rule-0--orchestrationmd) | 2026-04-26 | BACKLOG format flexibility |
| [v2.1-dogfood](archive/DISCOVERIES_pre-2026-05.md#v21-dogfood----2026-04-26----debate-flow-proven-on-tarot-p029--p030) | 2026-04-26 | Debate flow proven on Tarot (P029 + P030) |
| [P002](archive/DISCOVERIES_pre-2026-05.md#p002----2026-04-26----tarot-voicecharacter-template-harvest) | 2026-04-26 | Tarot voice/character template harvest |
| [P001](archive/DISCOVERIES_pre-2026-05.md#p001----2026-04-26----architect--worker-debate-loop) | 2026-04-26 | Architect ↔ Worker debate loop (initial framework) |

<!-- Future entries: 1 line per phiếu, link to per-file. Worker on EXECUTE adds entry as last step before commit. -->

- **[P080x](discoveries/P080x.md) · 2026-07-23** — dev `[8/8]` hook `hooks/pre-commit`: ported P078i's fail-CLOSED semantics into `[6/8]` no-code-on-default + `[7/8]` block-env (missing guard script → `FAIL_COUNT++`/blocked, not `⏭ skip`/silently allowed). Closes P080 round-1 gap D1. Pristine fixture (no seeding): missing-guard `.env`/code commits BLOCKED exit 1, both-guards clean commit exit 0 (negative control), both-guards `.env` still BLOCKED (regression), 2-directional revert/restore proof. `[1-5]`/`[8]` untouched by design (non-security or deferred), phase count `[8/8]` unchanged.
- **[P078j](discoveries/P078j.md) · 2026-07-23** — guard path canonicalize (symlinked roots): upgraded P078h's lexical `$REPO_ROOT/` prefix-strip to canonicalize BOTH `REPO_ROOT` and every absolute candidate path (dirname-resolve, macOS-BSD-portable, not-yet-existing-target-safe) — closes round-4 §B4 both directions (marked-actor advance leak via `/tmp` alias, main-thread false-block via same alias). Real symlink fixture matrix + two-directional negative-test (revert flips both gap-B tests red simultaneously). 64/64 tests pass, ×20 flake-clean.
- **2026-07-23 · SESSION LESSON (durability gap)** — private Claude memory is a Claude-only side-channel; durable resume-state must live in repo artifacts agents READ (BACKLOG/CHANGELOG/git/discoveries). This session BACKLOG went stale (P076/P078/P079 unchecked after ~30 phiếu) because updating it relied on agent discipline, not a gate. `docs-gate` does NOT catch a stale BACKLOG. → RESEARCH 2026-07-24 a git-level (agent-agnostic, Codex-safe) mechanical gate forcing any agent to record durable state. See BACKLOG '▶ NEXT' + memory feedback_durable_state_mechanical_gate.
